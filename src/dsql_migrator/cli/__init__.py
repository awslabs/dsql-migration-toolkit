"""Command-line interface layer.

Exposes a single console entrypoint (``mysql-dsql-migrator``) that wraps the core
engine and can launch the NiceGUI UI. Commands are extended in later tasks.
"""

__all__: list[str] = []
