# 変更履歴 (Changelog)

_言語: [English](CHANGELOG.md) | [한국어](CHANGELOG.ko.md) | **日本語**_

このプロジェクトの主要な変更点はすべてここに記録されます。本プロジェクトは
[セマンティックバージョニング(semver)](https://semver.org/)に従います(バグ修正はパッチリリース)。

## v0.1.281

### 修正

- **スキーマ変換の正確性修正(監査 C11, U1, U2, U3)。**
  - *C11 — `TIMESTAMP DEFAULT CURRENT_TIMESTAMP` のタイムゾーン.* MySQL `TIMESTAMP` は DSQL
    `timestamptz` にマッピングされますが、`CURRENT_TIMESTAMP` の既定値が `now() AT TIME ZONE 'UTC'`
    (naive)で包まれ、timestamptz 列がセッション TimeZone で再解釈して、既定値による挿入がオフセット分
    ずれていました。naive-UTC ラッパーは今後 DATETIME → plain `timestamp` ターゲットにのみ適用し、
    timestamptz は素の instant を保ちます。
  - *U1 — `TINYINT(1) UNSIGNED` の誤ラベル.* Evaluation が「boolean にマッピング」と表示(発生しない
    0/1 ロード失敗を警告)していましたが、変換器は `smallint` にマッピングします。assessor が unsigned
    形を除外し、変換器と一致させました。
  - *U2 — `TIME` の範囲.* MySQL `TIME` は継続時間(−838:59:59..838:59:59)、DSQL `time` は時刻です。
    範囲外の値は事前シグナルなくロード中に行単位で失敗していました。Schema Conversion が事前に警告する
    ようになりました(継続時間を格納する列は interval/text に再マッピング)。ENUM/BIT/YEAR と同じパターン。
  - *U3 — COMPOSITE_KEY での 24 インデックス上限の off-by-one.* インデックス上限の警告が COMPOSITE_KEY
    が追加する UNIQUE インデックスを無視していたため、ソースインデックス 23 個を composite key で変換すると
    25 個(> 24)になるのに事前チェックを通過し、ロード後に追加の CREATE INDEX ASYNC が失敗していました。
    変換で追加されるインデックスを数えるようにしました。

## v0.1.280

### 修正

- **CDC オフセットシーダーが、binlog ファイル名のロールオーバーで先行したコネクタを巻き戻さなくなりました
  (監査 C9)。** すでにウォーターマーク以上に先行したコネクタの再シードをスキップする no-clobber ガードが、
  binlog ファイル名を辞書順で比較していました。MySQL の binlog サフィックスはゼロ埋めですがロールオーバーで
  桁が増え(`mysql-bin.999999` → `mysql-bin.1000000`)、そこで文字列比較が反転し(`'1000000' < '999999'`)、
  先行したコネクタを遅れていると誤判定して再デプロイ時に巻き戻し、既にストリーミング済みの変更を再生して
  いました。今後は解析した数値の binlog シーケンスで比較します(解析不能なサフィックスのみ辞書順に
  フォールバック)。オフセットシーダー Lambda zip を再ビルドし `PLUGIN_VERSION` を `v23` に上げました
  (`PLUGIN_VERSION` の更新は CDC インフラの削除 + 再デプロイで反映されます)。

### 備考

- 監査 C10(composite-key の再キーイングで leading 列を変更すると行が重複しうる)はコード変更不要:
  composite-key 戦略は既に元の主キーに UNIQUE インデックスを発行するため、変更を伴う更新は静かな重複ではなく
  unique 違反 / DLQ イベントとして表面化し、不変性の要件は opt-in 時点で既に警告されています。

## v0.1.279

### 修正

- **Full Load のバッチ byte-cap が、サイズの偏ったバッチでも守られるようになりました(監査 C7/P1)。**
  単一の書き込みトランザクションが DSQL の 10 MiB 制限を超えないようバッチを分割します。従来の分割器は各
  バッチの**最初の行だけ**をサンプリングし、その外挿値が予算以下ならバッチの残り行の行単位バイトチェックを
  スキップしていました — そのため最初の行が小さく(空/NULL テキスト)後続行が大きい(BLOB/JSON)バッチが
  上限を大きく超えていました。下流の再帰分割で回復はされていましたが、公表された上限は黙って未適用でした。
  分割器は実際の累積バイト合計を保持し(各行を一度だけ推定して加算)、次の行が予算を超える時点で即座に
  flush します。単一の過大な行は引き続き自身のバッチを形成します。メモリはバッチ 1 つ単位で bounded を維持。

## v0.1.278

### 修正

- **検証の健全性:「match」なのに実際は異なるデータを通してしまう複数の経路を、閉じるか明示しました(監査 C1〜C6, U4)。**
  - *checksum トークン衝突(C4).* 各列の値を `|` セパレータで連結する前にエスケープ(`~`→`~~`, `|`→`~|`)し、
    `|` を含む値が列境界を越えて区切りをずらせないようにしました(`CONCAT_WS('|','a|','b')` と
    `CONCAT_WS('|','a','|b')` が両方 `a||b` になっていた問題)。NULL センチネルは偽造不能な `~N`
    (エスケープ済みの実値では決して生成できない)にし、旧 literal-`<NULL>` 対 SQL NULL の衝突を閉じました。
    エスケープは両エンジンでバイト一致し backslash-free です(以前 backslash 方式が MySQL/PG 間で食い違った)。
  - *DATETIME タイムゾーン(C3).* plain `timestamp`(DATETIME)の checksum 項を `AT TIME ZONE 'UTC'` なしで
    直接レンダーするようにしました — `timestamp without time zone` ではそれは no-op ではなく(セッション
    TimeZone を介して変換され wall-clock がずれる)、シフトを起こしていました。`timestamptz` は引き続き使用します。
    DSQL 接続に `TimeZone=UTC` も固定し、比較がセッション既定値に依存しないようにしました。
  - *正直なラベル/明示(C5, C1/C2, C6, U4).* readiness チェックは ROW_COUNT モードでは「Row counts match」
    (値は未比較)、CHECKSUM モードでのみ「Data identical」と表示し、後者では FLOAT/DOUBLE・JSON が値比較
    されないことを明示します。PK reconcile チェックは「No missing or extra records」に改名(値の同一性ではなく
    キー集合の検証)、composite/非整数 PK の脚注は ROW_COUNT モードで「row count only」と表示(旧「count/checksum」
    の過大表現を是正)。

## v0.1.277

### 修正

- **`FLOAT UNSIGNED` / `FLOAT(M,D) UNSIGNED` 列がテーブル全体の変換を中断させなくなりました。**
  sqlglot の MySQL ダイアレクトは `float unsigned` を単独の型として解析できず、それを含むテーブルが
  「自動変換できません」プレースホルダ(CREATE TABLE なし)に落ちていました — なのに Evaluation は依然
  AUTO/互換と評価し、運用者が診断できない矛盾でした(監査 B1)。近似数値型で unsigned は表現できず保存上の
  意味も持たないため、除去して `real` にマッピングします — `DOUBLE UNSIGNED` と同じ扱いです。整数の
  `UNSIGNED`(範囲拡張)と `DECIMAL UNSIGNED` は変更しません。
- **MySQL のプレフィックスインデックス(`KEY (col(N))`)が、黙って全列インデックスになりロード後に失敗する
  代わりに表面化されるようになりました。** Aurora DSQL にはプレフィックスインデックスの等価物がないため
  変換器は列全体をインデックスしますが、値が DSQL の約 255 バイトのインデックスキー上限を超える可変長列は、
  テーブルとデータが既にロードされた**後で** `CREATE INDEX ASYNC` が失敗し、事前のシグナルがありませんでした
  (監査 B2)。今後はソースからプレフィックス長を反映し(`IndexDef.prefix_lengths`)、Schema Conversion が
  インデックスと列を挙げた警告を表示するので、運用者は値が収まるか確認するか — 境界のある部分文字列の式
  インデックスに置き換えるか — をロード前に行えます。インデックス DDL は引き続き発行されます(警告は助言)。

## v0.1.276

### 修正

- **Full Load が、torn read を照合できないテーブルをリーダーシャーディングしなくなりました — 本番経路の
  クロスシャード torn-read データ損失の窓を閉じました。** リーダーシャーディングは大きなテーブルの読み取りを
  K 個の互いに素な PK 範囲に分割して並行ストリーミングし、各シャードが独立したタイミングで
  `START TRANSACTION WITH CONSISTENT SNAPSHOT` を開きます。ロード中にソースへ書き込みが起きると、複数行の
  ソーストランザクションがシャード間で裂かれることがあります(ある行はシャード A のスナップショットに、その
  兄弟行はまだシャード B にない)。これは CDC ストリームがスナップショット後の書き込みを照合する場合にのみ
  安全です。クリーンな **replace**(plain INSERT、CDC なし)や **非 CDC の append** には照合するものが
  ないため、単一リーダーで読む必要があります(スナップショット 1 つ = 1 時点のカット)。単一プロセス経路は
  replace のみガードし、非 CDC append のシャーディングは依然許可しており、さらに深刻なことに
  **マルチプロセス経路(テーブル並列度 > 1 のときの本番デフォルト)**は純粋に「単一整数 PK がある」だけで
  シャーディングを決め、replace/CDC 状態も `full_load_reader_shards` のオフスイッチ/上限も無視して replace と
  非 CDC append をそのままシャーディングしていました(監査 D1 + C12)。今後は両経路とも **CDC-coexisting の
  ときだけ**シャーディングし、マルチプロセスプランナはシャード数をワーカープール予算ではなく(ソース接続の
  上限でクランプした)`full_load_reader_shards` から導出します。シャーディングしないロードは変更なし。

## v0.1.275

### 修正

- **失敗した identity シーケンス同期が、成功として報告されなくなりました。** カットオーバー前に、ツールは各
  `GENERATED BY DEFAULT AS IDENTITY` シーケンスを移行済みの行の先へ進め(`RESTART WITH max+1`)、
  アプリケーションの最初の insert が重複キーに当たらないようにします。この `ALTER` は DSQL の楽観的並行性下の
  DDL であり(40001 / concurrent-DDL を送出しうる)、IAM トークンもセッション中に失効しうるのに、失敗が
  `except: pass` で握りつぶされ「このテーブルにはサーバ生成キーがない」と区別できなくなっていたため、
  カットオーバーのランブックが緑色の *"Done — no server-generated key needed advancing."* で描画していました。
  すると運用者が遅れたシーケンスの上にアプリを再ポイントし、同期が防ごうとしていた当の重複キー障害に
  遭遇しえました(監査 D2)。`sync_identity_sequences` は今後テーブルごとに区別された結果を返します —
  `int`(進めた)/ `None`(何もしない)/ `str`(`RESTART` **失敗**、値を含まない理由付き) — 新しい
  `partition_identity_sync` ヘルパーが分類します。すべての呼び出し元(カットオーバーボタン、検証の自動同期、
  Full Load / accept-quarantine のロード後同期)が失敗を表面化します:ランブックは成功行の代わりに
  **error** 通知("Identity sequence sync failed — do not cut over yet"、リトライ / 手動 `RESTART WITH`
  の対処付き)を表示し、アクティビティログに `FAILURE` で記録します。identity-sync のセットアップ例外
  メッセージも値を含まないサニタイザを通します(Property 7、v0.1.274 と一貫)。

## v0.1.274

### 修正

- **Full Load のエラーログが、失敗した行の列値をディスクに書き込まなくなりました (Property 7)。**
  ドライバ(psycopg)エラーの `str()` はサーバの `DETAIL:` / `Failing row contains (...)` 行を保持し、
  この行には問題となった行の列値(重複メール、トークンなど)が含まれます。この生テキストが quarantine
  レコード、テーブル単位の失敗メッセージ、永続 NDJSON アクティビティログ、そして(ミラーされる)
  CloudWatch にそのまま保存されていました。今後はすべてのロード失敗箇所(quarantine レコード、バッチ結果の
  `first_error`、テーブル単位の失敗ハンドラ)で単一行のサニタイザ(`safe_error_message`)を通します —
  実行可能な主メッセージ(`duplicate key value violates unique constraint "…"`)と SQLSTATE は保持し、
  値を含む `DETAIL` 行を除去します。従来の `" ".join(str(exc).split())` では不十分でした — DETAIL 行を
  1 行に折りたたむだけで値はそのまま残っていました。DEBUG 専用のアクティビティログのスタックトレースも
  同様に修正 — (値を含まない)スタックフレームと `Type: 先頭行` の末尾のみを保持し、`format_exception` の
  値を含むメッセージ行を除きます。

## v0.1.273

### 変更

- **特大 LOB 除外は、CDC インフラのデプロイ後にロックされる際、黙って固まるのではなく理由を説明するように
  なりました。** 以前は cdc-stack が存在すると(Stop CDC でスタックと MSK のコミット済みオフセットが保持
  される場合を含む)、除外チェックボックスがメッセージなしで無効化され、CDC を停止した運用者には理由のない
  凍結ボックスがバグのように見えていました。ロック自体は正しく、維持されます:すでにストリーミングした
  パイプラインで除外列を変更すると(再開オフセットは Stop 後も残るため)、移行済みの行と以降に処理される行が
  食い違うため、除外はインフラの存続期間中は固定されます。ボックスはその理由と唯一の安全な対処 — CDC
  インフラを削除して新しい構成で再デプロイ — を明示するようになり、これはテーブルピッカーが同じスタック
  フェーズでロックする方法と一致します。この説明は neutral トーンで表示されます(デプロイ済みパイプラインは
  想定内の状態であり警告ではありません)。CDC 開始点とテーブル選択の挙動は変更されていません。

## v0.1.272

### 追加

- **特大 LOB 列の除外がマイグレーション全体に適用されるようになりました — CDC だけでなく Full Load も
  この選択に従います。** 値が Aurora DSQL の 1 値あたり約 1 MiB 制限を超えうる MySQL の
  `mediumtext`/`longtext`/`mediumblob`/`longblob` 列を外すオプトインの「特大 LOB 列」コントロールは、
  これまで CDC キャプチャ(Debezium `column.exclude.list`)にのみ反映されていました。今後は単一の
  マイグレーション全体の選択になります。チェックした列は Full Load の INSERT 列リストと CDC キャプチャの
  **両方**から外れるため、ギャップレスなハンドオフをまたいで 2 つのデータ経路が食い違うことがありません
  (片方だけで外すと、暗黙の部分データが残ります)。Full Load を含むマイグレーションでは、このカードが
  Full Load 画面の**テーブル選択の直後・前提チェックの直前**に表示されるようになり、その列を運ぶロードの
  前に除外できます。CDC のみの場合はカードを CDC サブフローに残します(`column.exclude.list` プレビュー
  付き)。除外はストリーミング前に実効テーブルから列を落とすことで適用されます — エクスポーターの
  `SELECT` リストとインポーターの `INSERT` リストはどちらもそこから導出されるため — ソースからその列を
  読むことも、ターゲットへ書くこともありません。ターゲットのスキーマは引き続きその列を保持し(適用済み
  DDL から再作成)、既定値/NULL を取ります。**主キー列は決して除外されず**、**ロード可能性の前提チェックは
  除外後の列集合を評価します**。ターゲットで `NOT NULL` かつ既定値のない列を除外すると、ロード中に各バッチが
  失敗する代わりに、事前ゲートが正しく失敗します(もう埋められないため)。カードは**ロードがコミットされるまで
  編集可能**です(テーブルピッカーと同じ時点でのみロックされます — Full Load 開始、CDC ストリーミング、または
  CDC インフラのデプロイ時 — 読み取り専用チェックが実行されただけではロックされません)。チェック通過後に除外を
  変更すると、Run ボタンが「前提チェックを再実行してください」と新しく除外された列を挙げてブロックするため、
  古い合格状態のままチェックされていない列集合でロードが始まることはありません(テーブルピッカーと同じ非対称の
  ガード — 除外の解除は決してブロックしません)。既定の挙動は変わりません — チェックしない限り何も除外されず、
  特大の単一値は引き続き行単位で隔離(quarantine)されます。

### 修正

- **「Start Full Load」確認ダイアログが最初のクリックで開くようになりました。** 以前はダイアログ要素の
  生成と開く操作が 1 回のクライアント更新にまとめられ、Quasar の `QDialog` が開くアニメーションに必要な
  `false→true` の遷移を検知できず、最初のクリックが無視され、2 回目のクリック(要素が既に登録済み)でのみ
  表示されていました。開く操作を 1 tick 遅延させ、最初のクリックでダイアログが表示されるようにしました。
- **「Change migration type」ジャンプリンクが、他のナビゲーションボタン(Back / Next / Continue)と
  同じアウトライン枠線を持つようになりました** — 以前は flat スタイルで枠線がありませんでした。
- **「Start Full Load」直前のターゲットプローブが大幅に高速化しました(DSQL 接続数の削減)。** 選択した各
  テーブルの実際の主キーをテーブルごとに別々の接続で読んでいたため、N テーブルの選択で N+1 個の接続を開いて
  いました — DSQL の接続は毎回 IAM トークンの生成と(リージョン間の)TLS ハンドシェイク(1 個あたり約 1 秒)を
  伴い、これが確認ダイアログの表示に数秒かかっていた原因でした。主キーは全テーブルを**単一接続**で読むように
  なり(新しい `target_primary_keys`)、テーブル数に関係なくプローブを 2 接続に削減しました。ソウルリージョンの
  7 テーブルのスキーマで実測:主キー読み取りが約 9.5 秒 → 約 2.8 秒に短縮。

## v0.1.271

### 変更

- **カットオーバー前の identity シーケンス同期が、レンダー時の自動実行ではなく、Cut-over ランブックの明示的な
  「Sync identity sequences」ボタンになりました。** v0.1.270 はこの同期を Cut-over 画面のレンダーの
  side-effect として実行していました — ページを見るだけでクリックなしにターゲットへ
  `ALTER TABLE … RESTART WITH` が発行されており、これは good design ではなく(閲覧が書き込みを引き起こす)、
  「カットオーバーはオペレーターの明示的な行為」というこのステップの原則にも反します。ランブックは repoint
  ステップの直前に **「Sync identity sequences」** ボタンを表示するようになり、オペレーターが最終ドレイン/
  リロード後、repoint 前にクリックします。すべての identity キーを現在のターゲット `MAX(pk)` の先まで前進させ
  (冪等、何度クリックしても安全)、クリックが UI をブロックしないようバックグラウンドで実行し、結果(どのシーケンス
  が前進したか、または「前進が不要だった」)を表示します。これにより v0.1.270 が狙った安全網 — ランブックに
  到達してボタンを 1 回押せば、最後の Validation 以降に CDC が配信した行までカバーし、再検証を覚えておく必要が
  ない — を維持しつつ、レンダー時のターゲット書き込みを削除しました。(v0.1.270 はデプロイされておらず、本バージョン
  がこれを置き換えます。)

## v0.1.270

### 修正

- **Cut-over ランブックが開くときに identity シーケンスを最後にもう一度再同期し、クリーンなカットオーバーが
  「先に再検証する」ことを覚えているかどうかに依存しなくなりました。** Validation はすでに
  `GENERATED BY DEFAULT AS IDENTITY` シーケンスを現在の `MAX(pk)` の先まで再同期しますが(v0.1.266)、
  それは最終 CDC ドレイン後にオペレーターが検証を実行した場合のみです。その最後の検証の*後に* CDC が配信した
  行があると、シーケンスが遅れたまま残り、カットオーバー後のアプリの最初の INSERT が衝突(重複キー、
  SQLSTATE 23505)します — count/checksum は一致するため、カットオーバー後にのみ顕在化する静かな失敗です。
  Cut-over ステップは、ランブックが開く時点(release が clean または accepted)で best-effort の identity
  シーケンス再同期を実行するようになりました — オペレーターがアプリを repoint する直前、そしてターゲットの
  `MAX(pk)` が最終確定する、ツールが制御する最後のポイントです。verdict ごとに 1 回実行(再検証で再武装)され、
  検証済みのテーブルに対してバックグラウンドで現在の `MAX(pk)` を基準に行います。ターゲットカタログの
  `is_identity` により identity でないテーブルはスキップします。ランブックに到達するだけで十分になり、
  Validation を再実行する必要はありません。(「I've cut over」確認時に同期する案の代わりに選択 — その時点では
  アプリがすでにライブのため、最初の衝突を防ぐには遅すぎます。)

## v0.1.269

### 修正

- **隔離(quarantine)ギャップの受け入れ時にも identity シーケンスを同期するようになりました — v0.1.268 が
  「ロード後に受け入れる」フローに残した穴を塞ぎます。** v0.1.268 はロードが*すでに受け入れ済みの状態で実行*
  されたときにシーケンスを同期しましたが、実際のワークショップのフローは逆です:まずロードが実行され(まだ何も
  受け入れていない → `_finalize_run` は incomplete と見なして同期をスキップ)、その後ユーザーが
  **「Accept quarantined rows & continue」** をクリックしますが、このボタンはステップを完了扱いにして CDC を
  解除するだけで、同期をまったくトリガーしませんでした。その結果 `GENERATED BY DEFAULT AS IDENTITY` キーが
  `nextval` = 1 のまま、すでに移行済みの id が存在し、カットオーバー後のアプリの最初の INSERT が衝突(重複
  キー、SQLSTATE 23505)しました — 0.1.268 でも実イベントで再現。受け入れアクションはマイグレーション範囲全体
  に対してバックグラウンドで identity シーケンス同期を投入し、現在のターゲット `MAX(pk)` を基準に合わせます
  (隔離された行は恒久的に破棄されるため最終値)。ターゲットカタログの `is_identity` により identity でない
  テーブルはスキップします。ロード時の同期(クリーン/ロード時受け入れ)と検証時の再同期(v0.1.266)と併せて、
  ロードを完了するすべての経路でシーケンスが前進するようになりました。

## v0.1.268

### 修正

- **隔離(quarantine)を受け入れた Full Load の後にも identity シーケンスを同期するようになりました(完全に
  クリーンなロードのときだけ行っていたのを改善)。** ロード後の identity シーケンス同期(=`GENERATED BY
  DEFAULT AS IDENTITY` キーをロード済みの `MAX(pk)` の先まで前進)は、完全クリーンな完了パスでのみ実行されて
  いました。1 行でも隔離されると — 恒久的に破棄、例: DSQL の 1 値あたり約 1 MiB 制限超過 — 実行は「隔離受け入れ」
  パスで完了しますが、このパスが**同期せずに**戻るため、シーケンスが開始値(`nextval` = 1)のままになっていました。
  その結果、カットオーバー後にアプリの最初の自動 INSERT が移行済みの id と衝突し、重複キー(SQLSTATE 23505)で
  失敗しました。隔離された行は恒久的に破棄され、後から埋められることはないため、現在の `MAX(pk)` が最終値であり、
  それを基準に同期するのが正しい動作です — 同期ゲートを「完全クリーン」ではなく `real_failed == 0`(隔離受け入れを
  含む)に緩和しました。本当の部分失敗(`real_failed > 0`)は引き続き同期をスキップします(後続のリトライが行を
  追加し得るため)。これは v0.1.266 の「検証時の再同期」を補完します:以前はギャップを受け入れたロードが検証を
  スキップして直接カットオーバーするとそのセーフティネットを失っていましたが、今はロード時点で同期が実行されます。

## v0.1.267

### 修正

- **`bigint unsigned` の AUTO_INCREMENT キーをサーバー生成 IDENTITY に変換する際、範囲が狭まることを
  警告するようになりました(暗黙的に処理しない)。** Aurora DSQL の identity 列は `bigint` である必要が
  ありますが、`bigint unsigned` は全範囲 `0..2^64-1` を保持するため `numeric(20,0)` にマップされます —
  したがって identity にすると `bigint` の `0..2^63-1` に狭まります。新規生成される id には影響しませんが、
  *既存の*ソース値が 2^63-1(9223372036854775807)を超える場合は収まらず、その行が Full Load で数値範囲
  超過エラー(SQLSTATE 22003)で失敗します — ロード時まで顕在化しない実質的な失敗です。変換は正確なしきい値と
  安全な代替(ソース PK を維持、または UUID キーを使用)を明記する個別の LOSS 警告を出すようになり、スループット
  の助言と混ざらず Schema Conversion の「gaps」セクションに表示されます。無損失の拡張(`int`/`bigint`/
  `int unsigned` → `bigint`)は変更なくこの警告を出さず、ソース PK を維持すれば `bigint unsigned` は全範囲を
  持つ `numeric(20,0)` のままです。

## v0.1.266

### 修正

- **Identity(AUTO_INCREMENT)シーケンスを検証(Validation)時に再同期し、Full Load + CDC
  マイグレーションでカットオーバー後に発生し得た重複キー問題を防ぎます。** `GENERATED BY DEFAULT AS
  IDENTITY` キーの場合、Full Load と CDC の両方が*明示的な* id を挿入しますが、明示的な id は identity
  シーケンスを前進させません。Full Load はロード完了時点でシーケンスを同期済みですが、その後 CDC が挿入を
  続けるため、カットオーバー時点ではシーケンスが実際の `MAX(pk)` より再び遅れ、カットオーバー後のアプリの
  最初の自動 INSERT が重複キー(SQLSTATE 23505)で失敗し得ました。これは最悪の失敗形態でした — 行数・
  チェックサムは一致するため Validation はクリーンに通過し、ソースが凍結されたカットオーバー後にのみ顕在化
  します。今後は全体検証の実行時に、すべての identity テーブルに対して冪等な `ALTER TABLE … ALTER COLUMN
  … RESTART WITH max(pk)+1` を再実行します(ターゲットカタログの `is_identity` で判定するため、identity
  でないテーブルには触れません)。検証は CDC ドレイン後、カットオーバー直前にオペレーターが通る手順であり、
  そのときがターゲット `MAX(pk)` が最終確定する時点です。シーケンスが前進した場合、結果画面に「Identity
  sequences advanced for cut-over」の通知と新しい `RESTART WITH` 値を表示し、CDC カットオーバー
  ランブックの最後の「再検証」手順もこの動作を明記します。比較自体は読み取り専用のままで、シーケンスの前進は
  レポートを失敗させない別個の(報告される)ターゲット書き込みです。

## v0.1.265

### 追加

- **Schema Conversion の主キーピッカーが AUTO_INCREMENT テーブルに「Server-generated (IDENTITY)」
  タイルを提供するようになりました。** コンバーターには既に identity 戦略(`BIGINT ... GENERATED BY
  DEFAULT AS IDENTITY (CACHE 65536)`)が end-to-end で実装されており — Full Load 後の identity
  シーケンス同期まで含む — ただしターゲット DDL を手動編集しないと到達できませんでした。単一カラムの
  AUTO_INCREMENT キーの場合、ピッカーに 3 つ目のタイルが表示され、ワンクリックでこの戦略を適用します。キーに
  ギャップ・緩い順序が生じる(各 DSQL ノードが独自の値ブロックを引く)旨の注意書きを併記し、Full Load は
  引き続きソースの id を保持します。これは AUTO_INCREMENT の最も忠実な移植であり、データ経路をそのまま維持
  します(`BY DEFAULT` によりローダーがソースの id を挿入可能)。タイルは適用可能な場合(単一カラムの
  AUTO_INCREMENT キー)にのみ表示され、選択は複合キーの選択と同じ編集済み DDL フィールドに保存されるため、
  別途の状態を持たずに再開(resume)安全です。
- UUID はピッカータイルとして**提供しません**: UUID 戦略はキーカラムを `uuid` に再型付けしますが、本ツールの
  Full Load はソースの整数 id からそのカラムを埋められない(int→uuid の挿入が失敗)ため、ここで露出すると
  データマイグレーションを壊れたロードに誘導してしまいます。スキーマのみ/グリーンフィールド用途では、手動 DDL
  編集を通じて引き続き利用できます。

## v0.1.264

### 変更

- **Validation の「Validating」カードに「Migration type」の行を表示しなくなりました。** 検証はソースと
  ターゲットを比較する純粋な処理で — エンジン(`validator.validate`)はマイグレーションタイプを引数に取らず、
  データがどの経路(Full Load / CDC)で入ってきても同一に動作します — タイプがこの画面の動作を変えることは
  ありませんでした。さらにセッションは最後に選んだタイプしか記録しないため、Full Load → CDC only の順で
  進めた場合「CDC only」だけが表示され、実際に何が移行されたかを誤って伝えていました。マイグレーション
  タイプは Data Migration ステップと上部のバナーで既に伝えているため、ここでは削除しました。(カットオーバー
  ランブックとドリフト判定は引き続き CDC 使用の有無で分岐し、そのロジックは変更していません。)

## v0.1.263

### 変更

- **Validation 結果画面のセクション間に小さく表示されていた、不要なキャプションを 2 つ削除しました。** (1)
  「For a definitive zero-loss verdict, quiesce the source first」アラートをギャップ復旧カードから
  削除しました — スナップショット以降にソースが変化したか、およびソースの書き込みを止めて CDC をドレインして
  から再検証するという案内は、専用の「Source changes since the comparison」セクションが既に扱っているため、
  復旧カードにも載せるのはセクション間の重複でした。(2) オプションブロック下部の「Changing options applies
  on the next run — use Re-run (top right) to apply them.」の行を削除しました — オプションブロックは
  明らかに実行前の設定エリアで、Re-run ボタンは常に右上に表示されています。(3) 検証結果の下の「Completed in
  Xs」実行時間キャプションを削除しました。実行中に表示されるグレーの「Options apply to the next run.」の
  注記は残します — 実行中にトグルが無効である理由を説明するためです。

## v0.1.262

### 修正

- **Validation のテーブル別結果表の Result 列が常にダッシュ(—)で表示されていた問題を修正し、各行に
  match / mismatch / ERROR バッジが表示されるようになりました。** Result 列は失敗行を上に並べるため整数キー
  (`result_sort`)でソートしますが、色付きバッジのスロットがその列のフィールド値 — つまり整数 — を
  `{text, color}` バッジペイロードであるかのように読んでいました。整数には `.text` がないため、すべての行が
  「—」プレースホルダーにフォールバックしていました。Result には行から直接バッジペイロードを読む専用の
  セルスロットが用意され、列は引き続き `result_sort` でソートします(失敗・エラー行は今も先頭にソート)。
  行数(row-count)とチェックサム列は変更していません。

## v0.1.261

### 変更

- **Cut-over readiness カードが同じ説明を 3 回繰り返さなくなりました。** ギャップが完全に説明された
  場合、カードのリードインが既に「下の Heads-up 項目はすべてマイグレーションが報告した削除行」と
  述べているのに、「Data identical」と「No mismatched records」チェックがそれぞれ「…is exactly the
  N rows dropped during the migration — already reported, not new data loss」という末尾を再度付けて
  いました。現在はリードインがカバーする場合 (完全説明) はチェックごとの末尾を省略し、各チェックには
  数値の事実のみを残します。部分的に説明されたランにはリードインがないため、チェックが末尾を保持して
  原因を自ら伝えます。セクション間の繰り返し (判定、テーブル別など) はそのまま — その冗長性は各
  セクションを独立して読めるようにするためです。

## v0.1.260

### 変更

- **「まずソースを停止する」注意書きが、恒久的ギャップで clean match を約束しなくなりました。**
  この注意書き (ライブのソースドリフト時に表示) は両方の復旧分岐に共通で適用されますが、末尾の
  「re-validate — a clean match then truly means no data was lost」は、未説明でロード可能な
  ギャップでのみ真です。完全に説明されたギャップでは、それらの行が恒久的な Aurora DSQL 制限を超え、
  何をしても欠落したままなので、凍結して再検証しても clean match には到達できません — この約束は同じ
  カードの「これらの行はそのまま保存できない — 値を縮小するか受け入れる」と矛盾していました。現在、
  末尾は分岐対応です: 完全説明 →「再検証して他にドリフトした行がないか確認 — 説明済みギャップは値を
  縮小するか受け入れるまで残る」; 未説明 → 従来の clean-match 文言 (そちらでは正しい目標)。ドリフト
  ゲート自体は変更ありません。

## v0.1.259

### 変更

- **復旧セクションのタイトルとアイコンが、acceptable なギャップの内容と一致するようになりました。**
  v0.1.258 はセクション本文をギャップが完全に説明済みかで分岐しましたが、見出しはレンチアイコンの
  「How to recover」のままでした。完全に説明されたギャップでは行をそもそも保存できず、受け入れが正当な
  最終選択であるため、「recover」・レンチが決定を修理のように見せ、判定バナーおよびセクション本文
  (「shrink the value or accept the gap」) と矛盾していました。現在、完全説明ケースは Cut over
  ステップと同一の見出し・アイコン「Acknowledge the known gap」/ fact_check を使い、2 つの画面が同じ
  語彙で話します。本当に未説明の (ロード可能な) ギャップは、1 テーブルが説明済みで別テーブルが実損失の
  mixed ケースを含め、「How to recover」とレンチを維持します。

## v0.1.258

### 修正

- **「How to recover」セクションが、acceptable (完全に説明済み) なギャップを誤った修正へ誘導して
  いました。** 欠落行すべてがマイグレーションが既に報告した削除行 — 恒久的な Aurora DSQL 制限
  (例: ~1 MiB の値上限) 超過 — の場合、Full Load を再実行しても再び隔離されるのに、recovery
  セクションは「Re-run Full Load + CDC to backfill the gap. Full Load は欠落行のみ埋める」と
  Stop CDC → reload → 再開のランブックを描画していました。これらの行については事実と異なり、真上の
  判定バナーとも矛盾していました。現在はギャップが完全に説明済みかで分岐します: 恒久隔離ギャップには
  「これらの行はそのまま保存できない」と伝え、2 つの実際の経路 — ソース値を上限未満に縮小 (例: 大きな
  オブジェクトを Amazon S3 へ移動) してから reload、またはギャップを受け入れて進む — を示し、reload
  ランブックは省きます。本当に未説明の (ロード可能な) ギャップは、1 テーブルが説明済みで別テーブルが
  実損失である mixed ケースを含め、従来の reload ランブックをそのまま受け取ります。
- **判定バナー本文も「reload で完全一致」という含意を持っていました。** 現在は「reload だけでは
  解決しない — DSQL が元の値を保存できないため」を先に述べてからソース値の縮小またはギャップ受け入れを
  案内し、recovery セクションおよび Cut over ステップと整合します。

## v0.1.257

### 変更

- **Validation の復旧セクションの「まずソースを停止する」注意書きが、実際にドリフトがある場合のみ
  表示されるようになりました。**「How to recover」は no-go セクションで、残っている問題は説明されない
  実際の不一致であり、修正は順序化された Full Load 再ロード手順です。ソースを止めるという助言がこの
  画面に適切なのは、ライブドリフトによって不一致の一部が in-flight である可能性がある場合
  (再ロードが動く標的を追いかねない場合) だけです。この通知はドリフトがなくても `info` として無条件に
  描画され、具体的なギャップを修正する画面に一般的なカットオーバーの余談を添えていました。その
  フォールバックを削除します。ドリフトがない場合の停止案内はソース変更セクションと Cut over ステップが
  既にcarrying しているため、これは助言ではなく重複を取り除くものです。

## v0.1.256

### 変更

- **Validation の判定が、acceptable なギャップを「blocked」と呼ばなくなりました。** すべての差分が
  マイグレーションが既に報告した削除行と正確に一致する場合、ツールはこの状態を *acceptable*
  (ギャップ承認後カットオーバー可能) と分類しますが、黄色い判定ヘッダーは「Cut-over blocked only
  by rows dropped during the migration」と表示していました。「blocked」は赤レベルの「完全停止」を
  意味する語で、ツール自身のゲートと矛盾し、実際の赤い「Not ready」判定より深刻に読め、同じ状態を
  「Every difference is explained」と呼ぶ Cut over ステップとも食い違っていました。現在ヘッダーは
  決定を明示します —「Every difference is explained — accept the gap or fix the source and
  reload」— Cut over ステップと揃え、2 つの画面が同じ状況として読めます。緑の「Ready for
  cut-over」と赤の「Not ready for cut-over」ヘッダーは変更ありません。
- **Cut-over readiness パネルが、判定と結び付けるリードイン 1 行で始まります。** v0.1.255 以降、
  このパネルは最後 (証拠の後) に描画されるため、「Heads-up」の行が新しい弱い信号のように読める
  可能性がありました。すべての差分が既知の削除行である場合、パネルは「Same conclusion as the
  verdict above — nothing unexplained. Each 'Heads-up' item below is the same rows the
  migration already reported dropping, not a new problem.」で始まります。(未説明の差分がない場合
  のみ表示)

## v0.1.255

### 変更

- **Validation のセクション順序が、要約ではなく根拠を先に示すようになりました。** no-go の際、
  「How to recover」と「Cut-over readiness」チェックリストが、その判定の根拠となる比較結果より*上*に
  表示され、修正方法と要約が「なぜ」より先に来ていました。判定 (上部バナー) とその復旧案内は
  見出しの回答として一緒に残しつつ、根拠から要約される readiness チェックリストはその後ろに移動
  します: 判定 → 復旧方法 → 注意が必要なテーブル → テーブル別結果 → orphan レコード → ソース変更 →
  **cut-over readiness** → エクスポート。上から下へ読むと「カットオーバーできるか?」「何をすべきか?」
  「なぜ?」、そして最終集計、の順になります。

## v0.1.254

### 修正

- **Full Load 後の Validation/Cut over ステップで、マイグレーションタイプのバナーが「CDC only」と
  表示されていました。** バナーは「one journey」ヘッダーに従い全ステップに表示されていましたが、
  セッションのマイグレーションタイプは 1 つだけです — そのため案内された経路 (Full load only を
  実行後「CDC only」に切り替えて既存のターゲットへストリーミング) をたどると、以降のステップで
  「Migration type: CDC only」と「no Full Load in this session」という説明が表示され、直前に行った
  ことと矛盾するのに、その画面では訂正のしようがありませんでした。現在バナーはセレクタのある
  Data Migration ステップにのみ表示され、そこではラベルが常に現在の状態と一致します (Full Load
  実行中は Full load only、切り替え後は CDC only)。ステップ進捗表示は引き続き全ステップで連続性を
  提供します。

## v0.1.253

### 修正

- **CDC 削除中の seeder ENI 待機区間で、ログが依然として約 18 分間空白でした。** v0.1.251 で
  レポート機能を追加しましたが、インターフェース数が*変化したときのみ*記録していたため、実測された
  ティアダウンでは「MskCluster DELETE_COMPLETE」(02:46) → 何もなし → 「Seeder network
  interfaces released.」(03:05) となりました。操作者は終わってから状況を知ることになり、この機能が
  解消しようとしていた「停止して見える」症状がそのまま残っていました。現在は数が変わらなくても
  約 2 分ごとに再報告し、経過時間を併記します (「Waiting for AWS to reclaim 2 seeder network
  interfaces — 6 min so far」)。そのため回収に時間がかかっても進行中であることが分かります。
  その間隔内の高頻度ポーリングは 1 行のみに留まるため、スタックイベントが埋もれることはありません。

## v0.1.252

### 修正

- **CDC インフラを削除しても、パイプラインがまだストリーミング中のように見えていました。**
  「Delete CDC infrastructure」を押すと削除が始まりますが、CloudFormation はコネクタを即座に
  削除しないため、約 20 分の削除中ずっと検出は両コネクタを RUNNING と報告し続けました。その結果、
  カード本文は「Deleting infrastructure」なのに隣に緑色の **「Streaming」** バッジが並び、
  Live status パネルとその中の dead-letter queue、テーブル別マイグレーション状況の表までが画面に
  残り、解体中のパイプラインのレプリケーション数値を表示していました。現在は操作者が要求した
  ティアダウンが、あと 1〜2 分だけ真であるコネクタ状態より優先されます: バッジは「Deleting…」
  (MSK を残す Stop CDC は「Stopping…」) となり、3 つの監視ビューはティアダウン完了まで非表示に
  なります。3 つは 1 つの表示条件を共有するため食い違うことがなく、Start CDC 実行中は引き続き
  表示されます — そのランプアップ時が最も必要な時点です。

## v0.1.251

### 追加

- **CDC インフラ削除時に seeder ネットワークインターフェースの回収状況を deploy log に表示します。**
  削除で最も時間がかかる区間は、MSK クラスターが消える前に AWS が VPC 内 offset-seeder Lambda の
  ENI を解放する時間 (約 15-20 分) で、その間 CloudFormation はイベントを出さないため、ログが停止
  したように見えていました。現在は削除待機中にその ENI を読み取り専用でポーリングし、変化時のみ
  ログ出力します — 残っている間は「Waiting for AWS to reclaim N seeder network interface(s)…」、
  すべて解放されると「Seeder network interfaces released.」— そのため静かな区間でも何を待っているかが
  分かります。このためアプリスタックの CDC deploy role に `ec2:DescribeNetworkInterfaces`
  (読み取り専用) を追加しました。CDC インフラの再デプロイは不要です。

## v0.1.250

### 修正

- **CDC インフラ削除で、超過し続ける正確な ETA を表示していました。** ステージ進捗カードが「est.
  ~5 min remaining」と表示していましたが、削除は MSK クラスターが消える前に AWS が VPC 内 seeder
  Lambda の ENI を回収する時間に左右されます — 予測不可能で、実測では推定を大きく超えます — その
  ため 4 倍短いカウントダウンが停止した UI のように見えていました。現在、削除はステージ別 ETA
  ヒントなしで正直な上限 (「can take up to ~20 min」) を表示します。タイミングが安定している他の操作は
  カウントダウンを維持します。

## v0.1.249

### 変更

- **テーブル別 Consistency 列: それを埋める Refresh ボタンと結び付けました。** Consistency 判定は
  source/target の行数と high-water PK から計算され、それらは明示的な「Refresh source/target
  counts」操作 (意図的に自動ポーリングしない、ソースをスキャンする COUNT(*)) からのみ得られます。
  ボタンを押すまで各行の Consistency は「refresh to check」と表示され、これは利用者が自分で結び
  付ける必要のあるボタンを指していました。現在は表の上の info 通知が、最初の読み取り前にはその
  リンクを明示し (「Consistency 列は『Refresh source/target counts』を押すまで『refresh to check』と
  表示されます」)、読み取り後はその案内を外します。列とボタンを残す価値があるか検討しました:
  この列は Validation の厳密な検査とは異なるリアルタイムのカットオーバー信号であり、それを埋めるのが
  このボタンです — 2 つは 1 つの機能なので両方を残し、その関係を削除せずに明示しました。

## v0.1.248

### 修正

- **CDC ステップの見た目: 「Live status」セクションと折りたたみパネルに枠線がありませんでした。**
  CDC 開始前、「Live status」は空のチャートと「コネクタが検出されると表示されます」というむき出しの
  グレーの行の上に枠線のない見出しとして描画され、周囲の枠付きカードと調和しない空白でした。現在は
  CDC が開始されるまでセクション全体を非表示にし (テーブル別表と同様)、表示後もまだ起動中の状態の
  案内は適切な枠付き info 通知として表示します。また、折りたたみパネル (Connector configuration、
  cdc-stack パラメータファイル、Delete CDC infrastructure、Infrastructure inputs、デプロイログ
  など) に枠線がなく、カードセクションの隣でスタイルのない見出しのように見えていましたが、現在は
  design system の単一の `EXPANSION_PANEL_CLASSES` 枠線トークンを共有します。

## v0.1.247

### 修正

- **CDC インフラの作成中も migration type を切り替えられていました。** 作成処理はコネクタを作らず
  何もストリーミングしないという理由で除外されていましたが、それは「選択が自由」ということとは
  違います: これは課金対象の Amazon MSK クラスターをプロビジョニングする約 15-20 分の処理であり、
  進捗表示と「Delete CDC infrastructure」の操作はどちらも CDC サブステップにあります —
  「Full load only」に切り替えるとそのサブステップ自体が消えるため、アカウント内でクラスターが
  作成され続けているのに、進捗も完了通知も削除手段も画面に無い状態になります。ツール内部も
  一貫していませんでした: oversized-LOB 除外は既にインフラ作成中はロックされます。現在はその処理の
  実行中はタイプがロックされ、コストと操作の場所を示し、処理が終われば再び解放されます
  (アイドル状態のインフラは依然として利用者が引き受けるトレードオフです)。
- **CDC ステップの「Per-table migration status」テーブルが CDC 開始前に表示されていました。**
  CDC サブステップが表示されると同時に — インフラ作成中もずっと — 現れていましたが、その時点では
  コネクタが無いため CDC の全列 (Stream lag、Quarantined、Inserts/Updates/Deletes、Consistency) が
  必然的に空であり、「CDC が動作中で何も複製していない」と読めてしまいました。現在は CDC が実際に
  開始された後に表示され (上のライブステータスパネルと同じ基準)、コネクタが約 10-20 分の起動を
  終えた後ではなく Start CDC を押した時点から表示されます。情報の欠落はありません:
  Full Load 独自のテーブル別表は Full Load ステップに残ります。

## v0.1.246

### 修正

- **「Accept quarantined rows & continue」をクリックした後も、赤い「Migration failed」バナーが
  残っていました。** 隔離行の受け入れは、操作者がまさにそのエラーを解決する行為です — Full Load が
  DONE になり、当該ステップは「Full Load complete — with an accepted gap」を表示します — にも
  かかわらず、生の `FullLoadIncompleteError` がその上に固定されたままでした。1 つの画面が 3 つの
  判定 (失敗 / ギャップを受け入れた完了 / DONE) を同時に示し、操作者が既に対処した事柄を再び問題として
  提示し、たった今従った「'Accept quarantined rows & continue' を選択してください」という指示まで
  含んでいました。現在はギャップを受け入れるとバナーが消え、マイグレーションタイプを切り替えても
  非表示のままです (解決済みのエラーは前のタイプからの引き継ぎ情報でもありません)。情報の欠落は
  ありません: 受け入れ通知が既に削除された行数、対象テーブル、Validation が依然ギャップを報告する
  ことを伝えており、エラーログには削除されたすべての行が主キー付きで残ります。受け入れ前のバナーは
  変わりません — その時点では実際の失敗であり、明確に示す必要があります。

## v0.1.245

### 追加

- **Full Load 完了後の「次は CDC?」通知にジャンプリンクを追加しました。** 通知は「上の migration
  type を変更してください」と伝えていましたが、Full Load が終わった時点でそのセレクタは長いページの
  最上部にあり、通常は画面外です — 通知が求める唯一の操作を、利用者自身が探しに行く必要がありました。
  現在は「Change migration type」リンクがセレクタまで直接スクロールし、一時的に枠線を表示するため、
  到着時にどのコントロールかが明確です。文言も方向を示す代わりにリンクを指すようにしました。
  純粋にナビゲーションのみです: タイプは引き続きタイルを意図的にクリックして変更します。

## v0.1.244

### 修正

- **「Generate DDL for selected」がターゲットの存在を古いスナップショットで判定していました。**
  各 diff の「'x' already exists on the target — SKIP または REPLACE (破壊的) を選択」という警告が、
  キャッシュされたターゲットインベントリから回答されていました。このキャッシュは SQL を発行せず、
  Evaluation のブラウズか手動の「Refresh target」ボタンでしか更新されません。そのためターゲットを
  空にした後も、既に存在しないオブジェクトについて警告が出て、理由もなく破壊的な選択へ誘導して
  いました。逆のケースはさらに深刻で、スナップショット以降に作成されたオブジェクトには警告が一切
  出ず、予期しない SKIP が発生していました。現在は Generate がまずターゲットカタログを読み直します —
  読み取り専用、UI スレッド外、かつ静かに (トースト表示や二重レンダリングなし) — そのため利用者が
  更新手順を覚えておく必要なく、すべての判定が実際のターゲットを反映します。読み直しが失敗しても
  生成は妨げません: DDL diff は生成され、実際の適用パスはライブブラウズで存在を独立に再確認するため、
  古い判定が実際の DDL の処理を誤らせたことはありません。

## v0.1.243

### 修正

- **CDC ステップのテーブル別「Quarantined」列が、まだ Full Load の隔離行を集計していました。**
  v0.1.241 で dead-letter カード、v0.1.242 で Full Load ログをフィルタしましたが、この列が
  漏れていたため、1 つの画面が同じセッションについて矛盾する 2 つの数値を表示していました:
  カードは「0 quarantined」なのに、その真上の列は 3 でした。現在はカードと同じフィルタを共有する
  ため、両者は常に一致します。Full Load のアクティビティログのテーブル別失敗理由も併せて
  フィルタしました — そのままでは dead-letter された行の理由が FULL_LOAD のログ行で特定テーブルの
  理由として記録され得ました。共有エラーログを直接読む残りの箇所もすべて監査しました:
  フィルタしていない 2 箇所はフィルタ自身に直接供給する読み取りであり、隔離カウンターは Full Load
  の書き込み側のみが生成するメッセージ接頭辞を使うため既に安全でした。

## v0.1.242

### 修正

- **Full Load のエラーログが、dead-letter された CDC の行を Full Load の失敗として集計していました。**
  v0.1.241 の鏡像で、方向が逆です: Full Load が実行されたセッションでは CDC がその job id の下に
  記録するため、フィルタなしで読むと Full Load の隔離 3 件と dead-letter 2 件が合わさって
  「Download Full Load error log (5 errors)」となり、ファイルに CDC の行が混入していました。
  カットオーバー時点では実際には 3 行を失ったのに「Full Load が 5 行を失った」と読めてしまいます。
  テーブル別の失敗理由にも同じ欠陥がありました — テーブルごとに最後のメッセージだけを保持するため、
  dead-letter された行が、Full Load が正常にロードしたテーブルの「理由」を提供し得ました。現在は
  Full Load の表示 (件数、テーブル別の行と理由、隔離リスト、ダウンロードのラベル*および*ファイル
  内容) が自身のレコードのみを報告するため、2 つの画面がエラーログを正確に分割します:
  Full Load + CDC が全体と一致し、欠落も二重計上もありません。

## v0.1.241

### 修正

- **CDC の dead-letter queue カードが、Full Load の隔離行を DLQ レコードとして集計していました。**
  DLQ パネルのエラーログキーは Full Load が実行されたセッションではその job id になるため、2 つの
  ソースが 1 つのキーを共有し、バッチローダーが隔離した行が「Dead-letter queue (poison records)」に
  現れていました — ストリームに入ったことがなく、コネクタが存在する数時間前に隔離された行について
  「DLQ に隔離され、パイプラインは動作を続けている」と説明しながらです。Full Load に DLQ は
  ありません。特に有害なのは、大きな LOB 列を除外した直後の利用者です: 0 でない隔離件数を見て除外が
  失敗したと判断しますが、CDC の隔離が 0 であることこそ除外が機能した証拠です。現在はすべての DLQ
  表示 — 件数バッジ、テーブル別チップ、レコード表、ダウンロードのラベル*および*ファイル内容 — が
  CDC 由来のレコードのみを報告し、単一のフィルタを通すため件数とその下の行が食い違うことはありません。
  Full Load の隔離行は隠しません: ターゲットに到達しなかった行はカットオーバーの判断で依然として
  重要なため、パネルが Full Load セクションを指す中立的な 1 行で相互参照します。

## v0.1.240

### 修正

- **Stop CDC またはインフラ削除の後も、Data Migration バッジが「CDC: IN_PROGRESS」のままでした。**
  コネクタを検出すると CDC ワークフローのステップを NOT_STARTED → IN_PROGRESS に昇格させていましたが、
  戻す経路がなかったため、コネクタが消えた後もバッジは CDC が実行中だと示していました — ワークフローは
  永続化されるため、セッションを復元するたびにその古い値が再び現れていました。現在はステップの状態が
  コネクタの実際の有無を双方向で追跡します: 削除時は NOT_STARTED に下がり、これが正確な停止状態です
  (ストリーミング中のものはなく、Start CDC が再び提供されます)。CDC には依然として終端の DONE が
  ありません — 明示的な Stop/Delete によってのみ終了する継続的レプリケーションだからです。他の経路が
  記録した FAILED は、通常の探索処理で書き換えずそのまま残します。

## v0.1.239

### 修正

- **CDC インフラの削除に約 24 分かかり、そのほとんどが MSK クラスターを削除*しない*時間でした。**
  2 つの IAM ロールが、クラスターレベルの MSK 権限のスコープを
  `Fn::GetAtt: [MskCluster, Arn]` で指定していました。CloudFormation はこれを依存関係として読み、
  逆順で削除するため、クラスターは自分を参照するすべてのロールを待つ必要がありました —
  offset-seeder のロールも含まれますが、この seeder は VPC 内 Lambda であり、AWS が ENI を回収する
  のに約 15-20 分かかります。実測では、クラスター自身の削除 (93 秒) が始まる前に seeder で
  18 分 30 秒待機し、その間 UI は「Deleting infrastructure」を表示し続けていました。削除時点でそれらの
  ポリシーは一切必要ありません — コネクタは既に消えており、IAM は呼び出し時にのみ評価されます —
  そのため現在はロールがクラスター ARN を名前から構成します (`Fn::Sub`、AWS が付与する UUID
  サフィックスはワイルドカード)。権限は同一ですが、クラスターと seeder が並行して削除されます。
  作成順序は変わりません — コネクタは引き続きクラスターに `DependsOn` します。
- **削除が終わった瞬間、CDC カードがすぐにデプロイフォームを再提示していました。** 課金対象の MSK
  クラスターが削除されるのを 20 分待った利用者が求める答えは「削除されました」であり、たった今
  取り除いたものを再構築しようとするかのような 20 行の BYO-VPC フォームではありません。現在は
  カードが削除完了 (および課金停止、マイグレーションのデータには影響なし) を先に伝え、再構築は
  コストを明記した明示的なオプトイン (約 15-20 分、課金対象) として提供します。初回デプロイは
  ゲートしません (その場合フォームが次のステップです)。2 回目の削除では以前の回答を再利用せず、
  改めて確認します。

## v0.1.238

### 修正

- **oversized-LOB 除外のチェックボックスが、選択が既に確定した後もクリック可能なままでした。**
  ロックがまったくありませんでした — チェックは反応し状態も実際に変わりますが、
  `column.exclude.list` はインフラ作成時に cdc-stack の `ColumnExcludeList` パラメータに
  焼き込まれ、Start CDC 時にソースコネクタへ渡されるため、それ以降の変更は黙って破棄される一方で
  チェックボックスは反映されたように見えていました。現在はチェックボックスを実際に無効化します —
  グレー表示とクリック遮断を同時に行うため見た目と挙動が一致します。一時的な 2 つのケースでは、
  操作者がまだ選択中である可能性があるため理由と対処法も示します: インフラ作成中はスタックと共に
  送信済み、CDC 開始後はコネクタへ渡済み (変更するには Stop CDC)。インフラがデプロイ済みの状態では
  文言なしでロックします — CDC 実行の通常の状態であり、グレー表示のチェックボックスだけで選択が
  閉じたことが伝わるためです。
- **アプリ再起動後、CDC パイプラインのカードが空になり新規デプロイフォームを提示していました。**
  カードが「まだ調査していない」と「スタックが存在しない」を同じ状態として扱っていました。実際の
  CDC 状態を読む読み取り専用 AWS プローブはターゲットリージョンを必要とし、ない場合は黙って
  返ります。また復元されたセッションは再検証まで以前の接続を信頼しません — そのため実際の
  パイプラインがストリーミング中でも状態が不明となり、カードは重複した (課金対象の) MSK
  クラスターの作成を促していました。現在は 2 つの状態を区別し、状態が本当に判明していない場合は
  その事実と「実行中のパイプラインには影響がない」ことを示し、復旧方法 (ターゲット接続の再検証)
  を案内します。

## v0.1.237

### 修正

- **CDC コネクタの作成中でも移行タイプを切り替えられていました。** 切り替えると少し後にコネクタが
  現れてすぐにロックされていました。既存のロック条件 2 つはいずれも進行中のコネクタ開始を捉え
  られません — コネクタがまだ作成されていないため検出済みコネクタのチェックは空で、スタック
  フェーズもまだ `running` ではなく、CDC only プランでは `full_load` ステップも `IN_PROGRESS`
  ではありません。開始点とテーブル集合は Start CDC を押した時点で確定するため、現在はその時点で
  選択がロックされます — テーブルピッカーがすでに使用している `cdc_streaming_started` シグナルを
  そのまま利用します。インフラ作成中は意図的にロックしません: `create_stack` は MSK・
  ネットワーキング・プラグインのみを作成しコネクタは作らないため、15〜20 分の間は何も確定せず
  ストリーミングもなく、プランを変更できるべきです。併せて修正した点: ロックの説明が job manager
  なしで別途再計算されており、新しい条件でロックされた際にタイルが無効化される一方で理由が
  表示されませんでした。現在は無効状態と説明が 1 回の評価から得られ、食い違うことはありません。

## v0.1.236

### 修正

- **Start / Re-run Full Load ボタンが 2 回押さないと反応しないように見えていました。** CDC を
  含むプランでは Data Migration 画面の描画から約 0.05 秒後にアカウント全体の CDC 探索が実行され、
  ワーカースレッドで AWS を読み取り、完了時に画面全体を無条件で再描画していました。このときすべての
  ウィジェットが再生成されるため、描画と再描画の間に押されたクリックは既に存在しない要素に届き、
  黙って失われていました。現在は探索結果が実際に変化した場合のみ再描画します — 再訪時は通常同じ
  スタックとコネクタが見つかるため、再描画には意味がありませんでした。実際に変化した場合は今も
  再描画するので、MSK の重複防止の案内は判明した時点で表示されます。Full load only は元々
  影響を受けていません (探索が実行されないため)。
- **復元した CDC only セッションで、CDC を実行したことがないのに「CDC: DONE」と表示されて
  いました。** バッジのステータスは、すべての移行タイプが共有する単一の `full_load` ワークフロー
  ステップから来ており、ワークフローはまるごと保存・復元されるため、以前 Full Load を完了した
  セッションが「CDC」というラベルにその Full Load の DONE を付けて戻ってきていました。ある段階の
  名前に別の段階の値を付けるのは、v0.1.231 でラベルによって置き換えようとした曖昧な「DONE」より
  悪いことです。現在 CDC only は独立して管理される `cdc` ステップを読み、NOT_STARTED と
  IN_PROGRESS の間を移動します — 継続的レプリケーションには完了の概念がなく、明示的な
  Stop/Delete でのみ終了するため、これが CDC の実際の挙動です。表示のみの変更で、`full_load`
  ステップは引き続き Validation のゲートです。
- **CDC ステップに CDC インフラのカードが 2 回表示されていました。** v0.1.235 でそこに準備
  セクションの呼び出しを追加しましたが、そのステップのライフサイクルカードがスタックがないときに
  同じ BYO-VPC デプロイフォーム (または adopt の選択) をすでに描画するため、同一のフォームが
  2 回現れていました。準備セクションは*追加*のエントリポイントであり — 15〜20 分かかる MSK の
  作成を Full Load と重ねるために Prerequisites で提供されます — 重ねる Full Load がない
  CDC only では引き続き抑制されます。

## v0.1.235

### 変更

- **CDC only では、CDC インフラのカードが Prerequisites ではなく CDC ステップに表示されます。**
  Prerequisites に置いていた理由は 15〜20 分かかる MSK の作成を Full Load と重ねるためですが、
  Full Load がない CDC only には当てはまりません。そのままでは 1 つの連続した作業が 2 つの
  セクションに分断され、利用者は Prerequisites でプロビジョニングした後、別のセクションで
  Start CDC を探す必要がありました。現在は開始点の決定より前に配置されます — スタックなしでは
  後続の処理を実行できないためです。Full load + CDC は変更ありません: Prerequisites の配置と、
  その理由であるオーバーラップを維持します。カードは移行タイプごとに正確に 1 か所にのみ表示され、
  両方に表示されることはありません — 課金が発生するデプロイフォームが 2 度表示されては
  ならないためです。

## v0.1.234

### 修正

- **CDC インフラを操作した直後に CDC セクションが折りたたまれ、次の操作が隠れていました。**
  インフラカードは Prerequisites の下部にあるため cdc-stack のデプロイ・削除はそこで行われますが、
  CDC セクションはアクティブなサブステップが `cdc` のときにしか開かず、それを移す処理がありません
  でした。該当したのは 2 つのケースです — **CDC only でインフラが準備済みの状態**:
  「CDC infrastructure is ready」は Prerequisites 内に表示される一方、Start CDC は折りたたまれた
  CDC セクションの中にありました。**削除を送信した直後**: 操作中にもかかわらず画面が
  Prerequisites に戻っていました。CDC 実行中のケースを扱う既存の固定処理はありましたが、
  コネクタの存在を条件としていたため、まだコネクタがないこの 2 ケースを取りこぼしていました。
  現在はインフラの作成・削除が進行中のとき、および CDC only でインフラが準備済みのときも CDC
  セクションに固定します。Full load + CDC は準備状態だけでは切り替えません — 完了した Full Load
  は結果を画面に残し「Continue to CDC」で進む設計であり、ここで切り替えるとスナップショットの
  行数とウォーターマークが視界から消えてしまいます。
- **準備完了の通知が CDC only の利用者に「Full Load の後」にストリーミングを開始するよう案内して
  いました。** その計画に Full Load は存在しないため、未充足の前提条件のように読めていました。
  現在は CDC ステップの Start CDC を指します。

## v0.1.233

### 追加

- **Full Load の事前チェックが、ソースで埋められない target の NOT NULL 列を、ロード中では
  なくロード前に検出します。** Full Load は INSERT の列リストをソーステーブルから作るため、
  target にのみ存在する列 (例: Schema Conversion で target DDL を編集して追加した列) は INSERT
  に含まれません。その列が nullable、DEFAULT あり、または identity なら問題ありません (ライブ
  クラスターで確認: ロードが NULL / デフォルトで埋めます)。しかし DEFAULT のない `NOT NULL` 列
  の場合は致命的です — 入れる値がなく、target に一部データが投入された後に not-null 違反で失敗
  します。新しいチェック `TARGET_COLUMNS_LOADABLE` (必須) は target の値必須列からソース列を
  引いた残りにのみ失敗します — ソースにも存在する列は INSERT が埋めるため対象外です。target
  テーブルが存在しない、または読み取れない場合は `TARGET_SCHEMA_READY` に委ね通過させ、同じ
  原因を二重に報告しません。

## v0.1.232

### 追加

- **設定したモデルがアカウントで利用できない場合、AI assist が Claude Sonnet 4.6 に
  フォールバックします。** Bedrock の `global.` 推論プロファイルがリージョンで `ACTIVE` で
  あることは、そのアカウントが呼び出せることを意味しません。モデルアクセス権はアカウント単位で
  付与されるため、新規アカウントでは `global.anthropic.claude-sonnet-5` が有効に見えていても
  model-not-enabled エラーになります。その場合「Verify AI access」は行き止まりとなり、モデル
  ID を手で書き換えるしかありませんでした。現在は事前チェックがフォールバックモデルで再試行し、
  実際に応答したモデルと、選択したモデルを復旧する方法を併せて通知します。意図的に狭く適用します
  — 再試行は model-not-enabled の失敗時のみ発動します。IAM 権限不足、スロットリング、接続
  エラーは呼び出し側やネットワークの問題であり、別のモデルで再試行してもレイテンシが増えて本当の
  原因が埋もれるだけです。フォールバックで成功した場合は緑の成功ではなく警告として表示します —
  通常の成功のように見えると、選択したモデルが動作していると誤解させてしまいます。

## v0.1.231

### 修正

- **移行タイプを切り替えると "Success" ヘッダーの横に赤い "Migration failed" バナーが残って
  いました。** 行が隔離された Full Load の後に CDC only を選択すると、判定が 3 つ同時に表示され
  ました — ヘッダーは `Success`、ステータスは `DONE`、バナーは依然として `Migration failed`。
  ユーザーは「成功したのか」を判断できませんでした。バナーが最後のエラーメッセージだけを見て
  描画され、タイプを切り替えてもそれを消す経路がなかったためです。ただしこのメッセージは無用な
  ノイズではありません — ターゲットで実際に欠落している行を伝えており、CDC は以降の変更を
  ストリーミングするだけで Full Load の欠落を埋めないため、CDC を開始しようとする人が必ず知って
  おくべき事実です。そのため、別の移行タイプで記録されたエラーは削除せず error から warning に
  下げ、前の作業から引き継がれた状況であることを示して対処方法 (Full Load の再実行) も併せて
  案内します。出自が不明なエラー (古いセッションから復元されたもの) は静かに緩和せず error の
  まま維持します。

### 変更

- **Data Migration のステータスバッジが、どの段階のステータスかを併記します。** たとえば単なる
  `DONE` ではなく `Full Load: DONE` と表示します。すべての移行タイプが 1 つの内部ステップを
  共有するため、Full Load を終えた後に CDC only へ切り替えると CDC が完了したかのように読めて
  いました。Full load + CDC では実際の進行に従い、パイプラインが本当にストリーミングしている
  ときにのみ `CDC` になります。

## v0.1.230

### 修正

- **v0.1.229 を既存スタックに適用できませんでした。** ALB に名前を付けると (Cognito ログイン
  の修正) ALB が置き換えられ、CloudFormation の置き換えは「新規作成 → 参照の切り替え →
  旧リソース削除」の順で進みます。ターゲットグループには名前がなく変更点もなかったため
  **再利用**され、新しいリスナーが旧 ALB がまだ保持しているグループをアタッチしようとしました。
  ELBv2 はターゲットグループを 1 つのロードバランサーにしか関連付けられないため
  `The following target groups cannot be associated with more than one load balancer`
  (`ServiceLimitExceeded`) で失敗しロールバックしました — 新規デプロイは動くものの、既存
  スタックはすべて旧リリースに取り残される状態でした。ターゲットグループに
  `${AWS::StackName}-tg` という名前を設定しました。この属性も create-only なので同じ更新で
  ターゲットグループも置き換えられ、新しいリスナーはどのロードバランサーも保持していない
  グループをアタッチします。実際のスタックをアップグレードして検証しました — 変更セットに
  `TargetGroup` が現れ (以前は含まれていませんでした)、3 秒で失敗していたリスナーが成功し、
  スタックは `UPDATE_COMPLETE` に到達しました。一時的な対処ではなく構造的な修正です — 今後
  ALB が置き換わる変更 (同じく create-only の `AlbScheme` の変更など) はすべて同じ問題に
  ぶつかっていたはずです。ただしターゲットグループに固定名が付くため、以降そのグループの
  create-only 属性 (`AppPort`、`VpcId`、`TargetType`) を変更すると置き換えではなく
  `DuplicateTargetGroupName` で失敗します — 名前を付けた ALB で既に受け入れているものと同じ
  トレードオフです。

## v0.1.229

### 修正

- **選択したプライマリキーをすでに持つ空のターゲットを「キーを適用するため」に再作成して
  いました。** Schema Conversion で複合キーを選び Apply all to target を実行した直後、最初の
  Full Load の確認ダイアログが、まさにそのキーで作成されたテーブルに対して
  `1 empty table will be recreated to apply the chosen primary key` と表示していました。
  矛盾しているうえに不要な DROP+CREATE が実行されます (DSQL はトランザクションあたり DDL が
  1 文なので、テーブルごとに別のラウンドトリップになります)。ダイアログの表示とエンジンの
  昇格判定がどちらも適用済み DDL と**ソース**キーのみを比較し、ターゲットの実際のキーを
  読んでいませんでした — すぐ下の append パスはすでに読んでいたにもかかわらずです。現在は
  両方がターゲットの実際のプライマリキーを確認し、すでに一致していれば再作成をスキップします。
  キーはダイアログが開く前に実行されるプローブが同時に読むため、描画パスにターゲットへの
  問い合わせは追加されません。意図的に非対称です — 確実に一致する場合のみスキップし、キーを
  読めない場合は「安全」ではなく「不明」として再作成します。

- **Cognito ログインがまったく成功しませんでした — ALB とアプリクライアントでコールバック
  URL の大文字小文字が食い違っていました。** `EnableCognitoAuth=true` でデプロイすると
  サインインが常に失敗し、ホスト型 UI が `Client is not enabled for OAuth2.0 flows.` を
  返していました。`AllowedOAuthFlowsUserPoolClient` は終始 `true` だったにもかかわらずです。
  ALB に名前を指定しないと CloudFormation が大文字小文字混在の名前
  (`mysql--LoadB-u9DQdeKlckt9`) を生成し、`DNSName` がその表記をそのまま継承します。
  アプリクライアントの `CallbackURLs` は `GetAtt DNSName` から作られるため、同じく混在した
  ままになります。しかし ALB は OAuth `redirect_uri` のホストを小文字に変換して送信し、
  Cognito は 2 つの文字列を厳密に比較します。`/oauth2/authorize` はこの不一致を許容するため、
  ログイン画面は表示されて送信だけが失敗し、初回サインインでは「パスワードは変更されたのに
  エラーになる」ように見えました — リダイレクトが拒否された時点で、パスワード変更はすでに
  適用されていたからです。ALB は `${AWS::StackName}-alb` という名前になり、DNS 名が
  (小文字の) スタック名に従うので両者が一致します。

### 追加

- **スタックが最初の Cognito ログインユーザーを作成します。** ユーザープールは
  `AllowAdminCreateUserOnly` を設定するため、ユーザーが存在しないと
  `EnableCognitoAuth=true` のデプロイは成功しても誰もサインインできないアプリを返して
  いました。新しい必須パラメータ `CognitoAdminEmail` がそのユーザーを作成し、Cognito が
  仮パスワードをメールで送信します。未指定の場合はテンプレートの `Rules` アサーションが
  デプロイ前に拒否します。ユーザーを追加できるようにプール ID も出力し
  (`CognitoUserPoolId`)、`CognitoHostedUiDomain` はプレフィックスだけでなく完全な
  サインイン URL になりました。

### 変更

- **デプロイガイドにスタック名の制約を明記しました。** ALB 名はスタック名から派生するため、
  スタック名は小文字で 28 文字以内にする必要があります。これを超えると約 2 分の
  ロールバックの後に
  `The load balancer name '<スタック名>-alb' cannot be longer than '32' characters` で
  失敗し、大文字が混在すると上記のとおり Cognito ログインが壊れます。

## v0.1.228

### 修正

- **再構成されたソース DDL が文字列 DEFAULT を引用符なしで描画し、無効な SQL になっていた問題。**
  `information_schema.COLUMN_DEFAULT` は値を引用符なしで格納しており、それをそのまま出力していたため、
  MySQL が `DEFAULT 'pending'` と表示するカラムが `DEFAULT pending` になっていました。このペインには
  **Copy Source DDL** ボタンがあるため、渡されたテキストは実行できませんでした — MySQL は引用符なしの
  `pending` をカラム参照として解釈します。初回公開リリースから存在していた問題です。

  引用の判断は `default_is_expression` によるようになりました — コンバータが使用するものと同じシグナルです。
  MySQL の `DEFAULT_GENERATED` フラグ由来で、リテラル文字列 `'CURRENT_TIMESTAMP'` と同名の関数呼び出しを
  区別できる唯一の根拠です(値の形状からは判別不可能)。値に含まれる引用符もエスケープします。ライブソースで
  検証済み: 再構成された `ecommerce.orders` DDL が MySQL で実行でき、カラムが同一にラウンドトリップします。

## v0.1.227

### 修正

- **Schema Conversion 画面が、Evaluation で既に指摘済みの事項について沈黙していた問題。** ワークショップで
  `AUTO_INCREMENT` として報告され、収集されるすべてのフィールドとすべての assessor ルールを変換結果と
  照合したところ、同じ形の**9 件**が見つかりました — Evaluation では問題を伝え、変換画面ではクリーンに
  見える状態です。

  再構成された**ソース DDL** が、ターゲットで再現できないものを表示するようになりました:
  `AUTO_INCREMENT`、`ON UPDATE CURRENT_TIMESTAMP`、`COLLATE`、`FULLTEXT`/`SPATIAL` インデックス種別、
  および生成カラムとネイティブパーティショニングのマーカー(いずれも boolean のみ収集されるため、構文を
  捏造せず注記のみ)。通常のテーブルは以前とまったく同じに描画されます。

  **変換の注記**が、まったく通知のなかった 6 ルールをカバーします:

  - **FULLTEXT / SPATIAL インデックス** → 同一カラムへの通常の `CREATE INDEX ASYNC` として出力され、
    通常インデックスと DDL が同一になります。インデックスは作成されますが `MATCH … AGAINST` は使用できません。
    Evaluation は UNSUPPORTED / SIGNIFICANT と評価します。
  - **ネイティブパーティショニング** → 正しく除去されますが(DSQL は主キーで分散)、パーティション範囲の SQL と
    `DROP`/`TRUNCATE PARTITION` によるアーカイブは引き継がれません。
  - **255 カラム上限**、**24 インデックス上限**(主キーを含む) → ハード上限のため *apply で拒否*されます。
    インデックスはテーブル作成成功後に失敗し、ターゲットが部分的にインデックス化された状態で残ります。
  - **大容量 LOB/TEXT** → DDL は問題なく、~1 MiB 上限がマイグレーション中に**行単位で**作用し、超過値は
    恒久的に破棄されます。
  - **生成カラム** → 通常カラムになります。Full Load がソースの計算済み値をコピーするためターゲットは当初
    正しく、値を供給しない最初の書き込みから乖離します。
  - **大文字小文字を区別しない collation** → DSQL は区別するため equality、`LIKE`、`ORDER BY`、`UNIQUE` の
    挙動が変わる一方、行数とチェックサムは一致し続けます。`_ci` のみ報告し、`_cs`/`_bin` は既にターゲットと
    同じです。
  - **`ON UPDATE CURRENT_TIMESTAMP`** → `DEFAULT` は維持されますが `UPDATE` 時に値を更新するものがなく、
    `updated_at` が挿入時点で固定されます。

  生成される DDL は一切変わっていません — 注記です。上限と型集合は assessor から取得し、両エンジンを同一
  テーブルに対して実行してルールが発火したのに変換の注記がなければ失敗するテストを追加したため、将来の
  ルールが同じ空白へ退行することはありません。

## v0.1.226

### 修正

- **Schema Conversion のツリーが、Evaluation レポートで既に命名済みのオブジェクトを別名で表示していた問題。**
  Evaluation は **Stored procedures** と **Functions** として列挙するのに(`KIND_LABELS`)、オブジェクト
  ブラウザは両方を単一の **Routines (n)** ノードにまとめていました — 同じオブジェクトが次の画面では別名で
  現れ、一方の画面の一覧を他方と突き合わせる手段がありませんでした。ワークショップで発見されました。

  現在はツリーがこれらを分割し、見出しを `assessor.KIND_LABELS` から取得します — Evaluation の一覧、UI の
  チャート軸、HTML エクスポートが既に共有しているマッピングです。ここでラベルをハードコードしていたことが
  両者が乖離した原因でした。introspector は当初から `PROCEDURE` と `FUNCTION` を区別していたため、ツリーは
  すでに持っていた情報を捨てていたことになります。MySQL がいずれとも報告しないルーチンは、名前付きの種類に
  断定せず引き続き **Routines** にまとめられます。

  ノード ID は変更ありません(種類に関わらず `routine:<name>`): 選択の解決時にパースされ、チェック済み／生成
  対象の集合がレンダー間でこれを介して保持されるため、種類ごとにキーを変えると復元された選択と DDL 生成範囲が
  静かに無効化されます。

## v0.1.225

### 修正

- **"Generate DDL for selected" を押すとオブジェクトブラウザが初期状態まで折り畳まれていた問題。**
  スキーマを展開してテーブルを見つけてチェックし、生成するとツリーが閉じてしまい、まさに作業していた行が
  見えなくなっていました。ツリーは各レンダー(Generate、apply、進捗ポーリング)ごとに再構築され、NiceGUI は
  開閉状態をクライアント側に保持するため、Python 側で復元しないものは折り畳み状態に戻ります。チェック済みの
  集合はすでにレンダー間で保持されていましたが、展開状態が欠けていた残り半分でした。現在はソース・ターゲット
  両方のブラウザペインがこれを記録し復元します。"Clear" は生成された DDL、編集、AI 提案を引き続き破棄します
  が、どこまで辿ったかは分析結果ではないため保持します。

## v0.1.224

### 修正

- **キャッシュ付き identity 主キーが Full Load 後にシーケンスを進めておらず、カットオーバー後の
  アプリケーションの最初の INSERT が重複キーで失敗していた問題。** コンバータの identity 戦略は
  `GENERATED BY DEFAULT AS IDENTITY` を出力し、`BY DEFAULT` はまさに Full Load がソース自身のキー値を
  書き込めるようにする部分です。しかし**明示的に指定された値はシーケンスを進めません。** そのため
  ロード後もシーケンスは開始位置に留まり、それらの値はすでに使われている状態でした。

  実際の `ap-northeast-2` クラスタで再現しました: id 1〜3 をロードした後、id を指定しない INSERT で
  `duplicate key value violates unique constraint` が発生します。

  これはマイグレーション障害として最悪の形でした。行数とチェックサムが**一致**するため Validation は
  クリーンに通過し、カットオーバー後 — ソースを停止させ、ロールバックが容易でなくなった時点で — 初めて
  現れます。

  現在は Full Load がロードした行より先へ各ターゲット identity シーケンスを進め
  (`ALTER TABLE … ALTER COLUMN … RESTART WITH max(pk)+1`、これもライブ検証済み)、アクティビティログに
  記録します。**完全に終了した**ロードの後にのみ実行されます — `MAX(pk)` がシーケンスが超えるべき値である
  ため、部分的なロードで同期するとまだ到着していない行との衝突が残る可能性があります。ロードを完了させる
  再試行は同期を実行します。プレーンな整数キー(`KEEP_INTEGER` 既定)のテーブルにはシーケンスがないため
  変更せず、空のテーブルも同様です — NULL の `MAX(pk)` から再開するとシーケンスを後退させてしまいます。
  この手順が失敗してもロードは失敗扱いにしませんが、未修正のシーケンスはカットオーバー後の障害であるため、
  正確な手動コマンドとともに FAILURE として記録します。

## v0.1.223

### 変更

- **CloudFormation テンプレートの既定アプリイメージを `0.1.222` に更新**しました（従来は `0.1.209`、
  13 リリース遅れ）。これは新規に `git clone` したユーザーがデプロイする際の既定値であるため、放置すると
  新しいデプロイが静かに古いアプリを起動します — 0.1.218〜0.1.222 の Start CDC 再開修正や
  validation / cut-over の修正を欠いた状態で。

## v0.1.222

### 修正

- **複数スタックを対象とする Start over の teardown が最初のスタックのみを表示し、残りが削除中でも
  沈黙していた問題。** v0.1.214 で Start over が発見された**すべての** cdc-stack を削除するようにしましたが、
  durable な teardown マーカーはスロットが 1 つで、最初のジョブだけがそれを取得します — そのためバナーは
  スタック 1 のみを追跡し、それが終わった瞬間に消え、残りは削除中で MSK / NAT の課金も続いているのに画面には
  何も表示されませんでした。現在バナーは起動されたすべての teardown のキューを追跡します: 追跡中のスタックが
  完了すると次の未完了スタックへ進み、複数が待機中ならどれかを示します
  (*"Deleting 'cdc-b' (2 of 3; the rest follow)"* — そして最後のスタックでは *"(3 of 3, the last one)"*、
  オペレータが待つかどうかを判断する箇所であるため)。

- **完了した teardown がページ更新後に痕跡を残さなかった問題。** 完了は `ui.notify` トーストのみで通知され、
  これは `ui.timer` に依存しページとともに消えます — 15〜45 分かかり、意図的に離席できるよう設計された操作で
  ありながら、*「完了した」*と*「そもそも実行されていない」*を区別する手段がありませんでした。現在バナーは
  結果を durable に表示し（**"CDC infrastructure deleted — MSK / NAT billing has stopped"**、すべての
  スタック名を含む）、オペレータが **✕** ボタンで閉じるまで保持します。自動的に消えることはなく、進行中の
  teardown がある場合は古い完了通知より優先されます。

## v0.1.221

### 修正

- **残る差分が DSQL に格納できない行だけの場合に cut-over へ到達できなかった問題 — ゲート自身の文言は
  可能だと約束していたにもかかわらず。** メッセージは *"Cut over only when Validation reports a clean
  MATCH (**or every difference is explained**)"* と述べていましたが、ゲートは単純一致である
  `ready_for_cutover` を検査していました。"explained" の経路はどこにも存在しませんでした。そのため唯一の
  検出が恒久的に隔離された行（DSQL の 1 値あたり約 1 MiB 上限超過）であるマイグレーションはワークフローを
  完了できず、再ロードでも修正できませんでした — DSQL がその値をまったく格納できないためです。この
  ステップはオペレータの判断ではなく設計上到達不能でした。

  約束された経路を明示的なサインオフとして実装しました: すべての差分がマイグレーションが既に報告した
  ドロップ行と正確に一致する場合、ステップは **"Accept the N-row gap and continue to cut-over"** を
  提示し、テーブル、行数、そして再ロードでは変わらないという事実を併せて示します。受諾するとランブックが
  解放され、代替手段（ソース値を修正して Validation を再実行し完全一致にする）も併記されます。

  **自動解放はしません** — それらの行は実際にターゲットに存在せず、これはオペレータの判断です — そして
  受諾後もランブックは完全一致のように読まれないよう **"Cutting over with an accepted gap"** で
  始まります。最も重要な瞬間にサインオフが静かに忘れられないように。

  サインオフは不足分が**すべて**説明される場合にのみ提示されます。未説明の不一致、比較すらできなかった
  テーブル、あるいは説明済みテーブル 1 つが真に誤ったテーブルと並ぶ場合はいずれも cut-over を閉じたまま
  にし、受諾が後続のより悪い実行へ引き継がれることはありません。

## v0.1.220

### 修正

- **Validation がマイグレーションで既にドロップした行を未説明の失敗として報告し、同一画面上で自己矛盾して
  いた問題。** あるテーブルが隔離された行数とちょうど同じだけ不足している状況で、パネルは
  **Not ready for cut-over — "1 of 8 table(s) did not pass. Review the failing checks"** と、
  それらの行を数える赤い **Failed** チェック 2 件を表示していました — すぐ下のテーブル別項目は
  *"Fully explained: 3 rows were permanently dropped … this deficit is expected, not new data
  loss."* と述べていたにもかかわらずです。レビュー担当者は、既に発見・報告され Full Load ステップで明示的に
  受諾された欠陥の調査へ追い込まれていました。

  テーブル別モデルはこれを既に把握していましたが（`deficit_explained_by_quarantine`）、集約する箇所が
  なく、サマリと準備状況チェックがそれを参照していませんでした。現在は参照します:

  - **判定**が実際に残っているものを述べます — *"Cut-over blocked only by rows dropped during the
    migration … Nothing unexplained"* — そして実際の選択肢 2 つを提示します: ソース値を修正して再ロード、
    または意図的にギャップを受諾。
  - **準備状況チェック**が原因を文中に示し、**Failed** から **Heads-up** に下がります。

  意図的に**合格とは報告しません**: それらの行は実際にターゲットに存在しないため、cut-over はツールが
  通すものではなくオペレータが下す判断として残ります。

  緩和は不足分が**すべて**説明される場合にのみ適用されます。1 行ドロップして 3 行不足のテーブルは強い失敗の
  まま、説明済みテーブル 1 つが真に不一致のテーブルと並ぶ実行も赤のままです — 既知の損失の背後に実際の損失を
  隠すことは、この帰属ロジックが決して生んではならない結果です。

## v0.1.219

### 修正

- **Validation の進行中パネルで Cancel ボタンのツールチップがラベルとして描画されていた問題。** 文全体 —
  *"Stop the comparison. Tables not yet started are skipped; the ones already running finish
  first. Read-only, so nothing is left half-changed."* — がボタンのキャプションになり、ボタンが
  パネル全体に広がって、実際の動作を示す文字列（"Cancel validation"）が消えていました。

  原因: NiceGUI の `Element.tooltip()` はツールチップを作成した後、チェーン用に **`self`**（所有
  エレメント）を返します — ツールチップは返しません。そのため戻り値を受けて `set_text()` で実行中／
  停止中の文言を切り替えるコードは、実際には**ボタンの**テキストを書き換えていました。現在はボタンの
  コンテキスト内で `ui.tooltip()` からハンドルを取得します（こちらはツールチップ要素を返します）。
  テキストはその場で入れ替わるため、ポーリングがホバー中のツールチップを破棄することはありません。

  これがテストで見えなかったのは、NiceGUI のダブルが `Element.tooltip()` をツールチップオブジェクトを
  返すものとしてモデル化していたためです — 実際の API とは正反対です。その結果、誤った呼び出しが
  テストでは正しく見え、画面では壊れていました。現在はダブルが実際の戻り値を反映し、回帰テストが
  ツールチップの文言がボタンテキストとして現れないことを検証します。バグを再導入して確認: テスト 3 件が
  失敗します。

## v0.1.218

### 修正

- **Full Load のジョブ記録が失われた状態で Stop 後の Start CDC が無効 — 実際には完全に再開できるのに。**
  ボタンの準備条件が開始位置（Full Load ウォーターマークまたは手動入力の座標）を要求していましたが、
  ウォーターマークは Full Load の**ジョブ記録**から読まれます。そのためアプリ再起動後（ジョブ記録が削除済み）
  や CDC のみのセッションでは存在せず、Start CDC が *"Set the CDC start point above first"* と共に
  無効化されていました。

  実際には何も失われていません。CDC の停止はコネクタ 2 つだけを削除します: ソースコネクタのオフセット
  トピックはインスタンスごとの UUID トピックではなく固定名（`<stack>-debezium-source-offsets`）に
  ピン留めされているため Stop を越えて残り、次の Start で seeder がそのオフセットを読み、ウォーターマーク
  以降であれば再シードを*スキップ*します。停止したまさにその位置からストリーミングが再開されます。つまり
  ゲートはバックエンドが既に対応している再開を阻んでおり、オペレータを binlog 座標の手入力や Full Load
  全体の再実行へ追い込んでいました — コネクタが保持し続けていた位置を取り戻すために。

  現在はスタックにコミット済みの再開オフセットがある場合も Start CDC が解除されます。シグナルは
  `MskBootstrapServers` が空で `DeploySink=true` の組み合わせで、これは曖昧ではありません: インフラ作成は
  `DeploySink=false` に固定し、`true` にするのは Start CDC のみ、Stop は*ブートストラップのみ*を上書き
  します（他は `UsePreviousValue` で引き継ぎ）— したがってその組み合わせは「開始して停止した」経路のみで
  到達します。一度もストリーミングしていないスタックは引き続き開始位置を要求し、これが初回開始がソースの
  現在の binlog から始まって Full Load 期間全体を静かに失うことを防ぎます。

- **再起動を再起動として表現します。** パネルが初回開始の文言（"…begins streaming"）を表示し、開始位置
  カードは **Action needed** バッジと *"Automatic — needs a Full Load watermark (unavailable)"* を
  提示していました — その下のボタンは有効で正常に動作するのに。再開時に選ぶ開始位置は存在しないため
  （位置はそのカードが設定できないオフセットトピックにある）、現在は再開の事実を示します:
  *"Resuming from the last streamed position"*。

- **Stop CDC ダイアログがストリーム位置の保持を明記します。** 従来は MSK とプラグインが保持されるので
  "so you can restart with Start CDC" とだけ述べており、**インフラ**は語っても**位置**は語らないため
  オペレータに推測を委ねていました。その合理的な推測（コネクタを削除すれば位置も失われる）は誤りで、
  それに基づいて行動すると再ロードのコストが生じます。現在は Start CDC が停止したまさにその位置から続き、
  ギャップも再適用も、Full Load や開始位置の再入力も不要であることを明記し、停止／再起動を自由に
  繰り返せると案内します。

## v0.1.217

### 変更

- **Activity log タブの Download ボタンをフルサイズの主要アクションに戻しました。** 前リリースでは共有の
  フォーム行を通していましたが、それだとボタンが右側の**コントロールスロット**に入ります — 数値入力の
  サイズに合わせられ、入力の**列**が揃うよう右寄せされたスロットであり、ボタン 1 つには意味がありません。
  結果として小さなボタンが右端に取り残され、説明がその下に回り込んでいました。このタブはフィールド群では
  なくアクションなので、説明のあるセクションの下にアクションを置く形にし、単なる動詞ではなく対象を明示
  しました（"Download activity log"）。

- **Settings ヘッダーの注意文をグレーの微細テキストから info 通知に昇格しました。** これは必要です —
  実際のミスを防ぎます: 値を調整して離れたオペレータはそれが保持されると想定しますが、再起動（本人が
  開始していない Fargate タスク置換を含む）が起きると静かにデプロイ時の既定値へ戻り、次の実行が理由の
  分からないまま異なる挙動になります。タイトル下のキャプションでは定型文として読まれ、読み飛ばされて
  いました。現在は結果を先に述べ（"These settings are not permanent"）、永続的な代替手段（デプロイで
  `DSQL_MIGRATOR_*` 環境変数を設定）を示します。

- **Start CDC が正常系で同じ重みの通知を 2 つ積み重ねなくなりました。** "Ready to start CDC" の下に
  "This table set is now fixed" というヘッダーの全幅の青いボックスがもう 1 つあり、初回開始という通常の
  状態で通知が 2 つになっていました。そしてオペレータが実際に探す 1 行（**どの**テーブルがストリーミング
  されるか）が MSK パーティション会計の段落の中に埋もれていました。現在はテーブル一覧はチェックグリフ横の
  プレーンテキストです — 検証可能な事実なので**可視のまま**にし — 不変性の根拠と範囲変更の方法は info
  ツールチップへ移しました（多くても一度必要な背景知識）。再開始の警告（amber）はそのままです: 繰り返しの
  start/stop は回収されない MSK 容量を実際に消費するため、あちらは本物の警告です。

## v0.1.216

### 変更

- **Settings ダイアログがタブ切り替えでサイズ変更しなくなりました。** パネルコンテナが min/max の高さ
  範囲を使っていたため各タブの自然な高さになり（Full Load はノブ 3 個、Validation は 1 個）、切り替える
  たびにカードが伸縮していました。中央寄せダイアログは中心を基準に配置されるため**タブストリップ自体が
  ポインタの下で動き**、タブをクリックして進むたびにパネル全体が跳ねていました。現在は最も高いパネルに
  合わせた固定高さなのでストリップが固定され、内容だけが変わります（ビューポート上限と内部スクロールは
  維持 — 小さな画面でもダイアログが画面外へ押し出されません）。

- **タブ順をマイグレーションの流れに合わせました: Full Load → CDC → Validation。** CDC は Full Load と
  対（どちらもデータ移動のスループット）、Validation は事後の検証です。CDC が最後だったのは、そのノブが
  後から追加されたからにすぎません。順序は config レジストリの性質で、タブストリップはそれに従います。

- **"Sink compute (MCU)" に 1 行では収まらないガイダンスを info ツールチップとして追加しました:**
  いつ上げるか（ソースは追いついているのにシンクが遅れているとき — **ソース**の MCU を上げても無意味）、
  コスト（各段階でコネクタが稼働する間ずっと課金、8 が MSK Connect API の上限）、いつ反映されるか
  （次の Start CDC。リサイズのみを目的に再実行するのは安全 — コネクタ容量は in-place 更新なので
  レプリケーションのギャップも MSK パーティションクォータのコストもありません）。可視のラベル・説明・
  許容値は変わらないため、ホバー専用への逆行ではなく深さの追加です。

- **Activity log タブが他のタブと統一されました。** 段落の下にボタンが置かれた形で、別種の画面のように
  見えていました。現在は同じフォーム行（ラベル + 説明 + コントロール）で、行がすでに何であるかを述べて
  いるためボタンは "Download" に縮めました。ツールチップには、ECS ではこのファイルが一時的なタスク
  ストレージ上にあること、永続的な CloudWatch のコピーは Diagnostics → Mirror to stdout であることを
  記載しました。

## v0.1.215

### 変更

- **Settings にチューニングカテゴリごとのタブ — Full Load、Validation、CDC — を用意しました。**
  以前は "Performance" タブ 1 つにすべてが入っていました。"Performance" はオペレータが考える区分では
  ありません — Full Load を変えたい、あるいは CDC シンクを変えたいという目的で来ます。統合パネルは他の
  グループを読み飛ばさせ、各グループの反映タイミング文言（「次の実行」vs「次の Start CDC」）が一覧の
  途中に置かれて、直後のフィールドへの注記のように読まれていました。現在は各タブが自身の反映タイミングで
  始まり、Full Load のコネクション積算の注意は該当タブのみに表示します（Validation・CDC の単一フィールドの
  横では意味がありません）。タブ一覧は config レジストリから導出されるため、新しいグループにノブを追加すれば
  タブも自動的に増えます。

- **すべての設定コントロールが AWS スタイル（Cloudscape）のフォームフィールドになりました: 可視ラベル、
  説明、許容値を示す constraint テキスト。** 以前は各ノブを 1 行に収めるため説明を**ホバー専用**の info
  グリフの背後に隠しており、フォームを一目で読めず（何をする値か知るには 5 つのフィールドを順にホバーする
  必要がありました）、ホバーのないタッチ環境ではアクセスすらできませんでした。コントロールは右揃えにして
  入力列がパネル下方に整列し、constraint は monospace で描画して許容値がデータとして読めます。インライン
  スタイリングではなく `ui/design.py` の `form_field`（単一のソース）として追加しました。

- **Diagnostics タブも同じフォーム行を使用します。** フローティングラベル付き select の隣に素の switch が
  並び、1 つのフォームではなく無関係なウィジェット 2 つとして読まれていました。現在は双方がラベルと説明を
  持ちます（stdout ミラーが CloudWatch に届く経路であることも含みます — ログファイル自体は一時的な
  タスクストレージ上にあります）。

- **モーダルのヘッダーはもう "changes apply to the next run" と述べません** — これは Full Load /
  Validation のノブにのみ当てはまります。現在はダイアログ全体で真であることのみを示します: デプロイ時
  パラメータではなく、再起動で値がリセットされます。

## v0.1.214

### 修正

- **アカウントに cdc-stack が 2 つ以上あるとき、Start over が "Delete all CDC infrastructure" を
  提示して何も削除しなかった問題。** 提示側は発見されたすべてのスタックを数えていましたが、teardown は
  名前を*1 つだけ*解決し、発見されたスタックがちょうど 1 つのときにのみ採用していました — 複数ある場合は
  このセッション自身のスタック名にフォールバックしますが、その分岐はプローブがその名前を**見つけられ
  なかったときにのみ**到達します。そのため削除は対象を見つけられず成功として報告し、オペレータはツール上に
  それを指すものが何もない状態で MSK / NAT の課金を払い続けました。現在は提示・ダイアログの一覧・teardown が
  1 つのリゾルバを共有し、**すべての**スタックを処理します。共有される source 資格情報シークレットの
  クリーンアップは 1 回だけです（スタック外で作成されるため、スタックごとに削除を再スケジュールすると
  追加スタック分だけ失敗します）。

- **Start over の teardown タイルが削除対象の cdc-stack 名を表示します。** "Delete all CDC
  infrastructure" は*何を*削除するのか述べていませんでした。スタックに所有者タグがないため、アカウントには
  別のウィンドウが使用中のパイプラインが存在しうるうえツールには判別できないので、オペレータが安全に
  答えられる唯一の手がかりが名前です。タイル上部の notice には名前がありましたが、そこでは削除対象では
  なく質問の背景として読まれます。現在は破壊的な 2 つのタイル双方が名前を（複数なら件数も）示し、文言全体が
  複数形に切り替わります。

- **Start CDC のヒントが、すでにロックされている段階でテーブルを選ぶよう促していた問題。**
  "Pick all your tables before you start … Choosing everything you need up front keeps this
  smooth" はカード phase が `infra` のときにのみ描画され、これはプローブされた
  `cdc_stack_phase` が `infra` であることを要求します — まさに `selection_lock_reason` が
  テーブルピッカーを凍結する条件です（このボタンに到達しうるすべてのマイグレーションタイプで）。
  チェックボックスが無効なのにヒントがそれを指していました。

  現在は事実と**実際に有効な**解決策を状況別に示します: Full Load 後は **Start over** のみが範囲を
  変更します（Full Load のロック節が優先し、cdc-stack の削除では解除され*ません* — 本修正の最初の草案は
  オペレータを ~45 分の teardown に送り、それでもピッカーは同じくロックされたままでした）。CDC only の
  セッションではインフラの削除と再デプロイが実際に機能します。Full Load 後の文言には集合が固定される
  **理由**も含めました — スナップショットと一致することがギャップレスな引き継ぎの前提です。

## v0.1.213

### 追加

- **CDC シンクのコンピュートを UI から調整可能に — Settings → Performance → CDC →
  "Sink compute (MCU)"。** マニュアルは以前から、シンクが追いつかない場合は（ソースではなく）
  `SinkMcuCount` を上げるよう案内してきました。シンクがパイプラインの CPU バウンドな半分であり、
  シングルタスクの Debezium ソースは CPU に余裕があるためです。ところがアプリはそのパラメータを
  **一度も送っていませんでした** — `grep SinkMcuCount src/` は 0 件で、すべてのデプロイが静かに
  テンプレート既定値を使い、`submit_update` が `UsePreviousValue` としてそれを引き継いでいました。
  マニュアルの助言を実行する唯一の方法が CloudFormation コンソールでスタックパラメータを直接
  編集することであり、「コア機能はすべてブラウザから到達可能」という原則に反していました。
  今後は 3 つの経路すべてで `SinkMcuCount` を渡します（インフラ作成、Start CDC、および読み取り
  専用のパラメータプレビュー — プレビューが実際のデプロイと異なる値を示さないように）。

  提供値は 1 / 2 / 4 / 8 のみで、数値入力ではなくドロップダウンで描画します。これは MSK Connect
  API の `mcuCount` の有効値（ワーカーあたり最大 8）であり、数値フィールドなら 3 も受け付けて
  しまい、課金の始まった Start CDC の数分後に CloudFormation が拒否します。保存前にこの集合で
  厳密に検証します。

  ツールの既定値は意図的にテンプレートと同一（4）です。異なる値だと、このパラメータを送る前に
  デプロイされたスタックでは実際の設定変更として読まれ、次の Start CDC で RUNNING な
  コネクタ 2 つを不必要に再作成し、回収されない MSK パーティションクォータを消費します。

### 変更

- **Settings → Performance フォームをセクションに分割し、各セクションが自身の反映タイミングを
  明示します。** 従来はすべてを "applies to the next run" と表示していましたが、これは
  Full Load / Validation のノブにのみ当てはまります（ローダーと検証器が実行ごとに
  `load_config()` を呼ぶ）。CDC のノブは CloudFormation パラメータなので読み直す主体がなく、
  すでにストリーミング中のシンクは Start CDC がコネクタを更新するまで現在の容量を保ちます。
  そのため CDC セクションは "applies to the next Start CDC" と表示し、確認トーストも各ノブの
  実際のタイミングを繰り返します。グルーピングを config レジストリへ移したことで潜在的な
  描画バグも解消しました: 以前のループはタプルを走査しつつグループが変わるたびにヘッダを
  出していたため、あるグループのノブが連続していないと 2 つのヘッダに分割されていました。

- **マニュアル §7（パフォーマンスとチューニング）にシンク MCU 変更の反映タイミングを記載**
  しました。シンクのリサイズだけのために Start CDC を再実行しても安全である点も含みます:
  コネクタの `Capacity` は in-place 更新なので、シンクは再作成ではなくリサイズされます —
  テーブル集合の変更と違い、パーティションクォータのコストもレプリケーションのギャップも
  ありません。

## v0.1.212

### Fixed

- **gapless な開始が不可能なウォーターマークにも「Automatic — gapless from Full Load」を提示していました。**
  このオプションは「再開座標が 1 つでもあるか」で判定されていましたが、ハンドオフは MSK の
  `connect-offsets` に binlog の **file:position** をキーとするレコードをシードする方式です — in-VPC の
  シーダーはそれが無いウォーターマークを拒否し、`build_watermark_params` は全て空の値を返すため、
  テンプレートはシーダーをスキップし、コネクタはソースの **現在** の binlog から開始します。したがって
  GTID のみの場合「gapless (recommended)」と *Ready* が表示される一方、Full Load 中のすべての変更が静かに
  失われ、Validation まで、あるいはカットオーバー後まで気づかれませんでした。

  これは理論上の話ではありません: 2 つの座標は独立に劣化する別々のクエリから得られます —
  `SHOW MASTER STATUS` は `REPLICATION CLIENT` 権限を必要とし (RDS/Aurora ではしばしば制限される)、
  `@@GLOBAL.gtid_executed` は通常のグローバル読み取りです。Automatic は `can_seed_offset()` で判定するように
  なり、GTID のみのケースには独自の文言を使います — 「needs a Full Handle watermark」とは言わず
  (ウォーターマークは存在する)、欠けている binlog 位置とその結果、そして対処法 (`REPLICATION CLIENT` を
  付与して Full Load を再実行) を明示します。
- **CDC ステップは、別のテーブルをストリーミングしているパイプラインにも Attach を提示していました。**
  v0.1.211 はプランレベルのバナーを保護しましたが、このパネルは別の描画経路であり、検査が一切ありません
  でした。同じスコープ検査で Attach を提示しないようになりました。

### Changed

- **アタッチが安全でない場合は、デプロイを進むべき道として提示します。** デプロイフォームは
  「Deploy a separate CDC pipeline instead」という名前で警告の三角形の背後に折りたたまれていました —
  そのため候補が不一致のとき、運用者は押してはいけない目立つ青い Attach ボタンを見て、正しい操作は
  危険なものに見え、しかも **隠れて** いました。アタッチ可能な候補が無い場合は展開状態で
  「Deploy a CDC pipeline for this table set」というタイトル、警告グリフなしで表示されます。アタッチが
  **有効** な場合は折りたたみと警告を維持します — 2 つ目の MSK クラスタは高価で、意図される場合が稀だからです。

### Tests

- UI の gapless の主張が、シーダーが実際にデプロイされるかどうかと一致することを、ウォーターマークの 4 つの
  形すべてについて検証する不変条件テストを含みます。変異 4 件を検出。当初 1 件が通過しました —
  `can_seed_offset()` を `has_coordinates()` に戻す変異 — 他のすべてのテストがフラグを事前計算して渡すため、
  配線が未検証でした。

## v0.1.211

### Fixed

- **異なるテーブル集合をストリーミングしている CDC パイプラインにも「Attach」を提示していました。**
  アタッチはセッションを稼働中のパイプラインに向け、そのパイプラインがストリーミング中であるため
  Data Migration を `DONE` に昇格させ Validation を解放します。実アカウントで確認: あるスタックが
  `ecommerce_demo.*` の 11 テーブルを複製している一方、セッションは直前に `ecommerce.*` の 8 テーブルを
  投入していました。アタッチしていれば移行完了と報告してカットオーバーへ進めさせながら、**このセッションが
  投入したすべてのテーブルには CDC が一切付かず**、ウォーターマーク以降のソース変更をすべて静かに失って
  いました。

  候補パイプラインがこのセッションの投入テーブルを複製していない場合はアタッチを提示せず、どのテーブルが
  カバーされないかを正確に示す通知と、2 つの進め方 (このテーブル集合向けに CDC をデプロイする、または選択を
  そのパイプラインに合わせる)、そしてアイドルのインフラが課金され続けている旨を表示します。意図的に非対称
  です: パイプラインが選択より **広い** 場合は不一致ではありません — 別のテーブル集合を並行して扱っている
  可能性があり、このセッションが所有するものは何もカバーされずに残りません。また、テーブル集合を読み取れない
  候補はアタッチ可能なままにします — 未調査のスタックをブロックすると 2 つ目の高価な MSK クラスタのデプロイへ
  誘導してしまい、それはまさにこのバナーが防ごうとしているものです。

### Changed

- **Start over が、稼働中の CDC パイプラインをこのセッションのものだと示唆しなくなりました。** スタック名を
  明示し、以前のセッションが残したものか、同じアカウントを見る別のウィンドウが使用中である可能性を明確に
  述べます — スタックに所有者タグが無いため、ツールには判別できません。「Leave CDC untouched」は、他の何かが
  そのパイプラインを使っている場合の正しい選択として説明され、先延ばしのようには読めません。

## v0.1.210

### Fixed

- **Start over が、別の名前でデプロイされた cdc-stack の削除を提案しませんでした。** CDC は一切ないと
  報告した直後、CDC ステップへ移動すると `mysql-dsql-cdc-stack-0729-new` への *アタッチ* を尋ねました —
  アカウントに実在するスタックについて 2 つのプロンプトが矛盾していたのです。Start over の 2 つのシグナル
  (`cdc_stack_phase`、`cdc_connector_names`) は **この** セッションが対象とする名前に限定されるため、
  以前のセッションがデプロイしたスタックやカスタムのサフィックスを持つスタックは見えていませんでした —
  そして沈黙していた方が、MSK / NAT の課金を止められたはずのプロンプトです。Start over も検出済みの
  スタックを参照するようになり、削除対象は提案した **まさにその** スタックとして解決されます (提案だけを
  検出一覧に合わせると、削除を提案した後に存在しない名前を対象にし、静かに何もせずインフラの課金を
  残すことになります)。検出されたスタックが複数ある場合は提案はしますが選択はしません — それぞれが別の
  パイプラインである可能性があるため、どれを削除するかは CDC ステップで運用者が決めます。

### Deployment

- `0.1.209` を ECR Public に公開し、テンプレートの `ContainerImageUri` 既定値をそこに向けました。既定値は
  `0.1.188` まで遅れており (21 リリース遅れ)、これは新規に `git clone` した利用者がデプロイするイメージ
  であるため、これを強制するガードテストが再び通過します。ソウルの Fargate スタックも同じビルドに更新しました
  (change set: TaskDefinition と Service のみ、24 個のパラメータはすべて保持)。

## v0.1.209

### Fixed

- **破棄された行ごとに「Reload」ボタンがありましたが、Reload はテーブル全体に作用します。** 3 行が
  破棄されるとカードが 3 枚でき、それぞれの Reload が完全に同じ動作をしながら、その行だけを処理するように
  見えていました。破棄された行は **テーブルごとに 1 枚のカードへグループ化** され、Reload も 1 つになりました。

### Changed

- **1 つのテーブルの破棄行を、カード 1 枚ずつではなくコンパクトに表示します。** カードごとにテーブル名と
  同じ理由を繰り返していたため、3 行の破棄がほぼ同一のボックス 3 枚で画面を埋めていました。テーブルごとの
  1 枚のカードがテーブル名と理由を 1 度だけ述べ、件数を表示し (「3 rows dropped」)、主キーを monospace の
  チップとして列挙します — 件数が増えても読める形です。チップが 12 個を超えると「+N more」で省略され、
  件数バッジは常に実際の合計を報告し、完全な一覧はダウンロード可能なエラーログにあります。1 つのテーブル内で
  理由が実際に異なる場合はすべて保持します — 重複排除が 2 つ目の原因を隠してはならないため — 主キーを
  解析できないメッセージも消えずに理由を残します。

### Tests

- 変異 6 件を検出 — グループ化の削除、異なる 2 つの理由を 1 つに潰す、チップ上限の削除、件数バッジが実際の
  合計ではなく省略後の数を報告するケースを含みます。

## v0.1.208

### Fixed

- **複数行が破棄されたのに、一覧には 1 行しか表示されませんでした。** 件数は「3 rows permanently
  dropped」なのに下の一覧はちょうど 1 件 — パネルが `latest_messages()` から構築されており、この関数は
  **テーブルごと** に 1 メッセージしか保持しないため(最後の書き込みが勝つ)、N 行破棄したテーブルが 1 行
  しか列挙されず、同じ画面の 2 つの数値が食い違っていました。破棄された各行がそれぞれの主キーとともに
  表示されます — 主キーが各エントリの実行可能な部分 (ソースを検索する際に使う値) だからです。行ごとの
  レコードを渡さない呼び出し元 (旧来の呼び出し箇所、またはインメモリログが失われた復元セッション) には、
  何も出さない代わりにテーブル単位の表示を維持します。

### Changed

- **ギャップ受け入れ時に、ほぼ同一の緑のボックスが 2 つ出なくなりました。** 確認通知が、すぐ上の完全性
  バナーが既に述べている内容 (件数、テーブル、次のステップの解放、Validation がギャップを報告すること) を
  繰り返していました。今はバナーに無い事実だけを載せた 1 行 — ソース値を修正してテーブルをリロードすれば
  ギャップは閉じられる — と、クリックを認識するチェックマークで表示します。
- **エラーログのダウンロードを受け入れボタンの直下から移しました。**「Accept quarantined rows &
  continue」のすぐ下にあると、その決定の副次的な選択肢のように読めましたが、実際には同じ行ごとの情報を
  持ち帰る手段にすぎません。今はそれが直列化する詳細のそばに配置されます。
- **ウォーターマークのテーブルごとの件数が周囲のパネルと調和します。** Quasar の既定の展開ヘッダー
  (灰色の全幅バーと大きな先行グリフ) は平坦なパネルを横切る重い帯であり、件数の行は上の座標とは異なる
  配置を使っていました。今は両方がラベル + monospace 値の同じ形を使い、ヘッダーもフィールドラベルと
  同じサイズです。
- **「Workloads to migrate」の下の常設の説明文を削除しました。** その 3 つの主張はいずれも、読み手に
  とってより有用な場所で既に述べられています: ピッカーの説明文が選択の出所を伝え (すぐ上にバッジで列挙)、
  Export watermark パネルが約束ではなく実際の座標を示し、確認ダイアログがコミットの瞬間にソースは
  読み取り専用であることを明示します。

## v0.1.207

### Changed

- **ギャップ受け入れの操作が、それが実行する判定の下に移動しました。** 隔離パネル内で描画されており、この
  パネルは完全性バナーより *前* に来ます — そのため運用者は、判断の対象である結論を読む前に決定を求められて
  いました。順序は、行ごとの詳細 → 判定 → 判定が説明した操作 → ダウンロード になりました。
- **「Data errors」の見出しと件数を削除しました。** エラーがある場合、既に上にテーブル・主キー・理由と
  ともに全件が列挙されているため、件数を繰り返す見出しは同じ事実の 4 回目でした。無い場合は
  「No data errors recorded.」の上にセクションヘッダーを描き、不在を主張するブロックになっていました。
  このセクション固有の価値はダウンロードなので、ボタンのみを残しました。
- **ダウンロードボタンの名前を、ファイル形式ではなく読み手基準に変更しました。**「Download error log
  (NDJSON)」は誰も尋ねていない形式を前面に出し、Full Load と CDC の両方にダウンロードがあるのに、
  どちらのエラーなのかを伝えていませんでした。今は「Download Full Load error log (3 errors)」/
  「Download CDC error log (N errors)」と表示し、形式と 1 行あたりの内容はツールチップに移しました。

### Fixed

- **「Accept quarantined rows & continue」ボタンが無反応に見えていました。** クリックは **動作していました** —
  ステップを完了にし、次のステップを解放し、activity log にも記録します — しかし描画経路が受け入れフラグを
  誰も読んでいなかったため (次の投入実行時のみ消費される)、パネルとボタンが同一のまま再描画されていました。
  ボタンは緑の「Gap accepted — Full Load marked complete」通知に **置き換わり**、今何が可能になったかも
  伝えます — 2 度目に押しても同じく見えないクリックを誘う操作要素は残しません。
- **受け入れたギャップが依然として問題として報告されていました。** amber の「Full Load finished with
  issues」バナーが緑の確認通知の直下に残り、運用者が明示的な判断で解決したまさにそのことを再び問題として
  提示していました。今は「Full Load complete — with an accepted gap」と表示し、破棄された行を明示して
  Validation を指し示します — ただし全行が投入されたとは決して主張しません (事実ではないため)。**本物の
  失敗** がある実行はフラグが設定されていても警告を維持するため、ギャップの受け入れがリトライ可能な作業を
  覆い隠すことはありません。

### Changed

- **Snapshot row counts をウォーターマークのパネルに調和させました。** パネルの *下* に全幅の展開として
  ぶら下がり、枠付きの `ui.table` を包んで独自のソート可能なヘッダーまで備えていました — 画面の他のどこでも
  使っていないスタイルの 2 つ目の視覚的コンテナでした。テーブルごとに 1 つの値なので、今はパネル内の
  ラベル付き行として上部の座標と同じ形で表示し、右寄せの monospace と桁区切りで数値が揃い一目で比較できます。
  リストが長くなり得るため既定の折りたたみは維持します。

### Tests

- 変異 6 件を検出。当初 1 件が通過しました — 描画呼び出しから
  `quarantine_accepted=migration_state.accept_quarantined_rows` を削除する変異 — 他のすべてのテストが
  フラグを直接渡すため **配線** が未検証でした。今セッションで state→render の配線の穴がすり抜けたのは
  3 度目なので、2 つの描画呼び出しを covering する構造的な断言を追加しました。

## v0.1.206

### Changed

- **同じ破棄を 8 か所で告知していたのを整理しました。** 3 行の隔離が、要約チップ、行の Status バッジ、
  Attempts セル、セクションヘッダー、行ごとのカード、完全性バナー、data-error の件数、そして赤い
  「Load failed」ボックスの 8 か所で報告されていました。各ボックスが 1 つの役割だけを担います:
  - **Attempts** セルは繰り返しません — 同じ行の Status バッジが既に「3 dropped」チップ (ホバーで説明) を
    持っており、1 つのテーブル行で同じ事実を 2 度述べていました (その他のエラーは引き続き
    `1 · 3 errors`);
  - 隔離セクションの **件数ヘッダー** を削除しました。このセクションは他のどこにもない行ごとの詳細
    (どの行か、なぜか、Reload) を示し、判定はバナーが担います;
  - 隔離が **唯一** の不完全性である場合、赤い **「Load failed」** ボックスを抑制します。バナーの内容を
    例外クラス名とともに赤で繰り返し、amber の「残りは投入された」という枠組みと矛盾していました。
    決して投入できない行だけの実行に「failed」は過剰です。本物の失敗は原文のまま引き続き表示されます。
- **Export watermark が統計テーブルの下に移動し、コンパクトになりました。** 区切り線とテーブルごとの
  進捗の間にあり、進捗テーブル (および完了した実行では完全性判定と隔離の詳細) を静的な参照データの下に
  押し下げていました。一度読む出自情報なので、ライブ詳細の後に配置しつつ、refreshable 領域の **外側** で
  描画を維持し、~1.5 秒のポーリングが行数の展開を折りたたまないようにしています。4 つの固定座標は、
  4 行のために「Field」「Value」ヘッダーまで備えたソート可能な 2 列の `ui.table` でしたが、1 つの枠付き
  パネル内のラベル付き monospace 行になり、識別用のサマリーはヘッダー行に、利用不可の座標は欠損値のように
  見えないよう淡色にしました。テーブルごとのスナップショット件数は本来的に表形式なので、折りたたみ
  テーブルとして維持します。

### Tests

- 変異 4 件を検出。当初 1 件が通過しました: 描画順序の断言が `src.index("_live_detail()")` を使っており、
  これは `def` の行に先にマッチするため、2 つの呼び出しを入れ替えても通過していました。今は AST で
  **呼び出し** の行番号を比較し、実際に入れ替えて検証しました。

## v0.1.205

### Fixed

- **隔離のヘッダーが行数ではなくテーブル数を数えていました。** すぐ下のバナーが「3 rows permanently
  dropped」と言っているのにヘッダーは「Quarantined rows (1)」と表示され、同じ画面の 2 つのボックスが
  同じ数値について食い違っていました。ヘッダーが数えていたリストは *テーブル* ごとに 1 エントリ
  (そのテーブルの最新メッセージ) です。行数とテーブル数を分けて報告するようになりました:
  *「3 rows permanently dropped across 1 table — the rest of each table loaded」*。

### Changed

- **破棄された行が、1 行の生ログではなくラベル付きの 3 つの事実として読めます。** 以前は生のログテキスト
  (`quarantined row pk[id=3]: datatype limit greater than 1048576 bytes not supported for bytea`)
  で表示され、テーブル名は上のバッジに、主キーは文中に埋もれていました。今は amber のカードで表示します:
  テーブル名を目立つテキストで、**主キーを独立した monospace チップとして** (ソースを検索する際に使う
  実行可能な手がかりであるため)、「dropped」バッジ、そしてその下に冗長な `quarantined row pk[...]` の
  接頭を除いた技術的な理由。解析できないメッセージは加工せずそのまま表示します。
- **Attempts 列がその数値の意味を伝えます。** `1 · 3 err` はリトライ回数のように読め、それが *ターゲットが
  永久に保持しない行数* であるという手がかりが皆無でした。恒久的に隔離された行は
  `1 · 3 rows dropped`、それ以外は `1 · 3 errors` と表示します。
- **「Accept quarantined rows & continue」の横の重複した説明文を削除しました。** 完全性バナーの対処案内と
  同じ内容 (ソース値を修正して当該テーブルを Reload するか、ギャップを受け入れる) であり、同じ画面に同じ
  助言が 2 度出ていました。バナーは判定と対処を併せて示すため、バナー側を残します。

### Tests

- 変異 5 件を検出 — テーブル数ヘッダーの復元、難解な `err` 表記の復元、不正な形式のメッセージから誤った
  主キーを取り出すケースを含みます。

## v0.1.204

### Fixed

- **復元されたセッションで「Accept quarantined rows & continue」ボタンが消えていました — 完全な行き詰まり。**
  Full Load は `FullLoadIncompleteError` で終わり、そのメッセージは運用者にまさにそのボタンを使うよう案内
  しますが、アプリ再起動後はボタンが描画されませんでした。quarantine-only の判定が **インメモリ** の
  `ErrorLogStore` の行を数えていたため、再起動でカウントが 0 になり判定が `False` になっていました。他の
  復旧経路もありません — 恒久的に拒否された値はリトライしても投入されません — 残るのは「Start over」だけ
  でした。カウントは **ジョブの chunk** から取得するようになり (ジョブストアは永続的)、旧バージョンが
  書いたジョブでも動くようエラーログのスキャンにフォールバックします。未完了テーブルがある場合に
  override を保留するガードは変更ありません。

### Added

- **失敗が永続的な activity log に診断情報を残すようになりました。** activity log はセッションより長く残る
  記録ですが、失敗エントリの 3 種類はそれだけではトラブルシューティングできませんでした:
  - **隔離された各行** はインメモリのエラーログにのみ記録されており、再起動後は *どの* 行が失われたのか
    分からず、件数だけが残っていました。破棄された各行が主キー、拒否理由、そしてテーブルの残りは正常に
    投入された旨を記録します。共有ヘルパー経由で 3 つの投入経路 (in-process、シャードワーカー、単一
    テーブルワーカー) すべてに配線しました — シャードされるテーブルは大きいテーブルであり、手作業で
    確認される可能性が最も低いケースです。
  - **「1 of 8 table(s) did not fully load」** は件数であって診断ではありませんでした。実行サマリーが
    影響を受けたテーブルとその理由を明示し、重複を除いて 8 件に制限 (「+N more」表記) することで、大規模な
    実行がローテーションログを溢れさせないようにしました。
  - **「connector X failed」** は原因を示していませんでした。他のコネクタの状態 (パイプラインのどちら側が
    壊れたか)、DLQ の深さ、テーブルごとのエラー数、そしてスタックトレースを見るための CloudWatch
    ロググループへのポインタを含めます。ポーリングがまだ診断情報を集めていない場合は穏やかに縮退し、
    実際に読み取っていない DLQ の深さを報告することはありません。

### Tests

- 変異 6 件を検出。当初 1 件が通過しており、今はカバーしました: コネクタの遷移が `detail=None` を渡す変異 —
  他のすべてのテストが詳細生成関数を直接呼んでいたため **配線** が未検証でした。先に復元セッションの
  テーブル選択バグを出荷したのと同じ種類の穴です。

## v0.1.203

### Added

- **Full Load のテーブルで、行が破棄されたテーブルを明示します。** 隔離が発生したテーブルも `DONE` で
  終わるため、Status バッジはクリーンなテーブルと **同一** でした — 唯一の手がかりはテーブル全体の下にある
  黄色いパネル 1 つで、それは *どの行* かを示さず、スクロールすると見えなくなります(該当行が別のページに
  ある場合すらあります)。2 つのマーカーを追加し、どちらも amber 色で、ホバー時に自ら説明します:
  - 行の `Done` バッジの隣に置く輪郭線の **「N dropped」** バッジ — ツールチップが何が起きたか、残りの行は
    正常に投入されたこと(失敗ではなく `DONE`)、ギャップを閉じる方法(ソース値を修正し当該テーブルを
    Reload)を説明します;
  - テーブル上部のステータス要約に置く **「Dropped: N rows」** チップ — スクロールする前に `Done: 8` と
    同じ視野で損失が見えるように。報告された実行が完璧に見えた、まさにその要約です。

  どちらも実際に破棄があった場合にのみ描画され、クリーンな実行では変化ありません。

### Tests

- 要約チップ(有無・複数形)、行ごとのツールチップ、そして Quasar スロットテンプレートの契約検査を網羅
  しました — テンプレートが読むすべての `props.row.*` キーは行マッピングが提供しなければなりません。最後の
  項目が重要な理由: スロットのキーが誤っていると **実行時には空白で描画されるのにスイートは通過** します。
  キー名を変更する変異が検出されるようになりました。変異 4 件を検出。

## v0.1.202

### Added

- **Validation がターゲットの不足分を、移行が破棄した行に帰属させます。** 行が隔離されたテーブル
  (DSQL が保存できない値) はターゲットが不足するため、Validation は単に `MISMATCH` /「investigate」と
  報告し、マニュアルは運用者に *「不足分を Full Load エラーログ / CDC DLQ と照合せよ」* と案内していました —
  ツールが既に持っていた情報なのに。Validation には隔離に関する知識が **一切** ありませんでした。今は:
  - 不足分が破棄された行数と **正確に** 一致する場合、*「Fully explained: N rows were permanently
    dropped during the migration … this deficit is expected, not new data loss」* と表示されます;
  - 不足分が **より多い** 場合は *「Partly explained: … but N more are missing and are NOT accounted
    for」* と表示され、調査が必要な箇所を正確に指し示します。正確な一致を要求することが安全装置です:
    4 行不足で 1 行破棄なら 3 行が未解明であり、それを「想定内」と呼ぶことこそ、本物の損失がこの検査を
    すり抜ける経路です。
  - 判定は意図的に依然として **失敗** です。それらの行は実際に存在しないため、この帰属はギャップを解明する
    ものであって免除するものではありません — 隔離がテーブルを `matched` に反転させ、失われたデータの上で
    カットオーバーを解放することはありません。
  - アプリ再起動後はテーブルごとの件数が失われるため (永続化されない)、不足分を推測せず未解明として
    報告します。

  件数は Full Load ジョブの chunk (`quarantined_rows_by_table`) から流れ、完成したレポートに一度だけ
  付与されます。これによりソースとターゲットの比較は 2 つのデータベースのみの純粋関数として保たれます。

### Docs

- マニュアル §4.5 に、Full Load が隔離した行への以後の変更を CDC がどう扱うかを記載しました: `DELETE` は
  0 行に一致してサイレントに適用され (正常 — 意図した最終状態が既に成立しており、エラー扱いは冪等性を
  壊す)、値を 1 MiB 未満に縮める `UPDATE` は sink の upsert によって **ギャップを修復** し、依然として
  超過する `UPDATE` は DLQ に再隔離されます。2 つの帰結も明記: ギャップは自ら知らせない (報告するのは
  Validation) こと、0 行の delete は通常の再生と区別できないこと (冪等性のための意図的なトレードオフ)。
- マニュアル §5 の手作業による照合の指示を、新しい帰属の説明に置き換えました (en/ko/ja)。

## v0.1.201

### Fixed

- **行を永久に破棄した Full Load が、依然として「全てのソース行を投入した」と報告していました。**
  実際の実行で報告: 「Quarantined rows (1) — these rows were permanently dropped」の黄色いボックスの
  すぐ下に「Full Load complete — All 8 tables loaded every source row」の緑のボックスがあり、当該
  テーブル自体は `12 / 15` と表示されていました。原因は 2 つで、いずれも修正しました:
  - テーブル単位の `complete` 判定が投入数とソース推定値の比較のみで破棄を認識できず、推定値の 20%
    サンプリング許容差(`information_schema` の値はサンプリング由来で双方向にずれるため存在)が 15 行
    テーブルの 3 行不足を静かに吸収していました。隔離された行は推定ノイズではなく**確定した**損失なので、
    基準値の比較より**先に**判定を失敗させます — 推定値が全く無い場合でも検出されます。
  - その件数が判定まで到達していませんでした: `ChunkState`/`FullLoadTableRow` に隔離フィールドが無く、
    実行単位のサマリーが**構造的に**認識できませんでした。エンジンは既に破棄をエラーログに記録し不完全な
    投入として扱っていたので、chunk にも件数を記録し、完全性サマリーがそれを読むようにしました。
- **破棄された行が、想定内の推定値のずれとして報告され得ました。** 基準値が近似の場合、件数の差は
  (正当に)「counts differ from the pre-load estimate … This is expected」という穏やかな情報通知として
  表示されます。隔離された行はそこに属し得ないため(サンプリング推定値では書き込めなかった行を説明できない)、
  常に「Full Load finished with issues」として表示され、テーブル名と件数が明示され、別個の row-count
  mismatch として二重報告されません。
- **対処の案内が、該当しないコントロールを指さなくなりました。** 隔離が発生したテーブルは `DONE` で終わる
  ためリトライ対象ではないのに、バナーは失敗が無くても「Retry the failed tables」と案内していました。今は
  ソース値を修正してそのテーブルを Reload する(またはギャップを受け入れる)よう案内し、実際に失敗した
  テーブルがある場合にのみリトライに言及します。

## v0.1.200

### Fixed

- **Prerequisites のガードメッセージが右寄せで表示されていました。** 事前チェック後にテーブルを追加すると
  「Re-run the prerequisite checks — … was added to the selection after the checks ran…」が右端に
  張り付いて表示されていました。この行は本来 primary の「Continue」ボタンだけを収めるため
  `justify-end` になっており(デザインシステム上、primary アクションは右)、そのボタンを**置き換える**
  ガード文がその配置を継承していたのです。ボタンがある場合にのみ右寄せし、メッセージは左寄せするように
  なりました — 説明対象の内容の横で文章として読めます。Data Migration・Schema Conversion・Validation の
  他の `justify-end` 行も確認しましたが、いずれもボタンのみを収めており同じ欠陥はありませんでした。

## v0.1.199

### Fixed

- **復元されたセッションでは、依然として全てのターゲットテーブルが事前チェックされていました。**
  v0.1.198 は既定値を `generated_node_ids` に紐づけていましたが、このフィールドは「Generate DDL for
  selected」を押さないと設定されません — そのため、それを押さずに Apply したセッション(またはその後
  Clear を押したセッション)はこのフィールドが空のまま復元され、そのまま「ターゲット上の全テーブル」
  フォールバックに落ちて全てを再選択していました。「Start over」で直ったように見えたのは、新しい
  セッションが generated ids を再構築するからにすぎません。既定値は Schema Conversion 自身の apply と
  同じ方法(`_selected_apply_names`)で手順 2 の範囲を解決するようになりました: コミット済みの
  generated ids があればそれ、**無ければ ticked ids** — どちらも永続化されるため再起動に耐えます。
  ターゲット既存へのフォールバックは、どちらも不明な場合にのみ使われます。

### Tests

- 欠けていた配線の検証を追加しました: 呼び出し元から `ticked_node_ids` を削除する変異が全テストを通過して
  しまい、これがまさにこのバグが出荷された経路です — 純粋なヘルパーは正しくテストもあったのに、UI は依然と
  して過剰にチェックしていました。新しいテストは画面の `default_migration_selection(...)` 呼び出しを解析し、
  ticked の範囲を渡していなければ失敗します。変異 3 件を検出(ticked フォールバックの削除、generated より
  ticked を優先、呼び出し元の配線解除)。

## v0.1.198

### Fixed

- **「Tables to migrate」が Schema Conversion で選んだ項目ではなく、全テーブルをチェックしていました。**
  既定値が「ターゲットに既に存在するすべてのテーブル」だったため、以前の実行から残っているテーブルを持つ
  ターゲットではそれらを黙って全て再選択し、意図的な手順 2 の選択を破棄していました — 実際のセッションで
  3 つ選んだのに 11 個がチェックされていると報告されました。しかも要求より**多く**移行する方向の既定値で、
  長時間実行される投入では誤った方向です。

  事前チェックは、このセッションの Schema Conversion の選択があればそれになり、このセッションで何も
  生成していない場合(再接続、またはスキーマが外部で適用された場合)にのみターゲット既存の集合へ
  フォールバックします — その場合は手順 2 の選択が本当に不明で、空の既定値では説明もなく 0 件チェックの
  画面になってしまうためです。4 箇所の呼び出し元が `default_migration_selection()` ヘルパーを共有するので、
  互いに食い違うことはありません。
- **ピッカーの説明文が、実際にチェックされたものではなく既定値を説明していました。** 常に
  「Pre-selected: N table(s) already on the target」と表示されていましたが、既定値が Schema Conversion に
  従うようになった時点で事実ではなくなりました。今は実際の事前チェック数を全体に対して示し、その集合の
  出所を明示します(「selected in Schema Conversion」対「already on the target」)。2 つの集合が異なるかで
  判定するため、再接続したユーザーにこのセッションで行っていない手順 2 の選択だと伝えることはありません。

## v0.1.197

### Fixed

- **スキーマ適用中にアプリを再起動すると、そのステップが永久にスピンし続けていました。** 実際の
  セッションで報告: 「Applying converted DDL to the target...」の実行中に UI を再起動し、再接続後も
  スピナーが止まらず Apply コントロールがロックされたままでした。適用は in-process で走り、その job id は
  意図的に永続化されないため、再起動は作業を停止させると同時に**ハンドルも失います** — ステップは
  スナップショットから `IN_PROGRESS` として復元され(スピナーを描画)、状態を確定させるポーリングタイマーは
  job id が無いと即座に return していました。結果、これを解除できるものが何もありませんでした。

  生きた適用ハンドルの無い再接続では、ステップを `FAILED` に調整し(`DONE` ではありません: 完了を証明する
  レポートが無く、実際に完了していないため)、何が起きたかを説明します — 再起動前に作成されたオブジェクトは
  すでにターゲットにあり、「Skip if exists」で再実行すれば既存オブジェクトに触れずに残りを完了できます。
  job id を保持している**本当に進行中**の適用はそのままにします。ステップ 4(Validation)には既にあった
  この調整が、ステップ 2 には無かったのです。

### Changed

- **一括 Apply が、上の Generated DDL 一覧に対する操作として読めるようになりました。** このカードは一覧の
  下にあり、タイトルが文字通り「Apply to target」— 各行の個別ボタンと同じ 3 語 — だったため別機能のように
  見え、説明文が「…in the Generated DDL list above」で 2 度も上を指し示す必要がありました。カードの
  タイトルは「Apply generated DDL to target」になり、本文がライブな件数で範囲を明示し(「Applies the 7
  objects from the Generated DDL list above」)、ボタンも何を適用するかを名指しします
  (「Apply all 7 generated objects to target」— 範囲が曖昧だった「Apply all to target (7)」を置き換え)。
  範囲が 1 オブジェクトのときは個別適用の案内文を省略します — ボタンが既に行うことを指示するだけなので。

### Docs

- `CLAUDE.md`: UI が**表示する**バージョンは `pyproject.toml` ではなくインストール済みパッケージの
  メタデータ(`importlib.metadata`)由来であることを記録しました — editable インストールはコード編集を
  即座に反映しますがバージョンは更新しないため、バージョン更新後は再起動前に **`uv sync`**
  (`uv lock` だけでは不十分)が必要です。これでローカル UI が 6 リリース遅れていました。

## v0.1.196

### Fixed

- **ターゲットの主キー照会が、実際の Aurora DSQL では全テーブルの全カラムを返していました。**
  v0.1.192 で追加した `target_primary_key_columns()` は `pg_index.indkey` 全体を unnest して
  いましたが、実際のキーは先頭の `indnkeyatts` 個のみで、残りはインデックスの非キー stored/included
  カラムです。DSQL ではこれは例外ではありません: すべての primary index がテーブルの残りのカラムを
  ペイロードとして保持しており、11 テーブルのスキーマで `indnatts` が最大 14 に対し `indnkeyatts` は
  全て 1 で、この関数は `information_schema.key_column_usage` と **11 件中 11 件**で不一致でした。

  結果は v0.1.192 の意図と正反対でした: 全カラムのリストが適用済みの複合キーと一致することはないため、
  データがあるターゲットへのキー変更を伴う append は**すべて拒否**され、ありえない「実際の主キー」が
  メッセージに引用される状態でした。unnest を `indnkeyatts` で制限して修正し、同じクラスタで再検証して
  11/11 一致、存在しないテーブルは引き続き `None` を返します。

  ライブクラスタ(`ap-northeast-2`)に対し**読み取り専用**で検証: `unnest … WITH ORDINALITY`、
  `JOIN LATERAL`、`pg_index.indisprimary/indkey/indnkeyatts`、`pg_table_is_visible`、
  `pg_attribute` はいずれも DSQL で動作し、`indkey` が `'2 1'` の実在する 2 キーインデックスが
  カラムを**インデックス順**で返すことを確認しました — 複合キー戦略が依拠する保証そのものです。

### Tests

- `_PkCursor` のダブルが、あらかじめ用意した主キーをそのまま返すのではなく、クエリのキーカラム制限を
  実際に反映するようになりました。つまり statement に `indnkeyatts` が無ければ stored カラムまで返し、
  ライブクラスタの形状を再現します。新規テスト 2 件(ペイロード除外、複合キーの順序維持とペイロード除去)は
  制限を外すと失敗します。以前の fake では、実在する全テーブルで誤っている関数のまま 2394 件すべてが
  通過していました。

## v0.1.195

### Tests

- **Full Load の確認ダイアログを実際に開いて検証します。** v0.1.194 の開示は純粋なヘルパーと
  クロージャの構造的検査でのみ覆われていました — ダイアログが Start ハンドラ内で遅延生成されるためです。
  今は実際に駆動します: NiceGUI の `context.client`(Context インスタンスの読み取り専用プロパティ)と
  ダイアログ直前の `run.io_bound` によるターゲット照会をパッチし、捕捉した Start ハンドラを await して
  レンダーされたテキストとボタンラベルを断言します — テーブル名と「データは失われない」旨を含む通知、
  および再作成が無い場合の従来の「Confirm and start」経路。2 つの変異(通知の削除、ボタン名の未変更)は
  ソーステキストではなくレンダー結果で検出されます。

## v0.1.194

### Fixed

- **Full Load の確認ダイアログが、スキーマを再作成するテーブルを開示するようになりました。**
  v0.1.193 は適用済み PK がソースと異なる場合に空のターゲットを再作成しますが、その判断は確認
  ダイアログの**後**にエンジン内部で行われていました。そのためダイアログは「Confirm and start」と
  しか表示せず、テーブルがドロップ・再作成されることに一切触れていませんでした。失われるものは
  ありませんが(ターゲットは空で、DDL も Schema Conversion で承認済み)、Schema Conversion の外で
  ターゲットテーブルに加えた手動の変更は置き換えられるため、実行前に伝える必要があります。ダイアログは
  該当テーブルを情報通知として列挙し、ボタンを「Recreate and load」と表示します。**データがある**
  テーブルは影響を受けず、従来の Append / Drop & reload の選択(破壊的なラベルと赤いボタン)に従います。

### Added

- `schema_recreate_tables()` — 投入が PK を再作成する空のターゲットを返す純粋なヘルパー。ダイアログと
  エンジンが同じ集合を参照するようにします。

### Tests

- 開示ヘルパー(空のターゲットのキー変更、データ保有テーブルの除外、変換やインベントリが無い場合の沈黙)と、
  その一覧がパラメータとしてダイアログに渡されクロージャに捕捉されることの構造的検査を網羅しました。最後の
  テストが存在する理由: この開示を当初ダイアログのクロージャ内で `conv_state`/`inventory` を読むように
  書いており、そのスコープに存在しない名前だったため、**Start のクリックごとに `NameError`** が発生する
  一方でスイートは全て通過していたはずです(ダイアログを開くテストが無いため)。その欠陥の復元を含む
  4 個の変異が検出されました。

## v0.1.193

### Fixed

- **変更された primary key を、ターゲットの現状の形に append するのではなく、スキーマを作り直して
  反映します。** v0.1.192 は「衝突するものが無い」という理由で空のターゲットへの投入を許可しましたが、
  行の衝突については正しくても本質を外していました: PK の変更は**スキーマ**の変更であり、append では
  既存テーブルにキーを後付けできません。そのため元の単一カラムキーを保持したままの空のターゲットは
  すべての行を受け入れて成功と報告していました — ホットパーティションを避けるために Composite key
  戦略を選んだユーザーが、旧来の方式でキーが張られたテーブルを得て、しかもデータが投入済みのため
  破壊的な再投入しか修正手段がない状態です。誤った形でデータを黙って投入することは、拒否するより
  悪いことです。

  適用済み DDL が異なるキーを要求し、ターゲットが**空**の場合は replace 経路に昇格します: 適用済み
  DDL からターゲットを作り直し(失われるものはありません)、plain `INSERT` で投入するため、選んだキーが
  構造的に保証されます。**データがある**ターゲットは v0.1.192 と同じです — 実際のキーに基づいて判定し、
  不一致なら拒否します(そこでの `DROP` はユーザーが同意していないデータを破壊するため)。拒否メッセージは
  実際に利用可能な手段を示すようになりました: 通常は `Drop & reload`、sink がストリーミング中は
  テーブルの再作成が不可能なため「まず CDC を停止する」と案内します。
- **シャード化された投入が誤ったカラムで skip フィルタをかけていました。** シャーディングは*ソース*の
  PK を基準に決まるため、単一整数 `id` を持つテーブルは*ターゲット*キーが複合 `(leading, id)` でも
  シャード化されます — ところがシャードワーカーは `key_columns` をまったく渡していませんでした。
  importer がソースキーにフォールバックし、`(leading, id)` でキーが張られたターゲットに対して
  `WHERE (id) IN (…)` でフィルタしたため、`id` 単独ではターゲット上で一意ではなく、**別の行に一致して
  書き込まれていないソース行をスキップし得る**状態でした。シャード閾値(既定 100 万行)以上のテーブルのみが
  該当し、これは手作業で検証される可能性が最も低い投入です。シャードワーカーはターゲットキーを渡すように
  なり、キーが同一の場合はソースキーのフォールバックを維持します。

### Tests

- 空のターゲットのスキーマ再作成(旧キーを持つ場合を含む)、CDC 共存時の append とその区別された拒否
  文言、シャードワーカーの 2 つのキー経路を網羅しました。再作成昇格の削除、データ保有ターゲットへの適用
  (破壊的)、稼働中の sink 下での適用、シャードキーの欠落を含む 6 個の変異が検出されました。

## v0.1.192

### Fixed

- **推奨方式である Composite key 戦略を適用したテーブルを、空のターゲットに投入できませんでした。**
  Schema Conversion で「Composite key」(ツール自身がホットパーティション対策として推奨する方式)を
  選んで適用し、最初の Full Load を実行すると、*"configured with a changed primary key … Load it
  fresh (Drop & reload)"* でそのテーブルが失敗しました。ガードは append であれば「ターゲットは
  まだ元のキーを持っているはず」と仮定していましたが、Schema Conversion が複合キーを適用済みで、
  ターゲットは実際にそのキーを持っていました。安全な投入を危険と誤判定して拒否していたのです。

  さらに、エラーが指示する解決策には到達できませんでした。「Drop & reload」の選択肢はすでにデータを
  持つテーブルにしか描画されず、replace 集合もまさにその集合から*導出*されるため — 空のターゲットでは
  選ぶ手段がありませんでした。**Full load + CDC** はより深刻で、この場合 append 経路が強制される
  (DROP は稼働中の sink と競合する)ため、そもそも経路が存在しませんでした。

  Full Load は仮定せず、稼働中のターゲットに対してキーを解決するようになりました: **空のターゲット**
  は適用済みキーで投入し(競合する行が存在せず、ソースキーで一意な行はそれを含む複合キーでも一意)、
  **データがあるターゲット**はカタログから読んだ*実際の* PK と比較し、一致すればそれを使います。
  ターゲットが本当に異なるキーを持つ場合(実際のキーをメッセージに明示)、またはキーがまったく読めない
  場合は依然として拒否します(「不明」を安全とは扱いません)。ターゲットキーがソースキーと同じテーブルは
  影響を受けず、ターゲットへの問い合わせも発生しません。

### Added

- `target_primary_key_columns()` — ターゲットテーブルの実際の PK カラムをキー順で返す読み取り専用の
  カタログ照会(スキーマ名・テーブル名はバインドパラメータとして渡します)。「判定不能」は `None` で、
  呼び出し側はこれを安全ではないものとして扱う必要があります。

### Tests

- append のキー判定の全分岐(空のターゲット、Full load + CDC、一致するデータ保有ターゲット、旧キーを
  持つターゲット、読み取れないキー、そしてターゲットを照会しないことを断言するキー一致経路)と、新しい
  照会のキー順序・非修飾名の解決・インジェクション安全性・不明経路を網羅しました。旧来の一括拒否の復元や
  不明なキーを一致として扱う変異を含む 8 個の変異が、それぞれ検出されました。

## v0.1.191

### Fixed

- **Data Migration のテーブル選択が早すぎるタイミングでロックされ、しかも解除手段が行き止まりでした。**
  前提条件チェックが実行された瞬間にロックされていましたが — チェックは*プレビュー*であって確定では
  ないため、いかなるマイグレーションも始まる前に対象範囲が固定されてしまいました。さらにロックの
  ツールチップは「移行するテーブルを変えるにはチェックを再実行してください」と案内するのに、再実行は
  同じ集合を再び固定するため、Start over 以外に抜け道がありませんでした。選択は、実際に取り消せない
  何かにコミットされるまで編集可能なままとなり、各ロックはその原因と解除方法を説明します:
  - この集合で Full Load が実行済み(解除: Start over);
  - CDC がストリーミング中で、ソースコネクタのテーブル一覧が固定されている(解除: CDC を停止);
  - CDC インフラがデプロイ済み、またはデプロイ中 — 各テーブルの Kafka トピックのパーティションは
    トピック作成時に確定するため、その後に追加されたテーブルは永久に単一パーティションでストリーミング
    されます(解除: CDC インフラを削除)。このロックは MSK 作成が Full Load と重なる ~15-20 分の
    区間を守り、以前は無防備でした。
- **チェック後に追加されたテーブルが、Full Load 全体を静かに失敗させ得ました。** 前提条件レポートは
  それがカバーした選択より長く残ります(何も消去せず、選択も編集可能になったため)。その後に追加された
  テーブルはターゲットスキーマの検査を受けておらず、1 テーブルの失敗がジョブ全体を失敗させます。Run
  ボタンは未検査のテーブル名とともにブロックされ、Prerequisites パネルにも対応する通知が表示され、
  チェックを再実行するまで維持されます。テーブルの削除はギャップとは見なしません — その場合レポートは
  上位集合であり、選択されたままのものはすべて検査済みだからです。

### Tests

- これまでカバレッジが皆無だったテーブル選択ロックにテストを追加しました: すべてのコミット状態での
  純粋な `selection_lock_reason`(レポートのみの場合は編集可能; 実行中/完了した Full Load、稼働中の
  CDC、デプロイ済み/デプロイ中の CDC インフラによるロック; Full-load-only の実行が無関係な CDC
  スタックで凍結されないようスコープ)、原因ごとの理由を載せたレンダー済みロックツールチップ、そして
  非対称な `prereq_scope_gap`(削除は問題なし、追加はブロック)。すべてのテストを変異テストで確認 —
  旧来の早すぎるロックの再導入を含む 9 個の変異が、それぞれ検出されました。

## v0.1.190

### Fixed

- **Edit モードの Copy ボタンが編集前の DDL をコピーしていました。** エディタのヘッダーが
  エディタ構築時点の DDL 文字列を捕捉していたため、修正を入力した後に「Copy Target DDL」が元の
  DDL を返し(肯定的な「コピーしました」トーストとともに)、一方「Apply to target」は編集版を
  送っていました。同じボタン行が互いに食い違っていたのです。`_render_copy_ddl_button` はクリック
  時に読み取るコール可能オブジェクトを受け取るようになり、エディタのヘッダーは編集バッファを読む
  ものを渡します。ブラウザで確認済み: 入力後にコピーすると編集版が得られます。

### Tests

- **コードレビューが変異テストで見つけた 5 つの穴を塞ぎました** — テストがレンダー結果ではなく
  `inspect.getsource(...)` の部分文字列を検査し、`_NotesUi`/`_DdlPaneUi` のダブルが
  `props()`/`classes()`/`on_click` を捨てていたため、回帰してもスイートが緑のままだった挙動です。
  それぞれ変異を再実行して捕捉を確認しました:
  - 変換ノートの色調反転(本物の `LOSS` を落ち着いた sky-blue に、任意の推奨を中立グレーに —
    このシリーズが修正しようとした重大度の反転);
  - 推奨バッジを `negative` に(赤い助言);
  - `dialog.open()` の削除 / レンダーガードの反転 / 拡大ハンドラでの raise(拡大機能全体が無動作);
  - `current` を生成 DDL に戻す(保存した編集が表示から消える一方 Apply は編集版を送る);
  - インラインの `.ddl-pane` 高さルールの削除(比較ペインが CodeMirror の 256px 既定に戻る)。
  `_NotesUi` と `_DdlPaneUi` のダブルはカードとバッジの対応、エディタのクラス、ボタンのクリック、
  ダイアログの開閉を記録するようになり、ソーステキストではなく実際のレンダーを検証します。

## v0.1.189

### Changed

- **公開されている ECR Public の既定値が `0.1.188` を指すようになりました。** 2 つのリージョンの
  ECR(`ap-northeast-2`、`us-east-1`)と ECR Public のいずれも `0.1.188` を保持しており、新規
  デプロイが使用する匿名 pull の経路まで確認済みです。

## v0.1.188

### Fixed

- **Schema Conversion のノートカードが Evaluation の検出結果と同じスタイルになりました。** 各変換
  警告と推奨事項が色指定のない `border` を使っており、Tailwind の既定値であるほぼ黒で描画されて
  いました — このアプリのカードではなく枠線付きの表のセルのように読め、任意の推奨事項に、
  Evaluation が `UNSUPPORTED` の項目に引くよりも濃い線を引いていました。現在は両画面が同じ色付き
  サーフェスと対応する `*-200` のボーダーを使います — 実際のギャップは中立的なグレー、助言は
  落ち着いた sky トーン(Evaluation が使うのと同じ組み合わせ)で、`rounded-md` の角丸と余白も
  同一です。2 つの画面を結び付けるテストを追加したので、一方のスタイルを変えるともう一方が
  取り残されたことが明らかになります。

## v0.1.187

### Changed

- **公開されている ECR Public の既定値が `0.1.186` を指すようになりました。** 新規に clone した
  利用者がイメージをビルドせずに、コードエディタによる DDL 比較と揃えられた Bedrock の既定値で
  デプロイできます。2 つのリージョンの ECR(`ap-northeast-2`、`us-east-1`)と ECR Public のいずれも
  `0.1.186` を保持しています。

## v0.1.186

### Changed

- **拡大した DDL が画面全体を覆うのではなく、内容に合わせたサイズのダイアログとして本文の上に
  開くようになりました。** 最大化は 1440x900 のディスプレイを覆いながら、実際には **最大でも
  約 1060x800** しか必要としないパネルを表示していました(実際のソースで測定した最長行は 144 文字、
  最長の DDL は 29 行)。加えて背後のページが見えなくなることで、比較画面が置かれていた文脈も
  失われていました。現在ダイアログの幅は `min(1100px, 92vw)` で、高さは DDL に応じて
  `min(44rem, 74vh)` まで伸びます — 29 行のオブジェクトは 679px、4 行のものは 160px、いずれも
  切れません。これを可能にしているのがラッパーの `height: auto` です — これがないと CodeMirror は
  256px に固定され、長さに関係なくすべての DDL が同じ高さになります。

## v0.1.185

### Added

- **各 DDL ペインを全画面に拡大できるようになりました。** 比較画面は左右分割なので各ペインは
  ウィンドウの半分しか使えず、実際のソースで測定したところ **18 テーブルのうち 14 件がその幅に
  収まらない行** を持ち、4 件はペインの高さを超えていました。どちらもスクロールはできますが、
  144 文字の `CHECK` 制約を半分の幅の覗き窓から読むことが、まさに運用者がここで確認する代わりに
  DDL をエディタにコピーして持ち出す理由になります。各ペインのコピーボタンの隣にある拡大アイコンが
  その DDL を最大化ダイアログで開きます — 全幅、約 82vh の高さ、同じ方言のハイライト、読み取り
  専用。ペインを高くするのではなく全画面にした理由は、制約が **幅** だからです。オプトインなので、
  すでに収まっているオブジェクトでは既定の 2 ペイン表示は変わりません。Edit モードには意図的に
  設けていません — ダイアログは読み取り専用であり、編集中のエディタの横に置くと、閉じると破棄される
  コピーへの編集を誘発します。

## v0.1.184

### Fixed

- **DDL エディタがどちら側を編集しているのかを示すようになりました。** **Edit** を押すと 2 つの
  ヘッダーバンドが両方消え、名前のないコードボックスだけが残っていました — ソースペインは設計上
  読み取り専用なので、source 対 target が主眼の画面において「今どちらを変更しているのか」は
  当然の疑問でした。現在はエディタが読み取り専用の比較と **同じ `Target — Aurora DSQL` ヘッダー
  バンド** をコピーボタンとともに備え、`Editing` バッジもそのバンド上のタイトルの隣に移動しました。
  表示するのはターゲットのヘッダーのみ(全幅)です — ソースは画面上になく混同する対象ではなく、
  併記するとソースも編集できるという誤解を与えます。ヘッダーは共有ヘルパー 1 つになったため、
  2 つのモードがずれることはありません。
- **エディタが比較ペインと同じ扱いになりました。** 従来は汎用の `SQL` としてハイライトされ折り返しが
  有効だったため、隣のペイン(`PostgreSQL`、折り返しなし)と同じ DDL が異なって読めていました。
  現在はどちらもターゲットの方言を使い、1 つの論理行を 1 行に保ちます。

## v0.1.183

### Changed

- **Schema Conversion の DDL 比較が、両側とも本物のコードエディタになりました。** 従来は 2 つの
  DDL を行単位で揃えて見せる手作りの diff テーブルでしたが、行が長くなると崩れました —
  `break-all` で折り返すためトークンの途中で切れ、`ENUM` のリストが `'cancel` / `led')` と 2 行に
  分割され、1 つの論理行が複数行を占めることで、そのテーブルが本来提供しようとしていた左右の
  整列がかえって崩れていました。現在は各ペインが NiceGUI 内蔵の CodeMirror であり、テーブルには
  なかったものも併せて得られます — **方言ごとの本物の SQL ハイライト**(左が MySQL、右が
  PostgreSQL なので、バッククォート識別子と二重引用符識別子がそれぞれ正しく字句解析される)、
  行番号、コードの折りたたみ、そしてきれいな行としてコピーできるテキスト選択。長い行は 1 行のまま
  保たれ、Markdown のコードフェンスのように水平スクロールします。
  - 手放したのは行単位の対応付けです。各ペインが 1 行目から始まるため、変更された行が対応する行の
    横に物理的に並ぶことはありません。このペインは 1 つのオブジェクトの DDL を保持し、下の変換
    ノートが既に何が変わったか(削除された外部キー、async インデックス、再マップされた型)を明示
    しているため、その対応は行位置から推測するのではなく言葉で述べられます。
  - ペインは `readonly` ではなく `disable` です。NiceGUI の CodeMirror には readonly prop が
    なく、渡しても静かに無視されるため、比較画面が編集可能なまま残っていました — 利用者が入力しても
    次の再描画で消える一方、**Apply to target** は編集前の DDL をそのまま送っていました。編集は
    引き続き **Edit** ボタンの背後の専用モードで行い、Apply はそのバッファを送ります。いずれも
    ブラウザで端から端まで検証しました。
  - 旧ビューの diff エンジン(`diff_ddl_lines`、`DiffRow`、`DiffKind`、セルレンダラーと `DIFF_*`
    デザイントークン)は他の呼び出し元がなく削除しました — 差し引き 139 行の削減。

## v0.1.182

### Changed

- **公開されている ECR Public の既定値が `0.1.181` を指すようになりました。** 新規に clone した
  利用者がイメージをビルドせずに、揃えられた Bedrock の既定値と Sonnet 5 でデプロイできます。
  2 つのリージョンの ECR(`ap-northeast-2`、`us-east-1`)と ECR Public のいずれも `0.1.181` を
  保持しています。

## v0.1.181

### Fixed

- **既定の設定でデプロイすると AI Assist が `AccessDenied` で失敗していました。** CloudFormation
  テンプレートの `BedrockModelId` の既定値は `us.anthropic.claude-sonnet-4-6` でしたが、アプリ
  自身の既定値は `global.anthropic.claude-sonnet-4-6` でした。タスクロールの
  `bedrock:InvokeModel` のスコープはテンプレートの値から **導出** されますが、Connect フォームの
  Model ID を空にするとアプリは *自身の* 既定値で呼び出します — そのため既定のデプロイはポリシーが
  許可していないプロファイルを呼び出し、「Verify AI access」は 2 つの既定値のずれを IAM ポリシーの
  不備のように見せる権限エラーを返していました。2 つがずれ得ないこと、および既定値が
  `AllowedValues` に含まれることをテストで固定しました。
- **AI Assist を有効にすると `BEDROCK_MODEL_ID` がフォームに反映されなくなっていました。**
  prefill が設定 *全体* をまだ手を付けていない `AiAssistConfig()` と比較していたため、Enable
  スイッチを入れるだけで不一致となり、以降のすべての描画で seed が静かにスキップされていました —
  IAM はデプロイ時の値でスコープされているのに、アプリは組み込みの既定値を使う状態になります。現在は
  フィールドごとに判定します。モデル ID は組み込み既定値のままのとき、リージョンは未設定のときのみ
  seed し、利用者が入力した値は決して上書きしません。

### Changed

- **既定モデルが Claude Sonnet 5**(`global.anthropic.claude-sonnet-5`)になり、Opus 5 も併せて
  提供します。実際に検証しました — プロファイルは `ACTIVE` で `ap-northeast-2` から呼び出しに成功し、
  テンプレートが導出する IAM スコープ(`inference-profile/global.anthropic.claude-sonnet-5` と
  `foundation-model/anthropic.claude-sonnet-5`)は実在するモデルに解決されます。
- **提供するのは `global.` の推論プロファイルのみになりました。** `us.` 系は `us-east-1` /
  `us-east-2` / `us-west-2` からのみ解決され、それ以外では失敗しますが、`global.` はその 3
  リージョンでも動作します — `us-east-1`、`us-west-2`、`ap-northeast-2` で確認済みです。選択肢と
  いうより罠であり、そもそもテンプレートの既定値がアプリとずれた原因も geo プレフィックスが 2 つ
  あったことでした。リポジトリのどこにも `us.` のモデル ID は残っておらず、デプロイガイド・
  マニュアル(EN/KO/JA)と README も併せて更新しました。

## v0.1.180

### Fixed

- **同じ画面で 1 つのオブジェクト種別が 2 通りに呼ばれることがなくなりました。** 上部の集計は
  `3 Routines` と表示される一方、下の一覧とチャートはまさに同じオブジェクトを
  **Stored procedures** と **Functions** に分けていました — 存在しない見出しの件数を数えさせて
  いたことになります。MySQL は実際に両方を `information_schema.ROUTINES` にまとめるため、
  インベントリのフィールド名は正確です。アセスメント側が分けるのは DSQL が両者を異なる扱いに
  するためです(`LANGUAGE SQL` の関数は残せるが plpgsql は不可)。現在は集計が一覧の語彙を使い
  ます — `2 Stored procedures · 1 Functions`。サブタイプが本当に不明な場合のみ `Routines` と
  表示し、件数が 0 の種別はタイルを作りません。
- **チャートの軸が生の enum 値を表示していました。** UI のチャートと HTML エクスポートの
  チャートはいずれも棒を `PROCEDURE` / `FUNCTION` と表示し、すぐ下の一覧の見出しは
  `Stored procedures` でした — 同じ不一致が 1 行下にあったわけです。ラベルのマップを UI から
  `core/assessor.py`(`KIND_LABELS`)へ移動し、一覧の見出し・2 つのチャート・集計がすべて
  単一の出所を参照します。UI がコピーではなく同一オブジェクトを保持することもテストで固定
  しました。

## v0.1.179

### Changed

- **公開されている ECR Public の既定値が `0.1.178` を指すようになりました。** これは新規に
  `git clone` した利用者が何もビルドせずにデプロイする対象のイメージですが、11 リリースにわたり
  `0.1.167` に固定されていました — つまり新規デプロイには `0.1.168` 以降の Evaluation の改善が
  一切含まれていませんでした。2 つのリージョンの ECR(`ap-northeast-2`、`us-east-1`)と
  ECR Public のいずれも `0.1.178` を保持しています。

## v0.1.178

### Changed

- **折りたたまれたオブジェクト行が、単一の代表バッジの代わりにカテゴリごとのラベル付きバッジを
  持つようになりました。** 代表バッジは最も重い分類のみを述べ、残りについては沈黙していたため、
  `Unsupported` と表示された行が 6 件の検出結果を隠し、そのうち 4 件は単なる要レビュー、1 件は
  任意の助言というケースがありました — 大部分がブロックされていないのに、オブジェクト全体が
  ブロックされているように見えていました。現在は各行が
  `1 Unsupported · 4 Review needed · 1 Recommended` を色付きバッジで示します(赤、琥珀、そして
  助言がカード内で使う落ち着いた info-blue)。数字だけにせずラベルを保つため、重大度が色のみに
  依存しません — そうでなければモノクロのスクリーンショットや色覚多様性のある読者はチャートの
  凡例を参照する必要があります。先頭のバッジは従来の代表バッジが示していた分類なので、行は
  引き続き重いものから読め、これに置き換わった灰色の要約行はなくなりました。

### Fixed

- **クラスターレベルの項目が `concerns` 導入前のスタイルで描画されていました。**
  `Database / cluster-level` の行(複数のソースデータベース、テーブル数の上限)が **Risk** /
  **Recommendation** の段落をそのまま表示する一方、隣接するすべてのテーブルはラベル付きカードの
  スタイルを使っており、一覧の 1 行だけが別のアプリのように見えていました。原因はスタイルでは
  なくデータでした — インベントリレベルのチェックは `concerns` を設定する集約を経由せずに
  `AssessmentItem` を直接構築し、そのフィールドを空のままにしていました。現在は自身の検出結果を
  concern として持つため、他のテーブルと同じカテゴリバッジ・縦線・Risk/Recommendation パネルを
  備えます。テキストと HTML のエクスポートも同じリストを描画するため、併せて改善されます。
  `AUTO` オブジェクトのみが concern を持たないことを許されるというレポート全体の不変条件も
  テストで固定しました。

## v0.1.177

### Changed

- **折りたたまれたオブジェクト行が、バッジが隠していた検出結果の内訳を示すようになりました。**
  ヘッダーバッジは *代表* となる分類のみを述べ、残りについては何も語りません。実際のソースで
  測定したところ、18 テーブルのうち 16 件が 1 つのバッジの背後に混在した構成(多くは本物の
  ギャップ + `AUTO_INCREMENT` の推奨事項)を持ち、`Unsupported` と表示された行が 6 件の検出結果を
  隠し、そのうち 4 件は単なる要レビュー、1 件は任意の助言というケースもありました。大部分が
  ブロックされていないのに、オブジェクト全体がブロックされているように見えていました。現在は
  各行に `1 Unsupported · 3 Review needed · 1 Recommended` が付きます — すぐ上の種別グループ
  見出しが既に使っている `N Label · M Label` と同じ形式で、助言の項目はカード内のバッジと同じ語
  である `Recommended` として数えます。バッジをそのまま繰り返すだけになる場合(代表分類の単一
  項目、または問題のないオブジェクト)は表示しません。
- **オブジェクト単位の工数バッジが折りたたみ行から削除されました。** このバッジはオブジェクト
  全体を説明するものですが、行は現在その検出結果を要約しており、`SIMPLE` 1 件と `SIGNIFICANT`
  1 件を平均しても有用な数値にはなりません。各検出結果は展開すれば引き続き自身の見積もりを持ち、
  スキーマ全体の分布は一覧の上のサマリーにそのまま残ります。

## v0.1.176

### Changed

- **工数(effort)バッジがすべての箇所で同じニュートラルなアウトラインで描画されるようになりました。**
  従来はサマリー行のみが緑/琥珀/赤のランプでレベルごとに色付けされ、オブジェクト行と検出結果
  カードは同じ値をグレーで描いていました — 1 つの値に 2 つの表現があったことになります。さらに
  このランプは誤ったシグナルです。この画面でランプは *互換性* を意味し(チャート、分類バッジ、
  Risk/Recommendation パネルがすべて使用)、工数は重大度ではなく時間の順序尺度です。色を付けると
  その意味が薄まり、オブジェクト行で衝突します — 琥珀色の `Review needed` の隣に琥珀色の
  `effort: MEDIUM`、赤の `Unsupported` の隣に赤の `effort: SIGNIFICANT` が並んでいました。現在は
  3 つの箇所が 1 つの定数を共有するため、色は互換性専用として残り、再び分かれることはありません。

## v0.1.175

### Changed

- **工数(effort)サマリーがレポートヘッダーからオブジェクト一覧の横へ移動しました。** 従来は分類行
  の直下、つまり分類で分割されるチャートの上にありました — チャートが何も語らないサマリーが、
  チャートの根拠となるサマリーと並んでいたことになります。さらに悪いのは、2 つの行が同じ見た目
  なのに合計が一致しなかった点です(`SIMPLE 1 · MEDIUM 3 · SIGNIFICANT 2` = 6 に対しオブジェクトは
  8 件)。必須の作業がないオブジェクト — すべて `AUTO`、または v0.1.174 以降は推奨事項のみを持つ
  オブジェクト — は工数の見積もりを持たず、どのバケットにも入らないためです。すべてのオブジェクトを
  合算する分類行の隣で読まれると、別の問いであるというよりオブジェクトが欠落しているように見えて
  いました。工数は一覧を扱うための道具なので、現在はその一覧と工数フィルターの横に置かれ、
  「(*n* of *m* objects need work)」と自ら説明します。作業が必要なオブジェクトがない場合は
  まったく表示されません。ヘッダーには分類の件数のみが残り、チャートと文言が正確に一致します。

## v0.1.174

### Changed

- **オブジェクトの検出結果が優先度順に並ぶようになりました — 実際のギャップが先、助言が最後。**
  従来の重大度のみによる並び替えは 2 つを混在させていました。助言である `AUTO_INCREMENT` も
  分類は `MANUAL` であるため、単にルールの宣言順という理由で本物の `MANUAL` ギャップより上に
  置かれ、テーブルを展開した利用者は実際に対処すべき外部キーより先に任意のスループット助言を
  目にしていました。現在はギャップがすべての推奨事項より前に来て、ギャップ同士は重大度順
  (`UNSUPPORTED` → `MANUAL`)を保ちます。リストは上から下へ「今すぐ対処すべきもの」から
  「任意で調整できるもの」へと読めます。副次的な効果も有用です — 行ヘッダーの代表ルールは、
  ギャップを持つオブジェクトでは常にギャップになるため、推奨事項がオブジェクトの見出しのように
  表示されることがなくなります。検出結果が助言 1 件のみのオブジェクトは、引き続きそれを報告
  します。画面・テキストエクスポート・HTML エクスポートは同じリストを描画するため、3 か所が
  同時に並び替わります。

## v0.1.173

### Changed

- **Evaluation が推奨事項と実際の変換ギャップを区別するようになりました。** `Classification` は
  「どれだけ作業が必要か」に答えますが「実際に何かが間違っているか」には答えず、この 2 つが混在して
  いたため助言が欠陥のように見えていました。各検出結果は **note kind** を併せて持つようになりました
  — `LOSS`(移行できなかった、または意味が変わった)または `RECOMMENDATION`(変換は完全かつ正確で、
  無視した場合に失われるのは性能であって正確性ではない)。`AUTO_INCREMENT` がその推奨事項です —
  このキーは問題なく変換され、UUID/ランダムキーやキャッシュ付き identity への変更は挿入スループットを
  得るための選択です。
  - enum は `ConversionNoteKind` で、`core/converter.py` から `core/models.py` へ移動し **両方** の
    アセスメントが共有します。v0.1.151 での導入時は converter 専用であり、まさにそれが理由で
    Schema Conversion だけがこの区別を持ち、Evaluation は `AUTO_INCREMENT` キーをリスクと呼び続けて
    いました — 2 つの画面が同じキーについて 20 リリースにわたり矛盾していたのです。1 つの enum を
    共有することで、この種のずれは一度修正されるだけでなく構造的に起こり得なくなります。
    `core.converter` が再エクスポートするため、既存の import はそのまま動作します。
  - **推奨事項がオブジェクトの推定工数を膨らませなくなりました。** 工数は「これを移行するために自分が
    どれだけ作業する必要があるか」に答えるものであり、任意のスループット助言は移行が要求する作業では
    ありません。外部キーの回避だけが必要なテーブル(`SIMPLE`、2 時間未満)が、`AUTO_INCREMENT` キーも
    持つという理由だけで `MEDIUM`(2〜6 時間)と報告されており、MySQL のテーブルは大半がそのキーを
    持つため、最も一般的なテーブル形状の見積もりが膨らんでいました。実際の 7 テーブルのスキーマで
    測定したところ、2 つが `MEDIUM` から `SIMPLE` に戻りました。検出結果が *すべて* 助言である
    オブジェクトは、工数をまったく持たなくなります。助言を採用した場合のコストは引き続き表示される
    ため("effort if you take it")、判断に必要な情報は保たれます。
  - 助言の検出結果は落ち着いた info-blue で描画されます — `RECOMMENDED` バッジ、`Risk` ではなく
    `Note` のラベル — 画面、テキストエクスポート(`[RECOMMENDED]`)、HTML エクスポート(緑/琥珀/赤の
    重大度ランプの外にある info-blue のセル)のすべてに適用されます。検出結果の既定値は `LOSS` なので
    他のすべてのルールは変更されず、この変更より前に保存されたレポートも以前とまったく同じように
    描画されます。

### Fixed

- **KO マニュアルの壊れたリンクをさらに 3 件修正。** `ko/11-customer-faq.md` が依然として
  `10-conclusion.md` を英語のアンカーで参照しており、v0.1.166 で 16 件を修正した際に漏れていたもの
  です。これで `docs/manual/` 内のすべての章間アンカーが正しく解決します。

## v0.1.172

### Fixed

- **Evaluation が `AUTO_INCREMENT` キーを欠陥として提示しなくなりました。** 従来は琥珀色の
  **Risk** 見出しの下に *"AUTO_INCREMENT column 'id' produces monotonic keys that cause hot
  partitions in Aurora DSQL"* と表示されていましたが、このキーは問題なく変換され正しく動作します
  — 失われるものはなく、クエリの結果が変わることもありません。UUID/ランダムキーやキャッシュ付き
  identity への変更は **挿入スループット** を得るための選択です(DSQL は行を主キー順に格納する
  ため、単調増加キーは書き込みを 1 つのパーティションに集中させます)。現在はテーブルについて
  事実であることから述べ("converts cleanly and works as-is")、変更が任意であることを明示します
  — Schema Conversion が v0.1.151 で既に適用した修正
  (`ConversionNoteKind.RECOMMENDATION`)と同じ方針であり、当時このルールが漏れていたため
  2 つの画面が同じキーについて矛盾したことを述べていました。

## v0.1.171

### Changed

- **互換性チャートがオブジェクト種別を件数の多い順に並べるようになりました。** 従来の
  「問題の割合」順は、`UNSUPPORTED` のトリガー 1 件をテーブル 200 件より上に置くため、短い棒が
  長い棒の上に浮いた状態になっていました — 優先順位としてよりも、チャートが壊れているように
  読めていました。現在は棒が長さ順に下がっていき(`TABLE`、`PROCEDURE`、…)、各棒は引き続き
  自身の赤いセグメントと「*n*% need attention」の説明を持つため、重大度のシグナルは失われて
  いません。HTML エクスポートも同じ集計から作られるため同一の順序になり、2 つの順序がずれない
  ようテストで固定しました。
- **Evaluation の各項目が問題と対処法を明確に分かれた 2 つのブロックとして表示します。** 従来は
  リスクが素の 1 文で、対処法はその下に小さな矢印だけを添えた薄い 1 文だったため、2 つが 1 つの
  段落のように読め、対処法を読み飛ばしやすくなっていました。現在はそれぞれが独自の色付きパネル上に
  グリフとラベルを伴って配置されます — 琥珀色の **Risk**、緑の **Recommendation** で、アプリ全体
  の琥珀=注意 / 緑=解決というトーンに合わせています。テキストと HTML のエクスポートは以前から
  両者をラベル付けしていた(`Risk:` / `Fix:`、およびそれぞれの表の列)ため、今回の変更で画面が
  エクスポートの水準に追いつきました。

## v0.1.170

### Changed

- **Evaluation のチャートが工数(effort)ではなく互換性(classification)で棒を分けるようになり、
  エクスポートされるレポートも画面に追従します。** この棒は分類サマリーと Auto-converted /
  Review needed / Unsupported のバッジが並ぶ一覧のすぐ上にあるのに、棒自体は Simple / Medium /
  Significant actions に分かれていました — 1 つの図に 2 つの語彙が混在しており、「スキーマのうち
  実際に移行できるのはどれくらいか」に答えるには両者を頭の中で変換する必要がありました。現在は
  3 つの分類がその順で積み上がるため、棒は左から右へ「自力で移行できる」から「移行できない」へと
  読め、オブジェクト種別ごとの行は最も詰まっているものから並びます。HTML エクスポートも同じ集計・
  同じラベル・同じ色を使い、見出しを **Compatibility by object kind** に揃え、棒ごとの説明は
  「*n*% need attention」(自動変換されないもの全体)になりました。工数は変更なく、専用のサマリー
  バッジ・フィルター・オブジェクトごとの表示で引き続き報告されます。
- **Evaluation を展開した際、各項目が 1 本のインデント縦線の後ろに枠付きカードとして表示されます。**
  従来は項目が平坦な区切り線だけで分かれていたため、オブジェクトを 2 つ展開するとブロックが混ざり、
  どの項目がどのオブジェクトのものか分かりにくくなっていました。縦線 + カードは Schema Conversion
  のオブジェクトツリーと同じ包含関係の表現です。
- **HTML レポートが各項目に独立した表の行を割り当てます。** 以前の版はリスクの一覧を 1 つのセルに、
  対処法の一覧を別のセルに入れており、依然としてリストの順番を数えて対応付ける必要がありました。
  現在は各項目が自身の rule id・分類・工数を持つ 1 行になり、オブジェクトと種別のセルがそのグループ
  をまたいで結合されます。フィルターはグループ全体をまとめて隠し、件数表示は従来どおりオブジェクト
  数を数えます。

## v0.1.169

### Changed

- **Evaluation が各リスクをそれぞれの対処法とともに項目別に表示するようになりました。** 1 つの
  オブジェクトが互いに独立した複数のルールに該当することはよくあります — 外部キー、
  `AUTO_INCREMENT` キー、大文字小文字を区別しない collation、`ENUM` 列、`ON UPDATE` タイム
  スタンプは、それぞれ別個の判断であり対処法も別々です。従来はすべてのルールの文面が 1 つの
  **Risk** 段落と 1 つの **Recommendation** 段落に連結されていたため、報告すべき内容が最も多い
  ときにこそ読めなくなり、*n* 番目のリスクと *n* 番目の対処法を対応付ける作業が読み手に委ねられて
  いました。現在は該当したルールごとに独立したブロックとして表示され、それぞれの rule id・分類・
  工数を伴います — Evaluation 画面、テキストエクスポート、HTML レポート(2 つの列が整列した
  リストになります)のすべてに適用されます。項目ごとの分類バッジも付くため、1 件が
  `UNSUPPORTED` で残りが `MANUAL` という状況が可視化されます — 行ヘッダーは最も重い分類のみを
  表示するため、これまでは隠れていました。連結された `risk`/`recommendation` 文字列は後方互換と
  フラットなエクスポートのために引き続き設定され、この変更より前に保存されたレポートはそれらを
  レンダリングします。

## v0.1.168

### Changed

- **公開されている ECR Public の既定値が `0.1.167` を指すようになりました。** 新規の
  CloudFormation デプロイが取得するイメージなので、出荷バージョンに追随する必要があります —
  v0.1.166/v0.1.167 の列 `DEFAULT` の保持と DDL 拒否の修正 3 件が含まれており、これらがないと
  新しいデプロイはクラスターが拒否するスキーマを生成する(あるいは適用はされるがすべての
  デフォルト値が失われたスキーマになる)ことに静かになります。

## v0.1.167

### Fixed

- **`ON UPDATE CURRENT_TIMESTAMP` により生成された `CREATE TABLE` が失敗する問題を修正しました。**
  v0.1.166 で列のデフォルト値を出力し始めましたが、SQLAlchemy の MySQL リフレクションは
  `ON UPDATE` 句をデフォルト値の *中に* 折り込みます —
  `datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` は
  `"CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP"` という 1 つの文字列として返されます —
  そのためそのまま出力され、ターゲットが *`syntax error at or near "ON"`* で拒否しました。
  最も一般的な監査列において、デフォルト値の欠落が変換失敗に変わっていたことになります。
  根本原因をソース側で修正しました: 列のデフォルト値は
  **`information_schema.COLUMN_DEFAULT`** から読み取るようになり、`ON UPDATE` の部分は `EXTRA` に
  残って `auto_update_timestamp` がすでに読み取っています(そして assessor がすでに MANUAL として
  報告しています — DSQL には `ON UPDATE` 句もトリガーもありません)。
- **同じリフレクション経路が引き起こしていた欠陥 2 件。** MySQL 8 は *式* のデフォルト値を括弧付きで
  報告するため、`DEFAULT (uuid())` は `"(uuid())"` として到着しリテラルと誤認されていました。また
  `bit(1) DEFAULT b'1'` はリフレクションの正規表現によって **黙って破棄** されていました。
  `information_schema` は両方とも正確に報告します。式かリテラルかの判定は、引用符からの推測ではなく
  MySQL 自身の `DEFAULT_GENERATED` フラグが決定するようになりました — 推測ではリテラル文字列
  `'CURRENT_TIMESTAMP'` と関数呼び出しを区別できませんでした。
- **identity 戦略で `bigint unsigned AUTO_INCREMENT` キーが失敗する問題を修正しました。** 符号なし
  整数キーは範囲を保つため `DECIMAL(20,0)` にマップされますが、DSQL の identity 列は `BIGINT` で
  なければなりません — そのため参照スキーマの 11 テーブルのうち 6 つが *`identity column type must
  be bigint`* で拒否されていました。現在は狭い整数型とともに `DECIMAL` も拡張します。DSQL の
  identity シーケンスはいずれにせよ BIGINT の範囲なので、生成可能な値が失われることはありません。
- **`DATETIME` の `CURRENT_TIMESTAMP` デフォルトを UTC に固定します。** `DATETIME` はタイムゾーン
  なしの `timestamp` にマップされ、ローダーは移行行を意図的に naive UTC に正規化します。裸の
  `CURRENT_TIMESTAMP` デフォルトはセッションの `TimeZone` に従うため、カットオーバー後に
  アプリケーションが書き込んだ行が移行済みの行と数時間ずれる可能性がありました。

### Changed

- **サポートするインベントリの形は 1 つ、ヒューリスティックはなし。** コンバーターは
  `introspector.enrich_columns` が生成する形(引用符なしの値 + `DEFAULT_GENERATED` フラグ)のみを
  読み取ります。すべての MySQL ソースはこの補完を無条件に通過します。引用符ベースのフォール
  バックは推測に委ねるのではなく削除しました。検証スクリプトも補完を行うようになりました —
  それがないとアプリが決して通らないコードパスを試すことになり、実際にそこだけで存在する失敗を
  報告していました。
- **まれなリテラル/ターゲットの不一致には意図的に専用分岐を設けていません。** 整数ターゲットへの
  ビット文字列デフォルト、`bytea` への バイナリデフォルト、MySQL の `0000-00-00` ゼロ日付 —
  実際のスキーマには 1 つもなく、それぞれ誰も遭遇しないケースのためにコードパスとテストを増やし
  ます。一般規則に流すことで、拒否が起きた場合は変換時に静かでない形で明らかになります。
- **マニュアルにデフォルト値の扱いを記載しました**(EN/KO/JA) — 第 2 章の「変換が行うこと」の
  一覧にデフォルト値が欠けており、第 4 章の制約表には DSQL がデフォルト値を *サポートする* 一方
  `ON UPDATE CURRENT_TIMESTAMP` は再現不可であることを記録しました。

### Added

- **`scripts/verify_conversion_on_dsql.py` を配布対象に含めました**(`scripts/*` の無視ルールで
  除外されていました)。`scripts/README.md` に顧客向けの読み取り専用チェックとして記載しました:
  自分のスキーマを変換し、移行の *前に* Aurora DSQL が何を拒否するかを確認できます。合成マトリクス
  は上記のすべてのデフォルト形式を含む 53 ケースに増えました。

## v0.1.166

### Fixed

- **スキーマ変換で列の `DEFAULT` 値が黙って失われる問題を修正しました。** Aurora DSQL は列の
  デフォルト値をサポートしています(`DEFAULT default_expr` は文書化された `CREATE TABLE` の
  文法に含まれ、リテラル・式・`CURRENT_TIMESTAMP`/`now()`・`gen_random_uuid()`・
  `NOT NULL DEFAULT` を実クラスターで確認済み) — コンバーターがそれを出力していなかっただけ
  です: `ColumnDef.default` は introspector が設定していましたが、読む箇所がありませんでした。
  移行済みの行には影響しません(ローダーが明示的な値を書き込むため)が、カットオーバー後に
  **アプリケーション** が書き込む行は違います: MySQL はデフォルト値を持つ `NOT NULL` 列を省略
  した `INSERT` を受け付けますが、ターゲットは同じ `INSERT` を not-null 違反で拒否します。
  参照スキーマでデフォルト値を持つ 22 列は **すべて** `NOT NULL` だったため、例外的な事例では
  ありません。現在はデフォルト値が引き継がれ、単純な受け渡しでは誤りとなる 3 つを変換します:
  `tinyint(1) DEFAULT '1'` は `DEFAULT TRUE` へ(boolean に `DEFAULT 1` は DSQL でエラー)、
  `AUTO_INCREMENT` 列にはデフォルトを付けず(identity 列の DEFAULT は拒否される)、generated
  列にも付けません(値が計算されるため)。本当に翻訳できないデフォルト(MySQL の `UUID()`、
  0/1 以外の `tinyint(1)` デフォルト)は **警告付きで** 破棄し、カットオーバー後にどうなるかを
  明記します — 従来は何も報告されませんでした。
- **Identity 主キーが Aurora DSQL に拒否される DDL を生成する問題を修正しました。** 2 つの
  別個の欠陥で、いずれも `IDENTITY_WITH_CACHE` 戦略で発生し、テストが生成された DDL の
  *テキスト* を検証するため見えていませんでした:
  - `CACHE 100` — DSQL は `CACHE` の明示を要求し、`1` または `>= 65536` のみを受け付けます
    (*"CACHE (100) must be greater than or equal to 65536 or equal to 1"*)。現在は 65536 で、
    これは許容される最小のキャッシュ値であり、この戦略の目的にも合致します。
  - `INT` の identity 列 — DSQL のシーケンスは BIGINT 専用であり(*"datatype integer not
    supported, identity column type must be bigint"*)、MySQL の `int AUTO_INCREMENT` は
    最も一般的な主キーなので、典型的なテーブルが壊れていました。狭い整数の identity 列は
    `BIGINT` に拡張されます(可逆)。
- **Aurora DSQL の精度上限を超える `DECIMAL` が適用できない問題を修正しました。** MySQL は
  `DECIMAL(65,30)` まで許容しますが、DSQL は精度 38・スケール 37 が上限です。現在は仕様を
  クランプし — 範囲が失われるため警告付きで — 精度が縮小された場合は過大なスケールも一緒に
  縮小します(`scale > precision` はそれ自体がエラー)。

### Added

- **`scripts/verify_conversion_on_dsql.py`** — コンバーターの出力を実際の Aurora DSQL
  クラスターに適用し、クラスターが拒否したものを報告します。欠けていたのはまさにこの検証です:
  単体テストは生成された DDL テキストを検証するため(スナップショットが壊れた `CACHE 100` を
  そのまま固定していました)、E2E は上記の形状をどれも含まない手作りのスキーマ 1 つしか実行
  しません。MySQL 方言の *ロングテール* 49 ケース(`SET`、`BIT`、空間型、広い `DECIMAL`、
  引用符付き/空/負のリテラルデフォルト、generated 列など)を網羅し、任意で実際のソース
  スキーマの全テーブルも対象にします — 3 つの欠陥のうち 2 つが非デフォルト戦略でのみ到達
  可能だったため、すべての主キー戦略について実行します。ソースは読み取り専用で、ターゲットでは
  スクラッチスキーマにテーブルを作成して削除します。失敗時は非ゼロで終了するため、リリースの
  ゲートとして使えます。

## v0.1.165

### Fixed

- **公開されている ECR Public イメージ — 新規デプロイが実際に取得するもの — が 130 リリース分
  古いままでした。** `deploy/cloudformation.yaml` は `ContainerImageUri` の既定値を
  `public.ecr.aws/.../mysql-dsql-migrator:<タグ>` にしており、通常のデプロイでイメージビルドを
  不要にしていますが、アプリが `0.1.164` の時点でもそのタグは `0.1.34`(2026-07-02)のままでした。
  テンプレートからデプロイした人は 7 月 2 日のビルドを受け取っていました — Query Converter への
  改名、Settings ダイアログ、CDC セキュリティグループの修正など、それ以降のすべてが欠けた状態で。
  ECR Public への公開はビルドスクリプトに `PUBLIC_IMAGE_URI=…` を渡すオプトインの追加手順であり
  抜け落ちやすく、その結果を検証する仕組みもありませんでした。現在は `0.1.164` を公開し既定値も
  それを指しており、既定値が出荷バージョンと同じ major.minor ラインにあり、パッチ 20 個以内に
  収まっていることを検証するテストを追加しました — 失敗メッセージには再公開コマンドも含まれます。
  既定値が `latest` ではなく固定の数値タグであることも検証するため、「同じテンプレート」の
  再デプロイが静かに別のイメージを使うことはありません。

## v0.1.164

### Changed

- **サイドバーのフッターが単一の「Settings」項目になり、クリックするとタブ付きダイアログが開きます。**
  Performance tuning、Diagnostics、アクティビティログのダウンロードは、これまでインラインの
  展開パネル 2 つとボタンとしてサイドバーに置かれていました — 9 個の入力フィールドが幅約 16rem の
  サイドバー列に押し込められ、1 つのパネルを開くと他が押しのけられていました。3 つはいずれも同種の
  もの(アプリ全体のランタイム設定で、移行フローの一部ではない)なので、1 つの歯車項目の背後に
  まとめました。ダイアログはそれらを **Performance / Diagnostics / Activity log** タブに分類します —
  ここに来る目的は 3 つすべてを読むことではなく、1 つを変更することだからです。本体は一度だけ
  構築されるため入力途中の値は閉じて再度開いても保持され、ダイアログは persistent で明示的な
  閉じるボタンがあるため外側のクリックで失われません。各パネルには上限(`max-height` + スクロール)
  がありますが共通の高さには揃えません — コントロールが 2 つだけのパネルが画面いっぱいの空白を
  生むことはありません。余裕ができた分、文言も「live, app-wide」の注意を 3 回繰り返す代わりに、
  各セクションが実際に何に影響するかを説明するようにしました。

## v0.1.163

### Changed

- **Query Converter の SQL エディタを右下隅のドラッグでリサイズできるようになりました。** 長い文を
  固定サイズのボックス内でスクロールする必要がなくなります。そのため Quasar の `autogrow` を外し
  ました — 入力イベントごとに textarea のインライン高さをコンテンツ高さに書き換えるため、入力した
  瞬間に手動ドラッグが打ち消されていました。両者は共存できません。エディタは従来と同じ高さで開始し、
  `max-height` により非常に大きな貼り付けでも Convert ボタンが画面外に押し出されることはなく、
  入力中もドラッグしたサイズが保持されます(実ブラウザで検証)。リサイズグリップもフィールドの隅まで
  引き出す必要がありました — Quasar がフィールド内容を左右 12px 内側に寄せるため、ブラウザが描く
  グリップが角丸ボーダーの下に隠れ、半分に切れて表示されていました。

## v0.1.162

### Changed

- **「Query validation」ツールの名称を「Query Converter」に変更しました。** 旧名称は
  *Validation* を再利用していましたが、これはステップ 4 固有の名称であり、まったく別の作業 —
  **移行済みデータ** を正確な `COUNT(*)`、チェックサム、テーブルごとの PK 再照合で比較する作業 —
  を指します。そのため任意の補助ツールがワークフローのステップの繰り返しのように読めていました。
  さらに、副次的な操作を画面名にしていました: 変換はこの画面が常に実行する唯一の操作であり
  (ターゲットテストには検証済みのターゲット接続が必要で、AI レビューと AI DBA チューニングは
  その上でさらに選択する機能です)、現在はタイトルが中心的な操作を示します。ステップ 2 の
  **Schema Conversion** と対になります — あちらはスキーマ、こちらはクエリです。キャプションは
  引き続き *「Convert & test app queries」* なので、タイトルを狭めても何も隠れません。マニュアルは
  3 言語すべて更新しました。章のファイル名は多数の箇所からリンクされているため変更せず、コードは
  `query_playground` というモジュール名を維持したうえで、2 つの表記が別の画面と誤解されないよう
  当該モジュールの docstring に明記しました。

## v0.1.161

### Fixed

- **バックグラウンドジョブの実行中にツールチップが点滅して読めない問題を修正しました。** Quasar の
  ツールチップは付与先要素の *子* であるため、領域を再描画するとポインタが乗っている要素が破棄され
  ます — Quasar はツールチップを閉じ、再ホバーするまで開きません。サブ秒間隔のポーリング 2 箇所が
  毎ティック無条件に全体を再描画しており、ツールチップが毎秒 2〜3 回再生成されていました:
  - **Validation の「Cancel validation」**(0.5 秒ポーリング)。実行中に実際に変化するのは進捗
    ラベル、進捗バー、キャンセル/停止状態の 3 点だけなので、パネルは一度だけ構築し、ポーリングは
    その 3 点をその場で更新(`set_text` / `set_enabled` / `set_value`)して自身のタイマーを再設定
    します — Connect ステップが Next ボタンを制御する方式と同じです。終了状態になれば画面全体が
    結果ビューに変わるため、そのときは従来どおり再描画します。
  - **Query playground の「Test on target」**(0.4 秒ポーリング)。プロービング分岐ではティック間に
    変化するものがないため(スピナーと固定テキスト)、プローブの完了を待ち、判定を描画する必要が
    生じた時点でちょうど 1 回だけ再描画します。
  `render_notice` がヘッダー/本文のラベルを返すようにし、ポーリング領域が文言をその場で差し替え
  られるようにしました。静的な通知を描画する既存の呼び出し側には影響ありません。

### Notes

- より遅いポーリングには同じパターンが残っています — Full Load の進捗(1.5 秒)と CDC モニタリング
  (5 秒)。それらの間隔では影響がはるかに小さいため、別の変更として残しています。

## v0.1.160

### Fixed

- **ソース変更チェックが RDS MySQL で機能するようになりました — 従来は常に「unavailable」でした。**
  drift を GTID のみで判定していましたが、RDS MySQL 8.0 では GTID を有効化できません — そのため
  本ツールの主要サポートソースでは毎回 *「could not be determined (GTID unavailable)」* と表示され、
  このセクションは自らの問いに一度も答えられませんでした。ウォーターマークはすでに binlog
  `file:position` を記録しており(CDC もそこから再開します)、必要な座標を収集しながら使っていない
  状態でした。現在は両側に GTID があれば GTID で、なければ binlog `file:position` で判定し、
  どちらを使ったかをレポートに記録します。比較は順序ではなく同一性を検査します — これによりログ
  ローテーションでも正しく動作し(position は新しいファイルごとにリセットされるため、後のファイルが
  より小さい値を持ち得ます)、座標が後退した場合(ソースの復元、`RESET MASTER`)も正常ではなく
  「変更あり」として扱います。

### Changed

- **セクションが生の事実を並べるのではなく、マイグレーション種別を通して解釈します。** ソースが
  変化し続けるのは CDC が稼働中の正常な定常状態ですが、パネルは種別に関係なく「ソースがスナップ
  ショット以降に前進した」と表示し、問題のように読めていました。現在は CDC がある場合は `info`
  (「正常 — CDC が複製中です。最終確認の前にラグをゼロまでドレインしてください」)、CDC がない場合は
  `warning` として、それらの行がターゲットに **存在せず** 今カットオーバーすると失われることを明確に
  伝えます。変更がなければ `success`、判定不能なら警告せず `info` です。
- **見出しと詳細表示を平易にしました。** 「Drift since snapshot」は 2 語とも専門用語でした —
  「drift」はレプリケーション用語、「snapshot」はウォーターマークの内部名称です — そのため
  セクション名を **「Source changes since the comparison」** に変更しました。生の座標は折りたたんだ
  「Technical detail」ブロックに移し(その値から「どれだけ遅れているか」は読み取れません — GTID は
  距離ではありません)、実際に判定を生んだ座標を先頭に示し、GTID が無効な場合はその *理由* まで
  明記します — 「unavailable」2 行を最上部に置いて実際の根拠を埋もれさせていた従来の表示を改めました。

## v0.1.159

### Changed

- **Validation の「Objects to validate」ショートカットが他のオブジェクト選択画面と揃いました。**
  Schema Conversion と Data Migration は「Select all」/「Unselect all」を同じ扱い —
  肯定的な操作は primary + `done_all`、解除する操作は grey + `remove_done` — で描画して
  いますが、Validation の「Include all」/「Exclude all」には色もアイコンもなく、それらの画面と
  並べると別のアプリのように見えていました。現在は共通の規約に従い、3 つの画面が再びずれない
  よう他画面のソースと突き合わせて検証するテストを追加しました。有効化の条件は変更していません —
  実行中は両方とも無効で、各ボタンは実際に何かを変更できるときにのみ有効になります。

## v0.1.158

### Changed

- **「Cancel validation」が何を待っているかを表示するようになりました。** キャンセルは協調的
  (cooperative)で、ポーリング箇所は 2 つだけです — 各テーブルの開始前と、PK 再照合中の数千行ごと。
  そのため大きなテーブルで既に実行中の `COUNT(*)` やチェックサムには中断点がなく、完了まで実行され
  (数分)、並行して比較中の他のテーブルも同様です。画面には「Stopping…」だけが表示され、その隣には
  「Comparison in progress — safe to leave running」パネルがそのまま残っていたため、実際には
  正常に終了処理中のキャンセルが、無視されたクリックのように見えていました。現在ラベルは
  *「Stopping… waiting for the in-flight table comparisons to finish.」* と表示され、パネルは
  終了処理の説明(何がスキップされるか、実行中のクエリを中断できない理由、部分レポートは生成され
  ないこと)に切り替わり、ボタンは「Stopping…」に改名せず本来の名前を保ちます — 改名すると
  ステータスラベルと重複し、どの操作を要求したのかを示す表示が失われるためです。進捗バーは
  キャンセル中は非表示にします。テーブルの *完了* 数を追跡するため、「Cancelling」表示と矛盾して
  進み続けてしまうからです。動作自体は変更していません — 新しい停止メカニズムではなく正直な
  フィードバックであり、検証は一貫して読み取り専用です。

## v0.1.157

### Fixed

- **CDC start を押した瞬間に — まだデータが流れる前に — Data Migration が「Success」と表示される
  問題を修正しました。** このステップ(およびステッパーヘッダーと画面内ステータスチップのバッジ)は
  CDC がライブになると Done に昇格し、Validation も同時に解放されます。しかしその昇格判定を、CDC の
  *入力* をロックするのと同じシグナルに紐づけていました — そのシグナルは意図的に Start を押した
  瞬間に発火し(開始位置・テーブルセットを編集できなくするため)、コネクタが MSK Connect 上で
  起動している間(約 10〜20 分、まだ 1 行もターゲットに到達していない)も点灯したままです。そのため
  start の途中でヘッダーが Success と表示されていました。昇格は別のより狭いシグナル — コネクタが
  実際に検出された、または cdc-stack のフェーズが `running` — を使うようになり、「Success」は
  データが実際に流れていることを意味します。入力ロックのラッチは変更なし(従来どおり Start 時点で
  発火)、Full Load が完了すれば以前どおりステップは Done になります。

## v0.1.156

### Fixed

- **CDC テンプレート内のアポストロフィ 1 文字が原因で CDC デプロイが毎回失敗し、失敗した
  スタックは手動クリーンアップが必要な状態で残っていました。** `ConnectorSecurityGroup` の
  インライン HTTPS egress ルールの説明で、S3 への到達経路を "via the *customer's* own NAT"
  と記述していました。EC2 はセキュリティグループの **ルール** 説明に `a-zA-Z0-9` と
  `. _-:/()#,@[]+=&;{}!$*` のみを許可し、アポストロフィは含まれません。さらにこの文字集合は、
  同じテンプレートの `Parameters` やリソース説明で許可される自由記述より狭いため、文章としては
  まったく不自然に見えませんでした。その結果(`mysql-dsql-cdc-stack-0729` で確認)
  `ConnectorSecurityGroup CREATE_FAILED - Invalid rule description` でスタックがロールバック
  され、さらにそのロールバック自体が `ROLLBACK_FAILED` になりました — `CustomPlugin` の 2 つが
  まだ `CREATING` 状態で、MSK Connect はその状態のプラグイン削除を拒否するためです。つまり
  1 文字が、単純な再試行ではなく手動でのスタック整理を招きました。該当の文言を修正し、
  テンプレート内のすべてのセキュリティグループルール説明(インラインルールと独立した
  `AWS::EC2::SecurityGroup{Ingress,Egress}` リソースの両方)を EC2 の文字集合と 255 文字制限
  に対して検証するテストを 2 つ追加したので、次の同種のミスはデプロイに到達できません。

## v0.1.155

### Fixed

- **VPC ID を入力する前に「Deploy CDC infrastructure」が準備完了のように見えることがなくなりました。**
  VpcId は本ツールが推論できない唯一のデプロイ入力値です — サブネット/NAT、プラグイン用 S3 バケット、
  DSQL クラスター ARN、ソースホストとその認証情報シークレットはすべてデプロイ時に解決されます — が、
  検証が送信パスにしかありませんでした。そのためボタンは有効に見え、クリックすると確認ダイアログが開いて
  VPC ネットワーク診断とコスト見積もりまで実行し、最後に Deploy を押した後でようやく
  *「Enter your VPC ID.」* のトーストが表示されていました。現在はフィールドが入力されるまでボタンは
  無効で、不足しているものを 1 行のヒントで示し、ID を入力するとすぐに有効になります。
  フォームを再描画するのではなくボタンの状態をその場で更新するため、入力中のフィールドがカーソルの
  下で作り直されることはなく、最初の Deploy クリックが飲み込まれることもありません — ID を入力した
  後のユーザーの次の操作は Deploy のクリックであり、まだ無効なボタンを押してもそのクリックは静かに
  失われるからです。空白だけが残ったフィールドも送信パスの検証とまったく同じく空として扱い、
  前提条件チェック未達が依然として優先されるため、ブロック理由は常に 1 つだけ表示されます。

### Changed

- **サイドバーの Connect 項目が実際の接続状態を表示します。** 従来のアイコンは Connect が選択中の
  ビューかどうかだけを反映していたため、アプリの再起動で認証情報が失われたセッション(認証情報は
  決して永続化しません — Property 7)が正常なセッションとまったく同じに見え、何かを実行する前に
  Connect をやり直す必要があることを示す手がかりがありませんでした。現在はアイコンが接続状態を
  表します: ソースとターゲットの両方が検証済みなら緑のリンクと「Connected」、復元された進捗が
  再検証を必要とする場合は琥珀色の切れたリンクと「Reconnect to resume」、新しいセッションが
  まだ接続していないだけなら中立的なグレーのリンクです。赤ではなく琥珀色なのは意図的です —
  データは無傷で認証情報を再入力すれば解決するため、デザインシステムの重大度基準では進行を
  妨げるエラーではなく回復可能な警告であり、同じ状態を説明する既存の琥珀色の再接続バナーや
  ダイアグラムのバッジとも一致します。アイコンはそのバナーと同じシグナルで駆動されるため、
  両者が食い違うことはありません。

## v0.1.154

### Fixed

- **Full load + CDC の実行が完了した後に「Deploy CDC infrastructure」がブロックされる問題を修正
  しました。** v0.1.145 で追加した CDC 前提条件ゲートは CDC モードのレポートを要求していましたが、
  そのレポートはプロセスメモリ内にのみ存在します — 意図的に永続化せず、Full Load の開始時にクリア
  されます。そのため通常のフロー(CDC 前提条件を実行 → ロード完了 → デプロイ)で、たった今実行した
  チェックについて *"Run the CDC prerequisite checks first"* が表示されていました。今後はゲートが、
  ロード開始時に記録される durable なシグナルも受け入れます: Full Load は CDC の上位集合チェックが
  合格して初めて **開始できる** からです。レポートが存在して **失敗** している場合は依然として
  ブロックされ(ライブなシグナル)、Full-load-only の合格では CDC ゲートは免除されず、一度も
  チェックしていないセッションも依然としてブロックされます。CDC ライフサイクルのゲート 2 か所
  (Deploy infrastructure と Start CDC)の両方に適用しました。

## v0.1.153

### Fixed

- **「Stop Full Load」が永久に停止することがあり、その間ほぼ完了しているかのように表示していました。**
  実際に観測: 画面が *"Stopping… finishing the current batch."* のまま進捗せず — ジョブは
  `RUNNING`、ワーカープロセス 4 個は CPU 0% でアイドル、行数も変化なし。遅い終了ではなくデッドロック
  でした: progress drain が消費を止めるとワーカーが IPC キューを埋め、ブロッキングする `queue.put`
  の中で停止し、**その地点ではキャンセルイベントを確認するコードに到達できません** — したがって
  キャンセルは観測されえませんでした。親は timeout なしの `as_completed(futures)` で待機していました。
  3 つの修正で各リンクを断ちました:
  - ワーカーの progress は `put_nowait` で送り、キューが満杯なら破棄します。progress はテレメトリ
    であり — カウンタは次の flush が再累積するデルタで、確定した合計はワーカーの戻り値から得られます —
    メッセージを失っても進捗バーが少し古くなるだけです。ブロッキングは liveness を失いました。
  - クリーンアップの sentinel も非ブロッキングに。`finally` 経路で満杯のキューは、ジョブを片付ける
    はずの teardown 自体を停止させ得ました。
  - 親はキャンセル後、限定された猶予時間の間スライスで待機します。ワーカーが時間内に終了しなければ
    待機をやめてプールを破棄し、未完了のテーブルを再試行可能として記録します(ロードは冪等)。
- **停止メッセージが実際より大げさに表現しなくなりました。**「Stopping… finishing the current batch」は
  ツールが守れない約束のように読めました。今後は実行中のバッチを待っていると伝え、応答しないワーカーは
  猶予時間後に破棄され該当テーブルが再試行可能になることをツールチップで説明します。

## v0.1.152

### Fixed

- **Full Load 進捗テーブルの「Records per page」設定が保持されるようになりました。** ロード実行中、
  テーブルごとの進捗テーブルは約 1.5 秒のポーリングごとに再構築されますが、その再構築で保持されて
  いたのは *ページ* だけで、rows-per-page は 10 にハードコードされていました。そのため値を増やしても
  次のポーリングで元に戻り、設定が効かないように見え、選択値が戻ることでテーブルが自ら更新し続けて
  いるように見えていました。今後はポーリングを跨いで保持されるホルダーが `rowsPerPage` も保持し、
  Quasar の「All」オプション(`0`)にも対応します。テーブルが縮んだ場合もページはクランプされ、空の
  ページに取り残されることはありません。

## v0.1.151

### Fixed

- **顧客提供のサブネットでの CDC teardown が、課金対象の MSK クラスタを取り残さなくなりました。**
  offset-seeder Lambda は S3 への HTTPS PUT で CloudFormation に応答するため、カスタムリソースが
  削除される時点でもコネクタのセキュリティグループが 443 を許可している必要があります。まさにこの
  ために当該ルールをセキュリティグループの **インライン** ルールにしていました(インラインルールは
  Lambda の ENI が SG を参照している間は削除できない)が、それがスタックが自前のネットワークを
  所有する場合に限定されていました。BYO-subnet のデプロイでは SG が *standalone* の
  `ConnectorHttpsEgress` リソースに依存し、CloudFormation はこれをカスタムリソースと **並行して**
  削除します。`mysql-dsql-cdc-stack-0727` で実際に観測: seeder の `Delete` が実行される前に送信
  ルールが消え、応答が 3 回(各 5 分)タイムアウトしてスタックが `DELETE_FAILED` となり、
  **ACTIVE な MSK Serverless クラスタが課金**され続けていました。今後はインラインルールを
  **両方のネットワークモード**で作成し、競合を再発させうる冗長な standalone リソースは削除しました。

## v0.1.150

### Fixed

- **半分削除された CDC スタックが「Attach」の対象として提示されなくなり、静かに消えることも
  なくなりました。** teardown が `DELETE_FAILED` で終わった後、Data Migration ステップは
  **「Attach to &lt;stack&gt; (DELETE_FAILED)」** ボタンを魅力的に表示していました。そのような
  スタックへの attach は機能しません(リソースが一部消えているためストリーミング不可)。さらに
  そのボタンは本当に重要な事実を隠していました — 残存する **Amazon MSK / NAT が課金され続けて
  いる**のに、それを追跡するセッションが無いという点です。今後は検出したスタックを状態で分類します:
  失敗/ロールバック/削除中のスタックは課金リスクを明示して削除の完了を促す **error** 通知として
  表示され、attach ボタンは **ありません**。正常なスタックのみが attach 対象です。
  - 画面横断の teardown バナーが、*スタック*が壊れたままなのに *ジョブ*が終わったからといって
    自ら消えることはなくなりました。`DELETE_FAILED` の結果や、アプリ再起動で失われたジョブ記録は、
    従来はマーカーを消してバナーを完全に隠していました。今後は最後に確認したスタックの状態も参照
    するため、残存する課金対象のインフラは見え続け、対処できます。

- **ビューの source DDL が 1 行に続かず、整形して表示されるようになりました。** MySQL の
  `SHOW CREATE VIEW` は定義全体を 1 行で返し、先頭にサーバーのメタデータ(`ALGORITHM=`、
  `DEFINER=`、`SQL SECURITY`)が付きますが、それをそのまま表示していました — 読めないテキストの
  塊であり、ターゲット側は pretty-print されるため左右の差分で揃うことがありませんでした。今後は
  ソースを sqlglot の MySQL ダイアレクトで再レンダリングし、サーバーメタデータを除去します
  (変換には無関係で、`DEFINER=\`user\`@\`host\`` を往復させると backtick が double quote に
  変わり、無効な MySQL がユーザーに表示されます)。解析できない定義はそのまま表示します — まさに
  原文を見る必要がある場合です。
- **スキーマ適用の実行中はオブジェクトブラウザがロックされます。** 適用ワーカーは開始時に固定の
  オブジェクト一覧を受け取るため、実行中に選択し直しても実際に書き込まれる内容は変わらず、画面が
  ターゲットとずれるだけでした。さらに悪いことに、実行中に「Generate DDL」や「Reset all」を押すと、
  進行中の適用が実行している DDL が差し替えられたり破棄されたりします。今後は適用中、ツリー、
  一括選択ボタン、フィルター、ソースの再取得、Generate、Reset のすべてが説明付きで無効化されます。

### Changed

- **オブジェクトブラウザの左右パネルが同じ位置から始まるようになりました。**「Select all」/
  「Unselect all」を **Source (MySQL)** のヘッダー行(再取得ボタンの隣)へ移し、primary key の凡例を
  ツリーの下へ移動しました。どちらもソースツリーの上にあったためソースツリーが下に押し下げられ、
  ターゲットツリーはフィルターの直後から始まっていたので、左右の比較が明らかにずれて見えていました。

- **「drop & replace」の失敗時に、データベースの危険なヒントを繰り返す代わりに解決方法を示します。**
  ビューが参照しているテーブルを置き換えようとすると、ドライバの生のエラー(`cannot drop table …
  because other objects depend on it … HINT: Use DROP ... CASCADE`)がそのまま表示されていました。
  このヒントはここでは誤った助言です — CASCADE は、本ツールが再作成できないかもしれないビューを
  黙って削除します。適用処理はテーブルを再作成する前に **選択範囲内の** ビューを事前に DROP する
  ため、妨げているビューは単に選択されていなかっただけです(通常は以前の適用で作成されたもの)。
  今後は失敗メッセージが妨げているビューの名前を示し、オブジェクトブラウザでそのビューも選択して
  再実行するよう案内し(そうすれば事前パスが先に DROP し、そのビュー自身の適用単位が再作成します)、
  `DROP ... CASCADE` は避けるよう明示します。依存関係の失敗は OCC リトライも行いません — 依存関係は
  一時的な競合ではなく確定した状態です。

### Changed

- **Schema Conversion が推奨事項と実際の変換上の欠落を分けて表示するようになりました。**
  従来はすべてが **「Conversion warnings」** の下に同じ黄色の `MANUAL` バッジで並び、性能上の
  推奨が欠陥のように見えていました: 維持された `AUTO_INCREMENT` キーは完全に変換され正しく動作し、
  UUID/ランダムキーやキャッシュされた identity への変更は DSQL のパーティショニングに向けた
  *性能* の提案であって、修正すべき問題ではありません。それが「外部キー制約が DDL から削除されま
  した」(実際に何かを失った項目)のすぐ隣に並んでいました。今後、変換ノートは `kind`
  (`LOSS` / `RECOMMENDATION`)を持ち、UI は 2 つのセクションに分けます:
  **Conversion warnings**(引き継げなかった、または意味が変わったもの — MANUAL/UNSUPPORTED の
  重大度を維持)と **Recommendations**(落ち着いた info ブルーの `RECOMMENDED` バッジと、変換は
  完了しているという説明文)。オブジェクトのヘッダーでも別々に数えるため、推奨だけのテーブルが
  「Review needed · 1 warning」と読めてしまうことはありません。
  - ノートの既定値は `LOSS`(これまでのすべてのノートの意味)なので、AUTO_INCREMENT キーの
    ノートのみが `RECOMMENDATION` を明示します。composite key のノートは `LOSS` のままです —
    アプリケーションがキーとするものが実際に変わるためです。
  - AUTO_INCREMENT の文言を「hot partitions を引き起こす」から始めるのではなく、何が起きたか
    (「整数キーは維持され、きれいに変換された」)から始めるよう書き直しました。リスクを失敗の
    ように記述していたためです。
- **Primary key の選択 UI が segmented control から AWS スタイルのタイルになりました。**
  Keep source PK と Composite key は継続的な影響を持つ設計上の決定であるため(composite key は
  すべてのクエリ・結合・upsert を変え、DSQL のキーは作成後に変更できません)、各選択肢が
  トレードオフを説明する Cloudscape「Tiles」カードを持ちます — AWS が結果を伴う選択に用いる
  パターンであり、segmented control はビューの切り替え用です。このスタイルの単一の情報源として
  `ui/design.py` に `radio_tiles` を追加しました。
- **source/target の DDL 差分が AWS Console のコードサーフェス方式になりました。** 従来は変更された
  すべての行を濃い赤/緑で塗りつぶしていましたが、MySQL→DSQL の異種変換はほぼ全行を書き換えるため、
  パネル全体が塗られていました。レビュー面ではなくエラーレポートのように読め、濃い背景が
  monospace のテキストと競合していました。今後コード領域は **ニュートラル**(白い面、控えめな
  ヘッダー)で、変更は狭い **`+` / `−` ステータスガター**とごくわずかな行の陰影(`-50` 系の
  40% アルファ)が伝えます。色が唯一の signal ではなくなったため、モノクロのスクリーンショットでも、
  色覚特性のある読者にも差分が読めます。実際に変わった側だけを示すので、書き換えられた行は 2 つの
  派手なブロックではなく 1 つの before/after のペアになります。関連トークンは単一の情報源として
  `ui/design.py`(`CODE_*` / `DIFF_*`)へ移しました。
- **「Recommendations」の説明が固定テキストからツールチップになりました。**「修正すべき問題では
  なく任意のチューニング提案」という文言は、見出し横のヘルプグリフに移りました — `RECOMMENDED`
  バッジと見出しが既にその意味を伝えており、このブロックはオブジェクトごとに繰り返されるためです。

## v0.1.149

### Fixed

- **Evaluation ステップが、ユーザーが選んでいない移行タイプを表示する問題を修正しました。**
  Migration plan ステップの廃止(v0.1.147)に伴い、ジャーニーヘッダーの移行タイプバナーが
  すべてのステップで描画されるようになりましたが、`migration_type` は full-load-only が
  *既定値* であるため常に値を返します。その結果、選択の機会は 3 ステップ先にあるのに、最初の
  ステップから「Migration type: Full load only」と説明文が表示され、手を付けていない既定値を
  確定した決定のように見せていました。(廃止されたステップでは選択が先だったため起こり得ません
  でした。)今後はセッションがタイプを **明示的に選択したか** を追跡し、バナーはその時点以降のみ
  表示されます — それ以前のステップは進捗ステッパーのみを表示します。
  - すでに選択されているタイルを再度クリックすることも選択として記録されます。タイプには
    既定値があるため「Full load only」のクリックが確定操作そのものですが、従来のセレクタは
    「変更なし」で早期リターンしており、そのユーザーにはバナーが一切表示されませんでした。
    サブステップのリセットは実際の変更時のみ行うので、確定だけの場合は画面は変わりません。
  - このフラグは永続化されるため、すでに選択済みのセッションは再接続後もバナーを維持し、
    未選択のセッションに選択が捏造されることはありません。以前のスナップショットは「未選択」
    として復元され、これが安全な方向です。

## v0.1.148

### Changed

- **実行中のバックグラウンド操作を伝える notice に、アニメーションするスピナーと「In progress」
  バッジが表示されるようになりました。** 画面横断の CDC バナー(「'<stack>' をバックグラウンドで
  削除中(約 15〜45 分)…」)には静的な info アイコンしかなく、15〜45 分かかる teardown が停止して
  いるのか動いているのか区別できない、反応のないメッセージのように見えていました。
  `render_notice` に `busy` フラグを追加し、静的なグリフをトーン色のスピナーに置き換えて、
  ヘッダーの横に **In progress** バッジを固定します。実行中の teardown/stop バナーと、
  Data Migration の Prerequisites サブステップにある CDC インフラのデプロイ notice の両方が
  これを使用します。**失敗した** teardown は静的なままで Retry/Dismiss アクションを維持します —
  そこにスピナーを出すと、まだ作業が進んでいるという誤った印象を与えるためです。

## v0.1.147

### Changed

- **ワークフローが 5 ステップになりました: 「Migration plan」ステップを廃止しました。**
  `Connect → Evaluation → Schema Conversion → Data Migration → Validation → Cut over`。
  このステップは「CDC を含めるか?」という 1 つの問いを、**情報が最も少ない時点で**尋ねていました:
  その答えはその後 3 ステップの間、誰も使わず、(例えば CDC が決して複製できないカスケードする
  外部キーを検出する) Evaluation はまだ実行されてもいませんでした。さらに Data Migration が
  すでに持っている決定を重複させていました: 同じ CDC の選択がそこに 3 方向の移行タイプセレクタと
  して存在し、ロックもされていなかったため、タイプを 2 回決める構造でした。このステップが担って
  いたことはすべて、実際に対処できる場所へ移しました:
  - **移行タイプ**は Data Migration で、互換性レポートで状況を把握した後に選択します。
  - **CDC インフラのデプロイ**は Data Migration の Prerequisites サブステップで提供されます
    (v0.1.146)。ここは依然として Full Load より前なので、約 15〜20 分の MSK 作成が Evaluation
    より前に前倒しされる代わりに、スナップショットと並行して進みます。
  - Connect は今後まっすぐ **Evaluation** へ進み、重複していた「Include CDC?」コントロールは
    削除されました。
- **移行タイプのバナーがすべてのステップに表示されるようになりました。** 廃止されたステップは
  このバナーを抑制しなければならなかった唯一の画面でした(2 択の「Include CDC?」コントロールが
  3 値のバナーと矛盾して見えたため)。これでジャーニーヘッダーがようやくどこでも同一になります。
- **「Start over」が、取り残された CDC インフラをより多くのケースで警告します。** この警告は
  移行タイプが**引き続き** CDC モードを指していることを要求していましたが、これは抜け穴でした:
  タイプは自由に切り替えられるため、MSK をデプロイした後に Full-load-only に戻した人は警告を
  受けられず、課金され続けるクラスタを黙って残せてしまいました。今後は入力済みのインフラ値 —
  または新しいセッションが決して再発見できない既定以外のスタック名 — だけで十分です。

### Fixed

- **読み取れない保存済みセッションがページを壊さなくなりました。** SQLite セッションストアが
  ペイロードをエラー処理なしでパースしており、`SessionSnapshot` と `WorkflowState` はどちらも
  `extra="forbid"` であるため — より新しいビルドが書いた(あるいは以降削除されたフィールドを含む)
  スナップショットはページのビルドで例外となり、復元された進捗を失うだけでなくツール全体を
  使えなくしていました。今後は警告を記録して新規に開始し、S3 ストアと同じ挙動になります。

### Compatibility

- `WorkflowStep.MIGRATION_PLAN` と `WorkflowState.migration_plan` は **後方互換のためだけに
  維持**されます(既存の `data_migration` エイリアスと同様)。フィールドを削除すると、それを含む
  既存のスナップショットはすべて検証に失敗します。リファレンスのセッションストアにある 19 件の
  スナップショットはすべてそのまま読み込めます。
- 廃止されたステップに**留まっていたセッション**は、復元時に Evaluation へリダイレクトされます
  (19 件のうち 8 件が該当)。従来のように静かに Connect 画面へ戻ることはありません。
- プレゼンテーション資料(`docs/tech-talk-*`、`docs/full-load-cdc-slides-*`)はリポジトリから
  削除されました。`docs/` にはユーザーマニュアルと UI スクリーンショットのみが残ります。
- README/デプロイ文書のスクリーンショットは **静的な PNG**(`docs/demo-ui.png`)になり、5 ステップの
  UI で再撮影しました。以前のアニメーション GIF は、廃止された 6 ステップのサイドバーと古い
  バージョンチップがフレームに焼き付いていました。

## v0.1.146

### Added / Changed

- **CDC インフラを Data Migration ステップからデプロイできるようになり、約 15〜20 分の MSK 作成が
  Full Load と並行して進みます。** これまでデプロイフォームは CDC サブステップ内にのみ存在し、
  Full Load + CDC のマイグレーションではその地点はスナップショットが**完了した後**にしか到達
  しません。そのため待ち時間が直列化していました: ロードが終わってから約 15〜20 分のプロビジョニングが
  始まっていたのです。今後は **Prerequisites** サブステップの下部で提供され(依然として Full Load より
  前)、デプロイはバックグラウンドで進むのでスナップショットを今すぐ開始してよいことを明示的に案内します。
  CDC サブステップ内のフォームも維持されます(何もデプロイしていない状態で CDC に到達するセッションは
  引き続き可能です)。
  - マイグレーションタイプのタイルではなく Prerequisites が適切なアンカーです: チェックの実行こそが
    確定したテーブルセットを固定・ロックし、コネクタのテーブル一覧とトピックのパーティション計画は
    どちらもそれを必要とします。
  - このセクションは状況に応じて変化します: 未デプロイ → フォーム、デプロイ中 → ライブ進捗と
    「今すぐ Full Load を開始」、既にデプロイ済み → 短い「準備完了、ここでの作業なし」、別名で発見 →
    2 つ目の MSK クラスタに課金せず attach。アカウント全体の検出が報告するまでは **何も**
    レンダリングしないため、重複クラスタのガードが揃う前に新規デプロイフォームが現れることはありません。
- **前提条件チェックが、実際にカバーしたテーブルセットを記録するようになりました。** レポートが
  存在した時点でピッカーはロックされるため、そのセットがマイグレーション範囲そのものです。しかし
  ユーザーがピッカーを触らなかった場合は既定値によって暗示されるだけで、保存された選択は空でした。
  すると、それを読むすべての箇所が「テーブルなし」と解釈していました: Full Load の watermark が
  存在する前に開始した CDC デプロイでは、コネクタのテーブル一覧が空になり、トピックのパーティション
  計画が一律になっていました。
- **進行中の CDC インフラデプロイが、すべての画面から見えるようになりました。** これはユーザーが
  席を離れてよい唯一の CDC 操作ですが、画面横断バナーは stop/delete のみを対象としていました —
  そのため Data Migration を離れると、まだ実行中かどうかを知る手段がなく、ユーザーがそれを待って
  しまう可能性がありました。今後はバナーが実行中のデプロイも報告し、Full Load がブロックされない
  ことを改めて伝えます。
- **Evaluation の CDC 固有の外部キー指摘を、CDC を選択する場所で表示します。** アセスメントは
  自動の `ON DELETE`/`ON UPDATE` アクションを持つ外部キーを既に検出しています: MySQL はこれを
  InnoDB 内部で子行に適用するためバイナリログには残らず、CDC は認識できず、DSQL(外部キーなし)は
  再実行もできません — 子行がターゲットに黙って残されます。この指摘の推奨は「CDC を開始する前に」で
  始まるにもかかわらず、ユーザーが CDC を使うかどうかを知る**前**に読む Evaluation レポートにしか
  現れませんでした。今後は CDC マイグレーションタイプを選択すると、影響を受けるテーブルをその場で
  提示します。

### Fixed

- **CDC インフラデプロイの進捗見積もりが、実際より短く表示されなくなりました。**
  `ensure_bucket` と `upload_plugins` の各ステージは約 43 MiB のコネクタプラグインをアップロード
  するのに見積もりが無く、デプロイ中のユーザーにとって唯一の手がかりである合計 ETA が、コールド
  スタート時に約 1 分ほど短く表示されていました。

## v0.1.145

### Fixed

- **Full Load のみを実行した後に CDC を追加する際、CDC 前提条件チェックが省略される問題を
  修正しました。** 前提条件レポートは意図的に永続化していないため、一度ロードを実行した後は
  レポートが無くても実行ガードがそれを免除します — 再接続したユーザーが完了済みの Full Load を
  再実行できるようにするための仕組みです。しかしこの免除が **実際にゲートを通過させたモードに
  限定されていなかった**ため、「まず Full Load のみ、後で CDC を追加」の経路で Full Load の合格が
  **CDC** モードにまで継承されていました: Prerequisites が「完了」として折りたたまれ、
  **バイナリログ形式が一度も検証されないまま** CDC サブステップが開きました。`STATEMENT`/`MIXED`
  形式、あるいは `binlog_row_image=FULL` でないソースは決してストリーミングできないため、この問題は
  課金対象のコネクタ作成から約 26 分後に、診断不能な失敗としてのみ判明していました。今後はゲートを
  通過させたモードを記録(および永続化)し、異なるチェックが必要な切り替えではそのチェックを要求します。
  以前のスナップショットにはこのフィールドが無いため従来の緩やかな動作が維持され、再接続が強制的に
  ブロックされることはありません。
- **サイドバーの Data Migration 実行ガードが画面内のガードと一致するようになりました。** モードを
  渡さずにガードを呼び出していたため暗黙的に Full Load が既定値となり、CDC マイグレーションタイプでは
  サイドバーの Run ボタンが有効に見える一方、画面内のボタンは(CDC の上位集合で正しくゲートされて)
  無効という状態でした。今後は両方が選択されたマイグレーションタイプからモードを導出します。

### Added

- **Deploy CDC infrastructure と Start CDC に独自の前提条件ゲートを追加しました。** この 2 つの
  アクションはこれまで、サブステップの順序(Prerequisites → Full Load → CDC)によってチェックが
  先に実行されることを保証されていました — マイグレーションタイプを早い段階で選ぶからこそ成立する
  暗黙の保証です。今後は両方とも、CDC モードのチェックが実行され **`BINLOG_ROW_FORMAT` が合格**
  していることを明示的に要求し、課金対象のインフラが作成される前に何を修正すべきか(RDS では
  パラメータグループの変更と再起動)を説明します。このゲートは無関係な必須失敗(例: テーブル単位の
  ターゲットスキーマチェック)を意図的に無視します — それは Full Load ガードが既に報告するため、
  1 つの問題を二重に通知しません。
- **Start CDC が、スナップショット時点のバイナリログが既に削除されている場合に警告します。**
  watermark は Full Load の **開始**時に取得されるため、長いロード時間 + 約 15〜20 分のインフラ作成 +
  コネクタ作成のすべてが経過した後に Debezium がその位置を読みます。その間にソースが当該ログを
  削除していた場合、ギャップのない引き継ぎは不可能で、正しい復旧方法は新しいスナップショットのみです。
  今後はコネクタ作成前に読み取り専用の `SHOW BINARY LOGS` を 1 回実行し、失われたログ、まだ保持されて
  いる最も古いログ、保持期間を延ばすコマンドを併せて示します — 約 26 分後に診断不能な
  `CREATE_FAILED`(MySQL エラー 1236)で失敗する代わりに。これは **ブロックではなく警告**であり
  (ギャップを承知の上で開始することは意図的な選択になり得ます)、判断できない場合は静かに通過します —
  watermark が無い、手動開始位置を使用している、当該ステートメント/権限が利用できない場合です。

## v0.1.144

### Fixed

- **CDC インフラのデプロイが「CDC ストリーミング中」に化けなくなりました。** 約 15〜20 分かかる
  インフラデプロイ(`create_stack`: MSK Serverless、ネットワーク、プラグイン、IAM)は
  **コネクタを作成しません** — テンプレートが両コネクタを `HasBootstrapServers` 条件に置き、
  インフラパスはその値を空のままにするためです。つまりデプロイ中は何もストリーミングされません。
  ところが「CDC はストリーミング中か?」の判定が、実行中の **すべての** CDC ライフサイクル
  ジョブをインフラデプロイまで含めて数えていたため、(例えば Migration plan ステップで)
  デプロイを開始してから Data Migration を開くと、パイプラインが稼働しているかのように
  振る舞いました:
  - **Data Migration が `Success` に昇格**し、**1 行もロードされていない状態で Validation が
    解除**されました。この昇格は元に戻らないため、誤った状態が **永続化**され再起動後も残りました。
  - **Start Full Load が無効化**され「CDC がストリーミング中 — まず CDC を停止してください」という
    誤解を招くツールチップが表示され、テーブルピッカーも凍結されました。
  - **「Drop & reload」の再実行が黙って append になりました**: ライブ sink がターゲットに書き込む
    間は DROP が抑止されるため、再ロードが古い行を更新せずスキップしました(「新規 0 件 + 既存 N 件」)。
  - Schema Conversion ステップで **スキーマ適用がブロック**されました。Data Migration(CDC を
    停止できる唯一の場所)はそのステップを前提条件としてロックされているため、行き止まりになります。
  今後は `kind="infra"` のジョブを除外します。**コネクタレベル**の操作(Start / Stop / Delete CDC)、
  または実際にストリーミング中のパイプラインのみを対象とします。検出済みコネクタとスタック phase
  `running` は引き続き優先されるため、CDC ライブ時の安全ガードはすべて変わりません。
- **インフラデプロイ中ずっと前提条件の「Check」ボタンが無効化される問題を修正しました。**
  独立した 2 つ目の経路がありました: パネルが CloudFormation の `*_IN_PROGRESS` すべてを
  実行中のマイグレーション操作として扱っていたため、`CREATE_IN_PROGRESS` の間、約 15〜20 分に
  わたって読み取り専用チェックが使えませんでした — まさにチェックを実行すべきタイミングです。
  `create_stack` を使うのは初回デプロイのみ(Start / Stop CDC は `update_stack` →
  `UPDATE_IN_PROGRESS`)なので、`CREATE_IN_PROGRESS` は「インフラのプロビジョニング中、
  ストリーミングはまだなし」と認識され、チェックはそのまま使えます。`UPDATE_IN_PROGRESS` /
  `DELETE_IN_PROGRESS` は引き続き実行中の操作として数えます。

この 2 つの修正により、約 15〜20 分の MSK 作成が Full Load の後に直列化されるのではなく
**並行**して進みます — スナップショットのロード中にデプロイがバックグラウンドで実行されます。

## v0.1.143

### Fixed

- **ロード後のインデックス作成失敗が、完全にロードされたテーブルを失敗扱いにしなくなりました。**
  セカンダリインデックスは全行を書き込んだ**後**に `CREATE INDEX ASYNC` で作成されますが、その
  エラーが import の外へ伝播していたため、**データが完全にロードされた**テーブルが `FAILED` と
  なっていました。その結果、欠落が何もないテーブルのために **Validation ゲートがブロック**されて
  いました。再実行しても解決しません — 主な原因(DSQL の 24 インデックス上限)は一時的なエラーでは
  ないため、毎回同じ箇所で失敗します。
  - インデックスの失敗を**分離**します:データロードは成功として報告され(`failures=0`、全行が
    存在)、失敗は `BatchedImportResult.index_failures` として別途返されます。
  - **1つ失敗しても残りは作成を続けます。** 各 DDL を独立して試行するため、残りのインデックスが
    作成されます(従来は最初の失敗でループが中断していました)。
  - Full Load の結果に **info** トーンのブロックとして表示します — *"Indexes not created (N) —
    the data loaded completely"*。データの欠落ではないため、失敗一覧や隔離行とは分けています。
    エラーログにはどのインデックスがなぜ失敗したか、そして再実行が不要であることが記録されます。
  - マルチプロセスのロード経路にも適用され、ワーカーモードによって動作が変わりません。

## v0.1.142

### Added

- **Evaluation が Aurora DSQL のテーブルあたりインデックス上限を検査します**
  (`TOO_MANY_INDEXES`)。DSQL はテーブルあたり **24 インデックス**まで許可し(MySQL は 64)、
  **主キーのインデックスがこの上限に含まれます** — 実クラスタで検証しました(PK を持つテーブルで
  24 本目の `CREATE INDEX` が失敗し、そのとき `pg_indexes` が PK を含む 24 行を示しました)。
  したがって移行後のテーブルが持てる **セカンダリインデックスは最大 23 本**で、ソースから
  読み取ったインデックス一覧をこの値と比較します。
  - 計画段階で検出する理由:そうしないと最悪のタイミングで判明します。セカンダリインデックスは
    **ロード後**の `CREATE INDEX ASYNC` で作成されるため、上限超過は **Full Load が全行を
    書き終えた後**に初めて発生します — 数時間のロードが失敗テーブルで終わり、再実行しても
    解決しません(上限は一時的なエラーではありません)。
  - **MANUAL** に分類:未使用・冗長なインデックスは多いため、通常はいくつか削除すれば解決します
    (検出内容が `sys.schema_unused_indexes` を案内)。メッセージには両方の本数、正確なエラー
    コード(`54000`)、そしていつ発生する問題かが含まれます。

- **Evaluation が CDC で複製できない cascade 外部キーを検出します**(`FK_CASCADE_CDC_GAP`)。
  MySQL は `ON DELETE/UPDATE CASCADE`(および `SET NULL`/`SET DEFAULT`)を **InnoDB エンジン
  内部で**実行するため、その結果として変更された子行がバイナリログに記録されません — cascade
  動作がトリガーを起動しないのと同じ理由です。Debezium はバイナリログを読むためこれを認識できず、
  Aurora DSQL は外部キーを持たないため cascade を代行することもできません。結果として子行が
  **エラーも警告もなく**ターゲットに残ります。(MySQL バグ #32506 — コード修正なしで「文書化された
  動作」として終結。本ツール固有ではなく、binlog ベースの CDC ツール全般の制限です。)
  - 参照アクションをイントロスペクション時に取得します(`ForeignKeyDef.on_delete`/`on_update`)。
    ソースのリフレクションが既に返す情報を読むため、追加のソースクエリはありません。
  - **MANUAL** に分類(UNSUPPORTED ではない):テーブル自体は問題なく移行できますが、cascade を
    アプリケーションへ移す必要があります — DSQL には外部キーがないため、いずれ必要な作業です。
    検出内容には具体的なアクション、CDC が取りこぼす理由、そして暫定的な安全網(Validation の
    orphan レコードチェックと、最終比較前のソース書き込み停止)が含まれます。
  - `RESTRICT`/`NO ACTION` は検出**しません**:親の変更を拒否するだけで、記録されない子行の
    書き込みを生じさせないためです。

## v0.1.141

### Fixed

- **CDC ライフサイクル操作が、アクティビティログに「started」だけでなく結果まで記録するように
  なりました。** Deploy infrastructure / Start CDC / Stop CDC / Delete infrastructure は
  それぞれ数分〜数十分かかりますが、送信時点のみ記録され、成功・失敗・所要時間が残りませんでした。
  そのためカットオーバー時に最も重要な問い — *Stop は本当に成功したのか、いつ?* — に監査ログで
  答えられませんでした。コネクタ状態の遷移が唯一の代理指標でしたが、それは UI のポーラーが
  書き込むため、運用者が別画面にいる間に完了した操作はまったく記録されませんでした
  (Start CDC の所要時間をログから復元するには後続のポーリング行から推測する必要がありました)。
  - 各ライフサイクルジョブの本体をラップし、**ジョブスレッドから**結果を記録します(UI の表示
    状態とは無関係):経過時間付きの `success`、経過時間とエラー付きの `failure`、協調的な
    キャンセルは `info`(`run_cdc_*` はキャンセル時に正常復帰するため、例外ではなくジョブ
    ハンドルが「中断」と「完了」を区別します)。
  - 失敗は再スローされるため、JobManager がジョブを `FAILED` とする動作は変わりません。
  - `core` のデプロイヤは変更していません — `core` は意図的にアクティビティログに依存しないため、
    ロギングは UI 層に置きます(Full Load と同じパターン)。
  - 既知の制限:操作中にプロセスが停止した場合、そのジョブには `started` 行のみが残ります
    (再起動時に JobManager が `FAILED` に整合させますが、アクティビティイベントは残しません)。

## v0.1.140

### Fixed

- **失敗したソース読み取りが、再試行の待機中も MySQL 接続を保持し続ける問題を修正。** ソースの
  行ストリームはジェネレータで、自身の `finally` でエンジンを dispose するため、放棄された
  ストリームは close されるか GC されるまで接続を保持します — そして例外を投げたフレームが
  その参照を保持します。そのため v0.1.139 の再試行は、死んだ接続を開いたままフェイルオーバーの
  バックオフ(最大60秒)を待ち切り、その後で読み直すためにもう1本開いていました。16テーブル ×
  8シャードでは、**昇格したばかりの Aurora ライターが最も脆弱な瞬間にソース接続数が2倍**になり、
  `1040 Too many connections` のリスクがありました(その場合テーブルはそのまま失敗します)。
  - `migrate_table` がロード失敗時に自身の作成した行ストリームを閉じるため、例外が抜けるときに
    接続が解放されます。
  - 再試行のバックオフ待機を `except` ブロックの**外**へ移動し、待機前に traceback(および
    失敗した試行のフレームとジェネレータ)が破棄されるようにしました。
  - 実コードパスで検証:待機が始まる**前**に接続が dispose されます。

### Added

- **ソースの `Too many connections` も再試行し、専用の案内を表示します。** MySQL 1040/1203 は
  自己誘発的かつ自然に解消するため(フェイルオーバー時に全リーダーが同時に再接続し、他の
  リーダーが終わればスロットが空きます)transient として分類します。ただし案内は
  フェイルオーバーとは異なります — 待つことが解決策ではないため、
  `FULL_LOAD_TABLE_PARALLELISM`/`FULL_LOAD_READER_SHARDS` とソースの `max_connections` を
  示します。
- **reader shard 数が clamp された場合にそれを通知します。** 同時ソースリーダーは32に制限され
  (`table_parallelism × reader_shards`)、この上限が設定値を下げたときに変更前後の値と理由を
  ログに残します。従来は静かに少ないリーダーでロードするため、設定が無効に見えていました。

## v0.1.139

### Added

- **Full Load がソース Aurora のフェイルオーバーに耐えるようになりました。** ライター昇格
  (パッチ適用、インスタンス置換、AZ イベント)は開いている MySQL 接続をすべて閉じるため、
  数時間かかるロードは必ず遭遇します。従来は進行中のテーブルが単に失敗し、誰かが Re-run を
  押すまで待つだけでした。今後はそのテーブルを**自動的に再読み込み**します(既定3回、15秒 →
  30秒 → 60秒のバックオフ — 昇格したライターへ DNS が切り替わる時間を確保)。
  - 再試行は、死んだ読み取りを最後の主キーから**再開せず、新しい consistent snapshot で
    テーブルを最初から読み直します**。再開すると異なる2つの MySQL スナップショットが1つの
    テーブルに混在し、どの単一時点にも対応しなくなります。gapless な Full Load → CDC の
    引き継ぎは、各テーブルが実行のウォーターマーク時点で一貫していることに依存します。既に
    書き込まれた行は冪等なロードがスキップするため、再試行のコストは再読み込み I/O のみで、
    行が重複することはありません。(リーダーシャーディング使用時は各シャードが自前の
    スナップショットを持つため、該当シャードのみ読み直しでコストがさらに減ります。)
  - **接続レベル**の失敗のみ再試行します(MySQL 2013/2006/2003/2002/2055/1053/1077/1079/1927
    およびソケットタイムアウト)。データ/スキーマエラーは従来どおり即座に失敗します — 再試行
    しても同じ失敗までの遅延が増えるだけです。
  - 単一プロセス経路だけでなく**マルチプロセスのロード経路(大規模時の既定)にも適用**され、
    ワーカーモードによって実行の挙動が変わりません。再試行時にはターゲットが空にされたばかり
    という前提を正しく解除するため、失敗した試行が既に書き込んだ行と再読み込みが衝突しません。
  - バックオフ待機中でもユーザーの **Stop** が即座に反映されます(待機終了後ではなく)。
  - 調整可能:`DSQL_MIGRATOR_FULL_LOAD_SOURCE_RETRY_ATTEMPTS`(1 = 無効、従来の挙動)、
    `DSQL_MIGRATOR_FULL_LOAD_SOURCE_RETRY_BACKOFF_SECONDS`。

### Changed

- **ソース接続の切断が自ら説明するようになりました。** 再試行を使い切った場合、テーブルの
  エラーが `OperationalError: (2013, 'Lost connection to MySQL server during query')` という
  素のドライバ文言だけで表示されることはなくなりました。これは通常 Aurora のフェイルオーバー
  であること、ソースは変更されていないこと(ロードは読み取りのみ)、ロードは冪等で主キー順に
  再開するため再実行が安全であること(不足分のみを埋める)が併記されます。

## v0.1.138

### Fixed

- **完全にロードされた Full Load テーブルが、ソース推定値の過大計上により未完了と報告される
  問題を修正。** テーブルごとの `Progress` と完了判定が、ウォーターマークのスキャン不要な
  `information_schema` 件数を分母/比較対象にしていました。この推定値は InnoDB のインデックス
  サンプリング由来で**両方向に**誤差が出るため、過大計上が起きるとローダーが完全に読み切った
  テーブルが **「91%」と表示され mismatched として集計**され、実際には失われていない行の損失を
  示唆していました。
  - `DONE` のテーブルは定義上**100%**になりました:エクスポートは PK キーセットでテーブルを
    読み切るまでストリーミングするため、完了そのものが完全性の証拠であり、推定値との一致に
    依存しません。
  - `complete` は、不足分が推定値のサンプリング許容範囲を超えない限り、完了したテーブルに
    `True` を返します。よって本当に途中で切れたロードは引き続き検出され、数パーセントの差異は
    検出されなくなります。
  - 推定値より**多く**ロードされた場合(よくある過小計上)は、100% の上限で隠れることなく、
    Rows のツールチップにパーセントとともに正常であることが明示されます。

### Changed

- **Full Load 表でソースの数値が近似値であることを明示。** 列見出しが **Rows (target / source
  est.)** になり、サンプリング誤差、ターゲットがソースを上回るのが正常である理由、完了した
  テーブルが 100% なのは2つの数値が一致したからではなくローダーが読み切ったからである、という
  説明を含む ⓘ ツールチップを追加しました。正確な比較は引き続き Validation(ステップ4)です。

- **CDC ステータス表が正常なテーブルを「target ahead」と誤検出する問題を修正。** この画面の
  Source rows はスキャン不要な `information_schema` の**推定値**です(大規模な本番ソースに
  `COUNT(*)` フルスキャンを行わないため)が、consistency 判定がそこから正確なターゲット
  `COUNT(*)` を引き、その差を異常として扱っていました。InnoDB はこの推定値をインデックス
  サンプリングから求めるため数パーセント*少なく*数えることが多く、まったく正常なターゲットが
  推定値を上回ります — 実際の11テーブル構成で**8テーブルが amber の「target ahead」バッジ**を
  表示し、quarantined は 0、ストリームもすべて caught up でした。
  - `"target ahead"` 判定を**削除**しました。ターゲットが推定ソース件数を上回るのは正常です。
  - 判定は実際に正確で低コストなシグナルに依拠します:DLQ、時間ベースの `ReplicationLagMs`、
    `MAX(pk)` の先端位置。*推定値*に対する不足は、サンプリング許容範囲を超えた場合にのみ
    「rows missing」に昇格するため、実際のデータ損失は報告され、統計ノイズは報告されません。
  - 等値の主張は**正確な**ソース件数の場合のみ許可されます(`counts_comparable`)。`in_sync` は
    ソースが推定値のとき、誤った偽陰性ではなく「判定不能」を返します。

### Changed

- **CDC 表で Source rows が近似値であることを明示。** 列見出しが **Source rows (est.)** になり、
  サンプリング誤差を説明し正確な比較は Validation(ステップ4)へ案内する ⓘ ツールチップを
  追加しました。セルごとに繰り返していた `(est.)` の接尾辞は削除(まれな *exact* の場合のみ
  表示)。Consistency のツールチップと「How to read this table」凡例も、緑は「異常なし」であって
  厳密な一致の証明ではないと明記します。

## v0.1.137

### Added

- **Fast sweep で「行数のみ検証」となったテーブルも、その場で深く再検証できるようになりました。**
  従来はその脚注が*「Fast sweep をオフにして全体を再実行」*と案内するだけでした。今後は
  **Deep-check N count-only table(s)** ボタンが表示され、対象テーブルのみを今回の実行が
  スキップしたチェックサム / レコード reconciliation で再比較し、既存レポートにマージします —
  v0.1.136 が失敗テーブルに導入したのと同じテーブル単位の仕組みです。合格テーブルの中で
  再検証が本当に有用な唯一のケースです(行単位の同一性が証明されていないため)。
  - 無意味になる場合はボタンを出しません:`ROW_COUNT` モードで reconciliation なしの場合は
    実行できる深い検査が存在せず、同じ行数比較を繰り返すだけなので、従来の正直な
    「Fast sweep をオフにして再実行」という案内をそのまま表示します。
  - それ以外の合格テーブルには再検証ボタンは付きません — 検査が増える箇所(失敗テーブル、
    または count-only の fast-sweep テーブル)にのみ表示されます。

## v0.1.136

### Added

- **Validation で全体を再実行せず、個別テーブルだけ再検証できるようになりました。** 行数や
  チェックサムが不一致のテーブルは「Tables needing attention」の各項目に **Re-check** ボタンが
  付きます(失敗が複数なら **Re-check all N tables** も)。該当テーブルのみを再比較し、結果を
  既存レポートに**マージ**するため、他テーブルの判定と全体のカットオーバー可否はそのまま保たれ、
  自動的に更新されます — 最後の失敗テーブルを直せば、数時間かかる全体再実行なしで判定が
  「Ready for cut-over」に変わります。
  - 再検証は**元の実行時のオプション**(比較モード、reconciliation、orphan チェック)をレポート
    自身から復元して使うため、マージ後のレポートが自己矛盾しません。再接続で復元された
    レポートも再検証できます。Fast sweep は再検証では**強制的にオフ** — 既に差異があると
    分かっているテーブルなので、チェックサム/reconciliation こそ実行すべき検査です。
  - 時点が混在することを明示します:**「N table(s) re-checked at &lt;時刻&gt; — newer than the rest
    of this run」**(対象テーブルを列挙)。判定が2つの時点を併せて反映するためで、この表示は
    再接続後も維持されます。
  - 再検証は完了済みステップの上で動作します(ステップは **Done** のまま、レポートも表示され続け)、
    該当行にのみ「Re-checking…」のインライン表示が出ます。validation ジョブ枠を1つだけ共有する
    ため、再検証中は「Re-run validation」が無効化され、その逆も同様です — 全体再実行が再検証
    ジョブを孤立させたり、マージ対象のレポートを消したりすることはありません。
  - 再検証を開始できない場合(例:レポート作成後に短命な DSQL ターゲットトークンが失効)は
    **「Could not re-check those tables」**という独自の通知で知らせ、既存レポートには手を
    付けません — 「Validation failed」として表示されることはありません。

## v0.1.135

### Fixed

- **CDC 後に JSON 列が原因で Validation が「data differs」を誤報する問題を修正。** MySQL `JSON` は
  Postgres `json` 列にマップされ、チェックサムは生テキストを比較していました。MySQL は空白入りの
  正規形(`{"k": "v"}`)、CDC で書かれた行は Debezium のコンパクト形(`{"k":"v"}`)— 論理的には同じ
  でもテキストが異なり、JSON を持つ CDC 書き込み行が失敗していました(Full Load 行は一致)。JSON は
  チェックサムから除外(FLOAT/DOUBLE と同様)し、行数とその他の列は引き続き検証します。
  `customers`/`products`/`suppliers` の誤検出の原因でした。

### Changed(チェックサムのクロスエンジン堅牢化)

- **ソース MySQL セッションを UTC に固定**(`SET time_zone='+00:00'` — 接続テスト/イントロスペクション/
  validation/Full Load ストリームの全ソースエンジン)。MySQL `TIMESTAMP` は UTC 格納だがセッション
  タイムゾーンで読み出されるため、UTC 以外だとターゲットの UTC レンダリングとチェックサムでズレる
  可能性がありました。(`DATETIME` はウォールクロックで影響なし。)
- **Validation がマイグレーション除外列をスキップ**(例:CDC の oversized-LOB 除外):ターゲットに
  書かれていない列はチェックサムから外し、常に「差分あり」にならないようにします(PK は除外しない)。

## v0.1.134

### Changed

- **CDC ストリーミング中に適用できないアクションを、ツールチップ警告だけでなく視覚的に無効化
  (グレーアウト):**
  - **Start / Re-run Full Load** を CDC 実行中は**無効化**(従来は警告のみでクリック可能だった)
    — 実行するとストリームと衝突するため。ツールチップ/ヒントで「先に Stop CDC」を案内。
  - **CDC start point** はロック状態でしたがロックされているように見えなかったため、ラジオ選択と
    手動 GTID/binlog 入力を**明確にグレーアウト(淡色 + not-allowed カーソル)**し、「Locked」
    バッジと一致させました。

## v0.1.133

### Changed

- **Inserts / Updates / Deletes セルを色付きの数値のみ表示** — 先頭のアイコン(＋ / 鉛筆 / −)
  を削除しました(列ヘッダー + 緑/青/赤の色で既に区別可能)。ヘッダーの ⓘ ツールチップも
  1 文に簡潔化しました。

## v0.1.132

### Changed

- **テーブル別 CDC モニターに Inserts / Updates / Deletes 列を個別に分離**(DMS スタイル)し、
  従来の単一「Changes since Full Load」セルを置き換えました。各列は CDC がストリーミングを
  開始して以降の**累積カウンター**で、色分け(緑 insert / 青 update / 赤 delete)されます。

### Fixed

- **I/U/D カウントが表示されたり消えたりするちらつきを修正。** applied-ops 読み取りは
  ベストエフォートのため、1 回のポールが空/失敗(CloudWatch スロットル・タイムアウト、
  テーブルが一時的に空)だと保存値を空マップで上書きして列が空白になっていました。カウントは
  累積(単調増加)なので、空でない読み取りは直近値に**マージ**し、空の読み取りでは**上書きしない**
  ようにして、カウンターが保持され増加のみするようにしました。
- **テーブルヘッダーの ⓘ ツールチップ(Stream lag・Consistency など)がホバー中に閉じる問題を
  修正。** 従来は ~5 秒ごとのポールでテーブル全体が再描画され、ツールチップが消えていました。
  今後はテーブル要素とヘッダーツールチップを**一度だけ**生成し、行データのみを**その場で**入れ替える
  ため、読んでいる間に閉じません。
- **Stream lag / Consistency の説明をより分かりやすく**改善(ヘッダーツールチップと凡例の両方、
  硬いメトリクス定義ではなく平易な表現に)。

## v0.1.131

### Fixed

- **ドレイン済みパイプラインのセッション復元後に Stream lag パネルが消える問題を修正。**
  ライブのラグ推移はメモリ上のローリングバッファで永続化されないため、再接続時は CloudWatch
  `ReplicationLagMs` から再シードします — しかしこのメトリクスはイベント駆動で、ソースを停止
  (caught up)すると直近のデータポイントが無く、チャートは 2 点以上を必要とするため
  **パネル全体が非表示**になり、再接続後にストリームラグの表示が一切出ませんでした。今後は
  CDC が稼働中でも描画する推移が無い場合に **「Caught up — no replication lag in the recent
  window」** の行を表示し、メトリクスが常に存在するようにしました。完全に隠すのは
  ストリーミング開始前のみです。

## v0.1.130

### Changed

- **Validation 画面のテキストを整理。** 5 行のイントロを 1 文に短縮し、3 つのステータス通知
  (No export watermark / CDC still streaming / Comparison in progress)はヘッダーを残しつつ
  本文をそれぞれ要点 1 行に削減しました。これにより、3 つが同時に表示される状態(watermark
  なし + CDC 稼働中 + 実行中)でもテキストの壁になりません。通知ボックス自体は実際の条件付き
  状態を伝えるため残し、簡潔にしただけです。

## v0.1.129

### Changed

- **パイプラインがドレインすると Change flow が「idle」と表示されるように — ソースコネクタの
  ハートビート床を吸収。** ソース(Debezium)コネクタは完全には静かになりません —
  `heartbeat.interval.ms=300000`(5 分)が定期的にハートビートを送出するため、
  `SourceRecordPollRate` は 0 ではなく小さな床(CloudWatch 平均で ~0.03/s)に留まります。
  従来の idle しきい値が `0.01/s` だったため、このハートビート残量でソースを止めた後も
  change-flow が「streaming」と表示されていました。しきい値を `0.1/s` に引き上げ — ハート
  ビート床より上、実際の変更トラフィック(通常 ≥1/s)よりははるかに下。判定は依然として
  **source-poll・sink-send の両方**がしきい値未満であることを要求するため、stall(ソースは
  生成中だがシンクが送出できない)は idle と誤分類されず、正しく「streaming」のままです。

## v0.1.128

### Fixed

- **パイプラインがドレインした後、Stream lag が最後の値で固まる問題を修正。**
  `ReplicationLagMs` はイベント駆動のメトリクス(シンクが変更を適用したときだけデータポイントを
  送出)のため、カットオーバーのためにソースを停止(quiesce)するとパイプラインは送出を止めます。
  ところが reader は 15 分ウィンドウ内に残る最後のデータポイントを「現在の」ラグとして返し続けて
  いたため、source-poll / sink-send のレートはすでに idle(0)に落ちているのに、Stream lag の
  チャート/列が最大 ~15 分間、例: 1068 ms のまま平坦に留まっていました。最新データポイントが
  鮮度カットオフ(~3 分)より古い場合は「無し」として扱うようにしたので、ドレインした
  パイプラインは **caught up** と読み取られ、ソースが静かになった直後にチャートが 0 に落ちます。
  reader 側の修正(シンクの再デプロイ不要)。

### Changed

- **Data Migration / CDC 画面の整理: 常時表示の長い説明をホバー ⓘ ツールチップに移動(冗長な
  ものは削除)。** 常時表示の説明文は画面に慣れるとノイズになるため、ガイダンスはホバーに移し、
  画面をすっきりさせました:
  - **Stream lag** チャートのキャプション → タイトル横の ⓘ(タイトル + `lag (ms)` 軸が基本を
    伝える)。
  - **Tables to migrate** — 「なぜテーブルのみ(ビュー/トリガー/ルーチン除外)」の段落 →
    タイトルの ⓘ;「Locked — 前提条件チェックを再実行…」の行 → ロックアイコンのツールチップに
    統合; 事前選択の説明は `Pre-selected: N table(s) already on the target — untick any to skip.`
    に短縮。
  - **CDC start point** — 「ストリーミング開始地点 / Automatic はギャップレス」の段落 →
    タイトルの ⓘ;「CDC has started — locked…」の行 → **Locked** バッジのツールチップに統合。
  - **Stop CDC** — 「コネクタがストリーミング中… Stop はコネクタのみ削除…」の常時段落を削除
    (ストリーミング状況は live status で、影響は Stop 確認ダイアログに既に明記)、ボタンには
    短い安心用ツールチップのみ。
  - **Change flow** — 「変更がまだ流れているか / カットオーバー時に idle に落ちるのを見る」段落と
    「CloudWatch, ~last few min」の出典表記 → 「Change flow」ヘッダーの ⓘ 一つに統合し、状態行 +
    source/sink レートゲージのみを残す。

## v0.1.127

### Changed

- **テーブル別 CDC モニターが DMS スタイルの変更内訳(I/U/D)を表示するようになりました。**
  「Net rows since Full Load」列を **「Changes since Full Load」** に置き換え —
  テーブルごとに 3 つのライブカウンター: **insert**(緑 `add`)・**update**(青 `edit`)・
  **delete**(赤 `remove`)。これにより UPDATE トラフィックが初めて可視化されます。従来の
  net-rows 値は insert − delete のみを合算し update を無視していたため、update 主体の
  テーブルがアイドルに見えていました。依然としてスキャン不要(`COUNT(*)` なし):DSQL シンクは
  単一の `NetRowsApplied` に代えて CloudWatch メトリクスを 3 つ — `InsertsApplied` /
  `UpdatesApplied` / `DeletesApplied`(名前空間 `MysqlDsqlMigrator/CDC`、ディメンション
  `Stack` + `Table`)— を送出し、コントロールプレーンがウィンドウ単位でそれぞれ合算します。
  net rows は必要に応じて(insert − delete)で引き続き導出できます。再ビルドされたシンク
  プラグインが必要なため(`PLUGIN_VERSION` v21 → v22)、反映するには CDC インフラの
  **Delete + Deploy** が必要です。

## v0.1.126

### Changed

- **CDC ライブステータスの調整(可読性向上・ノイズ削減):**
  - **Change flow** のレートゲージが Pipeline health カードからはみ出す問題を修正(固定幅バー +
    内側パディング)。レート単位を `/s` ではなく **`rec/s`**(秒あたりの変更イベントレコード数 —
    `SourceRecordPollRate` / `SinkRecordSendRate`)と明示。
  - **Connectors** に状態の色付きバッジ(緑の "Running" など)を復活させ、一目で分かるように
    (コンパクトな 1 行レイアウトは維持)。
  - **「CDC behavior & limits」** 参照セクションを **既定で折りたたみ** に — 情報のみで長文のため、
    毎回展開して表示するとノイズになる。
  - **「Runs on … cdc-stack」** のオリエンテーションバナーは **cdc-stack のデプロイ前のみ** 表示
    (デプロイ済み/判定中は非表示) — 毎回の繰り返しや再接続時のちらつきを防止。

## v0.1.125

### Fixed

- **すでに稼働中の CDC パイプラインに再接続したとき、CDC のテーブル別ステータス表示(とライブ
  メトリクス)が空になる問題を修正。** テーブル別の集合(net rows・stream lag・ライブ lag チャートの
  メトリクス読み取り範囲でもある)が **Full Load ジョブのチャンクからのみ** 導出されていたため、
  Full Load ジョブのないセッション(稼働中パイプラインへの再接続、または CDC のみの実行)では、
  パイプラインが実際にストリーミング中でも空のテーブル + lag/チャートなしと表示されていました。
  ジョブがない場合は **ライブスタックの構成から reconcile されたテーブル** にフォールバックします。

## v0.1.124

### Changed

- **Stream lag チャートがライブの in-place 時系列になりました**(従来は 5 秒ポーリングごとに
  丸ごと再描画され、ちらついていました)。チャート要素を保持したまま in-place で更新するため、
  CloudWatch のグラフのように線が途切れず伸びます。**x=時間軸、y=lag(ms)**。データはハイブリッドな
  ローリング時系列 — CloudWatch の 1 分履歴でシード(リロードでも保持)し、以降は約 5 秒ごとの
  ポーリングで現在の最大 lag を追記(追いついたら 0)、約 15 分に上限。Live status 上部の専用
  「Stream lag」パネルに移動。
- **Change flow(source poll / sink send)をビジュアル化** — プレーンテキストの代わりに**同一
  スケールのバーゲージ 2 本**にし、sink が source に追いついているか(バーが揃う)/遅れているかを
  一目で把握できます。
- **Full Load と CDC の統計テーブルのステータスバッジを統一** — どちらも同じ**アウトラインチップ +
  title-case** ラベルに。(Full Load の「Status」バッジはソリッド・大文字でしたが、CDC テーブルの
  アウトラインスタイルおよびデザインシステムの規約に合わせました。)
- **Live status の「Connectors」一覧をミニマルに** — コネクタごとに 1 行(ステータスアイコン +
  役割名 + 淡色の詳細。生のコネクタ id はホバーのツールチップへ)にし、従来の 2 行(id + アウトラインバッジ)
  表記を置き換え。

## v0.1.123

### Added

- **Stream lag の推移折れ線チャート — CDC「Pipeline health」カードに追加。** テーブル別の
  「Stream lag」列は *現在* のラグしか示さず、ストリームが追いついているのか遅れているのか
  (まさにカットオーバー判断の問い)が分かりませんでした。このチャートは sink の
  `ReplicationLagMs` メトリクスから **直近約 15 分の 1 分バケットごとの全テーブル最大ラグ**
  (秒)を描画します — ゼロ付近で平坦なら追いついている(カットオーバー安全)、右肩上がりなら
  パイプラインが遅れています。テーブル別読み取りが既に取得している CloudWatch データポイント
  を再利用し(追加状態なし・リロードでも保持)、アプリ内蔵の ECharts を使います(新規依存なし)。
  解像度は約 1 分(CloudWatch の Period)なので、リアルタイムではなく傾向表示です。

## v0.1.122

### Fixed

- **CloudFormation の削除失敗が、CDC インフラを黙って放置しなくなりました。** in-VPC の
  seeder Lambda の CloudFormation レスポンス PUT が、1 回失敗しても諦めずに **リトライ**
  するようになりました(上限あり、約 4 回)。従来は teardown 中に PUT が 1 回失敗すると
  CloudFormation がレスポンスを受け取れず、自身の約 1 時間のカスタムリソースタイムアウトを
  待ち、結果として cdc-stack 全体が `DELETE_FAILED` に陥って MSK/NAT の課金が残っていました。
  リトライにより、ENI/ルートが落ち着くまでの一時的な S3 ゲートウェイ egress の瞬断を乗り
  切ります。(新規デプロイの CDC インフラに適用。`PLUGIN_VERSION` を v21 に更新。)

### Added

- **teardown バナーが `DELETE_FAILED` から復旧します。** CDC teardown が CloudFormation
  `DELETE_FAILED` で終わると、永続バナーが「進行中」から **「CDC teardown 失敗 — 要対応」**
  (エラー表示)状態に切り替わり、ワンクリックの **Retry cleanup**(スタックしたリソースを
  retain して削除を再実行し、MSK/NAT まで片付ける)と **Dismiss** を提供します。Start over
  でセッションがリセットされた後でもリトライは動作します — 必要な region/デプロイロール/
  プロファイルを durable な teardown マーカーに一緒に保存します。

## v0.1.121

### Fixed

- **CDC インフラの削除(teardown)が、完了するまで画面に表示され続けます。** Start over で
  「Delete all CDC infrastructure」(または「Remove connectors, keep infrastructure」)を
  選ぶと、セッションはすぐにリセットされ Connect 画面に戻り、teardown はバックグラウンドで
  進行します。従来は進行中である表示がまったくなく、MSK/NAT がまだ課金中なのか、インフラが
  すでに削除済みなのか判断できませんでした。今後は **すべての画面(Connect を含む)** の上部に
  永続バナー(「CDC infrastructure teardown in progress…」)が表示され、teardown が完了すると
  自動的に消えます。CDC ステップの Delete/Stop ボタンから開始した teardown も対象なので、
  別のステップに移動しても表示が消えることはありません。
- **Start over が進行中の CDC teardown と競合しなくなりました。** stop/delete の実行中は
  reset がすでにブロックされていましたが、Start over → delete の直後、CloudFormation スタックが
  まだ `DELETE_IN_PROGRESS` に変わる前の短いウィンドウで 2 回目の reset が通り抜け、重複した
  teardown を起こす可能性がありました。セッションのリセットを生き延びる durable な teardown
  マーカーがこのウィンドウを塞ぎます。

### Changed

- CDC インフラのデプロイまたは Start CDC ジョブが実行中のとき、Start over が(黙って進める
  代わりに)**警告** するようになりました。その作業は再検出(re-discover)可能で、ブロックすると
  スタックした実行からの脱出を妨げてしまうため、reset は引き続き許可しつつ、バックグラウンドで
  実行され続けることを伝えます。

## v0.1.120

### Changed

- **CDC Start が、ソースとシンクのコネクタを 1 回の並列パスで作成する**ようになり、コネクタ
  作成の実時間をおよそ半分にします。従来は直列の 2 パスでした — ソースコネクタを作成し、
  RUNNING を待ち(Debezium がテーブルごとのトピックを自動作成)、その後シンクを作成 — トピックが
  存在しないうちにシンクが起動すると empty-partition-assignment レースに陥るためです。cdc-stack の
  start-prep カスタムリソース(汎用化した seeder Lambda)が、毎回の start で**テーブルごとのシンク
  トピックを事前作成**するようになりました — ツールが既に算出する決定的な名前
  `<prefix>.<db>.<table>` とパーティション数で — その結果、両コネクタは互いにではなく事前作成済み
  トピックにのみ依存し、並行してデプロイされます。seeder は引き続き gapless な Full-Load
  ハンドオフ(ウォーターマークあり)のときだけ connect-offsets レコードを seed し、トピックの
  事前作成は無条件なので CDC-only の start でも恩恵を受けます。Start の進捗は source-then-sink の
  6 ステップから単一の "Waiting for connectors (source + sink)" ステップに集約され、コネクタ個別の
  状態はライブチップに残ります。

## v0.1.119

### Fixed

- **シャード分割された単一の大きなテーブルが、FAILED とされず正常にロードされるようになりました。**
  PK レンジのシャードワーカーは結果を `rows_skipped=result.rows_skipped` で構築していましたが、
  `BatchedImportResult` にその属性はありません（`conflicts` があります）。各シャードが return 時に
  `AttributeError` を発生させ、捕捉されて `rows_loaded=0` の `FAILED` として報告 → 大きな単一
  テーブル（エンジンがコアごとに 1 シャードへ分割）が**全行ロード済みでも FAILED** と表示されて
  いました。影響はシャード経路のみで、非シャードのテーブルは `rows_skipped = result.conflicts` を
  正しくマッピングするため、マルチテーブルのロード（テーブルごとに 1 ワーカー、非シャード）は
  無影響でした。シャードワーカーも `rows_skipped` を `conflicts` からマッピングするようにしました。
- **シャードテーブルの失敗時、失敗した全シャードの status/rows/message をエラーログに記録**します
  （メッセージを持つシャードだけではなく）― "one or more shards failed" が常に診断可能になります
  （以前はメッセージなしで失敗したシャードは原因が残りませんでした）。

## v0.1.118

### Fixed

- **`measure_performance` ハーネスが、失敗した実行でテーブル/シャード/バッチ単位のエラー
  レコードをダンプするようになりました。** シャード分割されたテーブルは、いずれかのシャードが
  失敗すると `FAILED` になりますが、シャードの実際の理由はエラーログにのみ記録され、perf 実行は
  "one or more shards failed" とだけ出力して原因がありませんでした。今は `FAILURE REASON` と
  ともに各 `DATA ERRORS` 項目（テーブル/チャンク・コード・メッセージ）を出力し、大きな単一
  テーブルロードの終盤の単一シャード失敗まで、ログだけで診断できます。

## v0.1.117

### Fixed

- **DSQL の「クラスターあたりスキーマ 10 個」制限が、実用的なエラーとして表示されるようになりました。**
  ターゲットクラスターが既に 10 個の上限に達している場合、マイグレーション用スキーマの
  `CREATE SCHEMA` が `program_limit_exceeded`（SQLSTATE 54000、"more than 10 schemas not
  allowed"）で失敗します ― `IF NOT EXISTS` があっても、DSQL は存在チェックより先に上限を
  チェックするためです。これはハード制限（再試行しても解消しない）なので、即座に明確な
  メッセージへ翻訳し、未使用スキーマの削除（`DROP SCHEMA ... CASCADE`）または別クラスターの
  使用を案内します（不透明なドライバエラーの代わりに）。OCC/一時エラーの再試行経路には
  意図的に載せていません。
- **`measure_performance` ハーネスがジョブの失敗理由を出力します。** `run_full_load` の外へ
  伝播した失敗（例：上記の pre-pass スキーマ/DDL エラー ― テーブルワーカーが動く前）は
  JobManager が捕捉した例外としてのみ保持され、実行は `status=FAILED` で全テーブル `PENDING`・
  理由なしと表示されていました。今は `FAILURE REASON: <例外>` を残し、失敗した perf 実行を
  ログだけで診断できます。

## v0.1.116

### Fixed

- **すべての replace テーブルを、並列データロード開始前に一度、順次 DROP+recreate** するように
  変更し、最大並列時のスタートアップ DDL ストームを解消しました。従来は各テーブルワーカーが自身の
  プロセス内でターゲットを recreate していたため、高い table-parallelism では全ワーカーが同時に
  共有スキーマカタログへ `CREATE SCHEMA`/`DROP`/`CREATE` を発行していました。DSQL はトランザクション
  あたり 1 つの DDL を楽観的並行性で処理するため、この同時カタログ書き込みが OC001
  (`SQLSTATE 40001`、"schema has been updated by another transaction")で競合し、DDL 再試行の予算を
  使い切って 1 行もロードする前にテーブルを失敗させることがありました。メタデータのみの
  DROP+recreate を既存の pre-pass で(シャード分割済みだけでなく)**すべて**の replace テーブルに
  対して実行し、ワーカーは既に空のターゲットへ DDL を再実行せずにロードします(ロード後の
  `CREATE INDEX ASYNC` DDL は適用済みの変換から導出)。最大並列の Full Load がカタログを競合させず
  決定的に開始します。

## v0.1.115

### Fixed

- **テーブルごとの DROP+recreate 接続が、一時的な接続失敗時に再試行されるようになりました。**
  接続ストームでテーブルが失敗する最後のギャップを塞ぎます。最大並列の Full Load
  (table-parallelism 16、テーブル 20 個)では、キューで待機する 4 テーブルは先頭 16 個が
  完了して初めて開始しますが、その 16 個はほぼ同時に終わるため、4 個が一斉に新しい DSQL
  接続を開き、DSQL の毎秒約 100 新規接続の上限を超えます。`recreate_table`(および
  `schema_applier` の他の DDL 接続経路)はこの接続を**いかなる再試行でも包まずに**開いて
  いたため、発生した `ConnectionTimeout: connection timeout expired` が**バッチが 1 つも
  実行される前に 0 行でテーブル全体を失敗**させていました(OCC 再試行も give-up ログも
  なし — 以前の修正が強化したバッチループの外側での失敗)。接続の open を、バッチローダの
  プールが既に使うのと同じ一時的接続再試行で包み、ストームを乗り切ります。
- 一時的接続の分類器を `core/target_connection.py`(`is_transient_connection_error`)へ移動し、
  **すべて**の DSQL 接続/実行経路が単一の定義を共有します(バッチローダのプール lease と
  DDL 接続の両方)。`batched_import` には後方互換のエイリアスを残しています。

## v0.1.114

### Changed

- **OCC/接続リトライループがリトライと give-up をログ出力する**ようになり、バッチ
  失敗をタイミング推論ではなくログで直接診断できます。`with_occ_retry` は無音でしたが、
  各リトライを DEBUG(attempt N/max、エラー型 + SQLSTATE、バックオフ遅延)で、予算
  枯渇時に WARNING(**リトライ回数、総経過時間、最後のエラー + SQLSTATE**)で記録します。
  この WARNING が *「リトライ予算が小さすぎた」* vs *「一時的ストームが予算より長かった」*
  vs *「リトライ不可のエラーだった」* を区別する直接的証拠です — 例:
  `occ-retry gave up after 30 attempts over 131.4s; last=ConnectionTimeout
  sqlstate=None`。純粋に追加のログであり、リトライ動作自体は変更なし。

## v0.1.113

### Changed

- **Full Load のバッチごとのリトライ予算をより忍耐強く(10 → 20)し、オペレーターが
  調整可能にしました** — バッチがより長い一時的な DSQL 接続ストームを乗り切り、
  テーブル失敗にならないように。この予算(`occ_max_attempts`)は OCC(`40001`)競合と
  v0.1.110/112 で追加した一時的接続リトライで共有されます。高並列度でのロード遷移時
  (多数のテーブルが同時に完了 → 再接続のバースト)にストームが従来の 10 回(~20秒)
  予算を超えて枯渇すると、エラーが正しくリトライ対象に分類されていてもテーブルが
  `ConnectionTimeout` で失敗しました。デフォルトを 20(指数バックオフで ~70 秒の
  リトライ)に引き上げ — 大規模ロードは数時間実行され、こうしたブリップに必ず遭遇
  します — `DSQL_MIGRATOR_FULL_LOAD_OCC_MAX_ATTEMPTS`(1–100)として公開しました。
  各リトライは依然として新しい接続で冪等バッチを再実行するため、忍耐が増すだけで
  重複は発生しません。

## v0.1.112

### Fixed

- **Full Load が、既知のメッセージシグネチャだけでなく SQLSTATE の無い接続エラーを
  すべてリトライするようになりました。** v0.1.110 で SQLSTATE の無い接続ドロップを
  リトライするようにしましたが、libpq/OpenSSL のメッセージ部分文字列リストで照合して
  いました。高並列の接続ストーム(多数のテーブルが同時に完了し数百の同時接続)では
  DSQL はドロップを様々な形で返します — "SSL error: unexpected eof"、"Network is
  unreachable"、そして **"connection timeout expired"** — リストに無いメッセージは
  恒久的失敗としてすり抜けていました(512 接続の 1TB 実行が `connection timeout
  expired` でテーブルを失った)。分類器は **`sqlstate=None` の psycopg
  `OperationalError`/`InterfaceError` をすべて**一時的な接続エラーとして扱うように
  なり(実データ/制約エラーは常に SQLSTATE を持つ)、例外の型でゲートするため、ツール
  自身の SQLSTATE 無し構造エラーは引き続きリトライしません。メッセージシグネチャ
  リストは型情報が失われたラップ済みエラー用のフォールバックとしてのみ残します。

## v0.1.111

### Fixed

- **DSQL 接続を IPv4 に固定し、IPv4 専用ネットワークで再接続がエンドポイントの到達
  不能な IPv6 アドレスで失敗しないようにしました。** Aurora DSQL のエンドポイントは
  デュアルスタック(A + AAAA レコード)です。IPv4 専用 VPC(例: IPv6 egress の無い
  ECS タスク)では、libpq が再接続を IPv6(AAAA)アドレスに向けると *"connection to
  server at … failed: Network is unreachable"* で失敗します。通常は表面化しませんが、
  一時的な DSQL イベント(例: 瞬間的な `XX000 server unavailable`)で再接続が一斉に
  発生すると、IPv4 は問題なく到達可能でも IPv6 の試行が実行中の Full Load を失敗させ
  ます(実測: in-VPC の 1TB ロードが DSQL ブリップ直後に IPv6 `Network is
  unreachable` でテーブルを失った)。`DsqlConnector.connect` がエンドポイントの IPv4
  アドレスを解決し `hostaddr` として渡す(DNS 名は TLS SNI / 証明書検証のため `host`
  に維持)ようになり、すべての接続・再接続が到達可能なアドレスファミリーに留まります。
  IPv4 が無い場合は従来の host ベース解決にフォールバック(IPv6 専用環境は影響なし)。
  Full Load・Validation・プローブなどすべての DSQL 接続に適用されます。

## v0.1.110

### Fixed

- **Full Load が、SQLSTATE を伴わないクエリ実行中の接続ドロップ(TLS 切断など)で
  テーブル全体を失敗させず復旧するようになりました。** バッチローダーは一時的な接続
  ドロップを新しい接続で冪等バッチを再実行してリトライする設計ですが、
  `_is_transient_connection_error` はサーバーが報告した **SQLSTATE クラス `08`** の
  ドロップしか認識していませんでした。TLS ソケットがクエリ実行中に切断されると
  サーバーはエラーコードを送れず、psycopg は `sqlstate=None` の `OperationalError` と
  *"SSL error: unexpected eof while reading"* / *"server closed the connection
  unexpectedly"* のようなメッセージのみを投げます。これらが**恒久的エラーと誤分類**
  され → リトライされず → バッチ(およびテーブル全体)が失敗していました。特に高い
  書き込み並列度で顕著(同時接続が多いほど DSQL がピーク時に一部接続を切断):
  in-VPC の 1TB ロードを `table_parallelism=16 × batch_parallelism=32`(512 接続)で
  実行したところ、完了間際に 16/20 テーブルが `SSL error: unexpected eof` で失われ
  ました。分類器が **SQLSTATE の無い接続喪失エラー**(libpq/OpenSSL のドロップ
  シグネチャで一致)も一時的として扱い、再接続・リトライするようになりました — CDC
  sink の一時的再接続に対応する Full Load 版です。SQLSTATE を持つ実データ/制約
  エラーや、接続ドロップでない SQLSTATE 無し構造エラーは影響なし(引き続き表面化し、
  無限リトライしません)。

## v0.1.109

### Changed

- **CDC のテーブル別ステータスの「How to read this table」を格段にスキャンしやすくし、
  分かりにくい列はその場で自己説明するようにしました。** 従来の凡例は小さなグレーの
  箇条書きの塊で、列名が説明文の中に埋もれ、consistency の色は文字でしか説明されて
  いませんでした。今回、薄いボーダーのパネルの**定義行(definition rows)**になり、各
  用語がテーブルヘッダーと一致するため対応が一目で分かります。また Consistency の項目は
  色を文字で書く代わりに、テーブルセルと**同じ色の実際のバッジチップ**(`consistent` /
  `replicating…` / `rows missing` / `data quarantined`)をそのまま表示します。さらに、
  意味が自明でない 3 つの列ヘッダー(**Net rows since Full Load**、**Stream lag**、
  **Consistency**)に一行説明の **ⓘ ツールチップ**を追加し、ヘルプを視線のある場所に
  置きました。凡例レイアウト用の再利用コンポーネント `definition_row` をデザイン
  システム(単一の情報源)に追加しました。

## v0.1.108

### Fixed

- **負荷が少数のテーブルに集中する(skewed)CDC ワークロードで、ホットテーブルが
  単一の sink タスクに直列化される問題を修正 — Kafka トピックのパーティションを
  テーブルサイズに比例して割り当てます。** 従来のスケーリング既定値はパーティションを
  均等に分散しており、これは書き込み負荷がテーブル間で均等だと仮定します。テーブル数が
  多い(sink 並列度の上限以上)と**トピックあたり 1 パーティション**に縮退し、1
  パーティションのトピックは最大 1 つの sink タスクしか消費できません。そのため
  少数のホットテーブルが書き込みの大半を担う場合(例: 9 個中 4 個のテーブルだけを
  叩く sysbench)、各ホットテーブルが単一タスクでしか処理されず、残りはアイドルになり
  純粋なスループット損失が生じていました(DSQL はほぼアイドル)。今回、スキャン不要の
  テーブル別行数見積り(Full Load ウォーターマークの値、または Full Load 前に CDC
  インフラをデプロイした場合は `information_schema` からの即時見積り)を読み取り、
  大きいテーブルに Debezium `topic.creation` グループでより多くのパーティションを
  付与します(ホットテーブルは 2 または 4。単一テーブルは同時 DSQL upsert の競合で
  効果が頭打ちになる 4 が上限)。これによりホットテーブルが複数タスクで並列に
  ストリーミングされます。負荷が均等な場合は何もせず(no-op)、サイズシグナルが無い
  場合や `DSQL_MIGRATOR_CDC_TOPIC_PARTITIONS` オーバーライドが設定されている場合は
  従来の均等既定値にフォールバックします。パーティション数はトピック作成時に固定される
  ため CDC インフラのデプロイ時点で決定され、順序には影響しません(Debezium は各
  レコードを主キーでキーイングするため、同じキーは常に同じパーティション)。有効化には
  CDC インフラの再デプロイが必要です(既存トピックのパーティション数は変更不可)。

## v0.1.107

### Changed

- **Evaluation の「Objects by importance」フィルターを、分かりにくい単一コントロール
  から、カテゴリーベースの明確な 2 つのドロップダウンに変更しました。** 以前の
  セグメントコントロールは、派生的な「Needs attention」区分と分類ごとの値を 1 つの
  軸に混在させており曖昧でした（例:「Needs attention」と「Review needed」）。これを
  AWS コンソール風のフィルタードロップダウン 2 つ — **Classification**（Automatic /
  Review needed / Unsupported）と **Estimated manual effort**（Simple / Medium /
  Significant）— に置き換えました。これはサマリーバッジがすでに示している色分け
  カテゴリーと同じです。2 つのフィルターは AND で結合され、いずれかのフィルターが
  有効なときは **Clear filters** リンクが表示されます。ドロップダウンが Cloudscape
  の「filtering」スタイルに一致するよう、再利用可能な `filter_select` / `filter_bar`
  をデザインシステム（単一の情報源）に追加しました。

## v0.1.106

### Fixed

- **MSK Serverless が自動選択されたサブネットのアベイラビリティーゾーンを拒否した
  場合に、CDC インフラのデプロイが自己修復するようになりました。** MSK Serverless
  はリージョン内の一部の AZ のみをサポートし、それを一覧取得する API がありません。
  そのためデプロイが AZ ごとに NAT イグレスのサブネットを 1 つずつ自動選択する際、
  サポートされない AZ（例: `ap-northeast-2d`）のサブネットを選んでしまうことがあり、
  その場合 `MskCluster` が
  `CREATE_FAILED … unsupported availability zones: [ap-northeast-2d]` で失敗し、
  スタック全体がロールバックされます。デプロイヤーはこの特定の失敗を検知して
  スタックイベントから拒否された AZ を解析し、ロールバックされたスタックを削除した
  うえで、その AZ を除外してコネクタサブネットを再選択し、作成を自動的に再試行します
  （無限ループを防ぐため回数制限あり）。サポートされない AZ を除外した結果、NAT
  イグレスの AZ が 2 つ未満になった場合は、繰り返さずに除外された AZ を明示した
  明確なメッセージで停止します。新たな入力は不要で、ユーザーは引き続き VpcId のみを
  指定します。

## v0.1.105

### Added

- **正確な時間ベースの CDC レプリケーションラグ — 不正確な `MAX(pk)`「Stream lag」を置き換え。**
  従来のテーブルごとの「Stream lag (newest)」は両側の `MAX(pk)` を比較していました: PK 単位の数
  (時間ではない)で、insert のみを反映(UPDATE/DELETE の遅延は見えない)し、単一整数 PK でのみ動作
  しました。DSQL シンクが各変更の**ソースコミット時刻**(Debezium `source.ts_ms`)を読み、テーブル
  ごとの **`ReplicationLagMs`** CloudWatch メトリクス = 適用時刻 − ソースコミット時刻(オフセット
  コミット窓ごとの最悪ラグ、ミリ秒)を送信するようになりました。移行モニターの **「Stream lag」**
  列が実際の時間値(「8.5s behind」「2m 10s behind」「caught up」)をライブかつスキャン不要で表示
  します — PK 型に依存せず、最新の insert だけでなく update/delete の遅延も反映します。時間
  メトリクスが利用できない場合(旧プラグイン)やカウント未更新時のみ、従来の `MAX(pk)` リーディング
  エッジチェック(「N behind (PK)」)にフォールバックします。送信は完全に best-effort(レプリケーション
  に影響なし)で、v18 のメトリクス配管/IAM(`cloudwatch:PutMetricData`、`metrics.stack`)を再利用 —
  新規 IAM なし。
- 反映するには、再ビルドされたコネクタプラグイン(`PLUGIN_VERSION` → `v19`)と CDC の再デプロイが
  必要です。それまでは列は `MAX(pk)` フォールバックを使用します。

## v0.1.104

### Fixed

- **Query Playground の「Test on target」が、修飾なしのテーブル名を解決できるようになりました
  （`relation "orders" does not exist` (42P01) の失敗を修正）。** MySQL データベースに対して書かれた
  クエリはテーブル名を修飾なしで使います（`SELECT * FROM orders`）が、移行では各 MySQL データベースを
  同名の PostgreSQL **スキーマ**（`ecommerce_demo`）にマッピングするため、DSQL 上ではテーブルはその
  スキーマに存在します — プローブが動作していたデフォルトの `public` search_path には無いので、修飾なし
  参照はすべて拒否されていました。プローブは `EXPLAIN`／ドライラン の前に `search_path` をソース
  データベースのスキーマ（次に `public`）へ設定するようになり、MySQL の実行コンテキストを再現して、
  変換後のクエリが移行済みテーブルに対して検証されます。ソース接続でデータベースが指定されていない
  場合は影響なし（search_path はそのまま）。

## v0.1.103

### Fixed

- **再実行中に別のステップへ移動して戻ったときに Validation が「In progress」で固まり、「Re-run
  validation」ボタンがロックされる問題を修正。** Re-run をクリックした後、実行中に画面を離れると
  (例: Data Migration ステップへ移動して Stop CDC)、ステップを `DONE` に切り替える poll タイマーが
  ページとともに破棄されます。そのため実行がバックグラウンドで完了した後に Validation へ戻ると、
  ステップは**コンテンツ描画内で** `DONE` に調整されますが、これはワークフローシェル(ステップ
  ヘッダーのバッジ + Re-run ボタン)が既に古い「In progress」状態を描画した後で、シェルは再描画され
  ません。今回、コンテンツ内の調整が永続ステータスを変更するたびに(実行中離脱ケースおよび v0.1.102
  の再接続ケース)、**ワンショットのリフレッシュを予約してシェルを再描画**するようにしました → 完了
  レポートが表示され Re-run ボタンが有効になります。(次の描画は `DONE`/`NOT_STARTED` を見るため
  ループしません。)

## v0.1.102

### Fixed

- **再接続後に Validation が「In progress」で固まり、「Re-run validation」ボタンが永久にロックされる
  問題を修正。** 検証が終わる直前にブラウザが再接続した場合(または実行中にセッションが保存された
  場合)、ステップが `IN_PROGRESS` として復元されると同時に完了レポートも復元されますが、検証ジョブ
  ID は永続化されないため、それを `DONE` に切り替えるライブジョブが存在しません。content 内の `DONE`
  への調整は**遅すぎ**(シェルが既に古い「In progress」バッジ + 無効な Re-run ボタンを描画した後で、
  シェルは再描画されない)、完了レポートが「In progress」ヘッダーの下に表示され Re-run が永久ロックの
  まま残っていました。セッション復元時に、**IN_PROGRESS だが完了レポートがある**場合(レポートの存在は
  実行完了の証拠)、**シェル描画の前に**ステップを `DONE` に調整するようにしました → 完了結果が表示され
  Re-run ボタンが有効になります。実際に実行中のランはレポートを持たない(実行開始時にクリアされる)
  ため、ライブランを隠すことはありません。

## v0.1.101

### Fixed

- **CDC インフラを削除した後、再接続したセッションで古い「Infrastructure deleted」ログに固まらず、
  再び「Deploy CDC infrastructure」アクションが表示されるようになりました。** 再接続時、セッション
  復元が**完了済み**の CDC ライフサイクルジョブのリンクを再適用し(終了した削除のステージログが
  描画され続ける)、テアダウン前の**古いコネクタ名**を復元していた(カードがパイプラインを誤分類)
  ため、再デプロイの導線が出ませんでした。最後の CDC 操作が**テアダウン(`delete`/`stop`)**の場合、
  復元時にその**完了ジョブのリンクと古いコネクタ名の両方をスキップ**するようにし、カードは新しい
  読み取り専用の AWS フェーズプローブで駆動されます: **absent → Deploy CDC infrastructure**、
  **infra → Start CDC**。スタック識別子は引き続き復元され(プローブがどのスタックを見るか分かる)、
  進行中のテアダウンは古いジョブではなくプローブのライブなスタック状態で反映されます。

## v0.1.100

### Added

- **永続的な S3 ジョブストア — 中断された Full Load とテーブルごとの移行モニターが Fargate の
  再デプロイでも失われなくなりました。** JobManager のジョブ状態はタスクの**エフェメラルな
  `/tmp`** の SQLite ファイルにあったため、アプリ再デプロイ(ECS タスク置換)で消えていました:
  中断された Full Load は resume できず、Full Load ジョブに紐づくテーブルごとのモニターが
  **再デプロイ後に空**になっていました(S3 セッションストアは `job_id` のリンクだけを保持し、
  ジョブ本体は保持しない)。新しい `S3JobStore` は各ジョブのスナップショットをツールの**管理型
  プラグインバケット**(セッションストアと同じバケット、自動プロビジョニング — 追加設定なし)の
  `jobs/` プレフィックスに JSON オブジェクトとして保存し、タスク置換をまたいでジョブ/resume 状態が
  残ります。Fargate では `DSQL_MIGRATOR_JOB_STATE_BUCKET` → 管理型バケットに接続; ローカル開発は
  従来の SQLite ストアを維持(どちらも `JobStore` プロトコルを満たすため JobManager は不変)。
- **スケール安全な書き込み(PUT ストームなし)。** Full Load のドレインは progress tick ごとに
  保存しますが、ローカル SQLite では安価でも大きなテーブルでは S3 を氾濫させます。resume には
  chunk/ジョブの**状態遷移だけ**が重要なので(非 `DONE` chunk は丸ごと再実行、サブ chunk の進捗は
  表示専用で中断された chunk は reload 時に `FAILED` に調整)、`S3JobStore` は状態シグネチャが
  変わったときに即 PUT し、純粋な進捗の書き込みは 5 秒あたり最大 1 回に throttle します — 行数に
  関係なく PUT を状態遷移数程度に抑制。best-effort(S3 エラーがライブ移行を壊すことはない)、新規
  IAM なし(タスクロールの既存のバケット `/*` 権限が `jobs/` プレフィックスをカバー)。テンプレート
  +コードのみの変更でコネクタ/プラグインの変更はなし — 次回のアプリ再デプロイで反映。

## v0.1.99

### Fixed

- **テーブルごとの正味行数モニターが、cluster モードだけでなく single-database モードでも
  動作するようになりました。** DSQL シンクは `NetRowsApplied` メトリクスの `Table` ディメンションを
  常に**スキーマ修飾**（`db.table`、例: `ecommerce_demo.orders`）で送信しますが、single-database
  モードではツールがテーブルを**修飾なし**の名前（`orders`）で扱うため、モニターの厳密ディメンション
  CloudWatch 照会が外れ、「Full Load 以降の正味行数」列が静かに `COUNT(*)` ベースの値にフォール
  バックしていました。reader は、当該スタックについてシンクが実際に送信した `Table` ディメンション値を
  `ListMetrics` で発見し、要求されたテーブルを厳密名で、なければ**曖昧でない修飾なし**のテーブル名で
  マッチするようになりました — これにより修飾スキームを仮定せずに cluster（すでに修飾済み）と
  single-database（修飾なし）の両方でスキャン不要の列が動作します。修飾なし名が曖昧な場合（2 つの
  スキーマに同じテーブル名）は、誤った帰属を避けるためスキップします（そのテーブルは COUNT に
  フォールバック）。アプリのタスクロールに `cloudwatch:ListMetrics` を付与します（リソースレベルの
  スコープがないため Resource `*`）。reader + IAM の変更のみでコネクタ／プラグインの変更はありません:
  デプロイするとロールが更新され reader が反映されます — プラグインの再ビルドや CDC の再デプロイは不要。

## v0.1.98

### Added

- **テーブルごとの「Full Load 以降の正味行数」を `COUNT(*)` なしで表示するようになりました —
  ソーススキャンではなくシンクのメトリクスから取得します。** DSQL シンクコネクタがテーブルごとの
  `NetRowsApplied` CloudWatch メトリクス（名前空間 `MysqlDsqlMigrator/CDC`、ディメンション
  `Stack` + `Table`）を送信します。コミットごとに insert − delete を記録するため（insert は +1、
  update は 0、delete は −1）、このメトリクスを合計すると、CDC がストリーミングを開始してから各
  テーブルに適用した正味行数になります。テーブルごとの移行ステータスモニターが既存の約 5 秒間隔の
  CDC ポーリングでこの値を読み取ってそのまま表示するため、「Full Load 以降の正味行数」列はソース／
  ターゲットに対する `COUNT(*)` を一切必要とせず、軽量で（数十億行になりうる）ソースを決してスキャン
  しません。CDC がストリーミング中は、テーブルごとの表もそのポーリング周期で再描画されるようになった
  ため（保存済みメトリクスを読むだけでネットワーク呼び出しなし）、「Refresh source/target counts」を
  クリックしたときにしか更新されなかった以前と異なり、列が**ライブで**更新されます（そのボタンは
  引き続き正確なソース／ターゲットの `COUNT(*)` を実行し、それらの列は従来どおりです）。シンクでの
  メトリクス送信は完全に best-effort であり（メトリクスの失敗がレプリケーションやオフセットコミットに
  影響することはありません）、メトリクスが利用できない場合（旧バージョンのプラグイン、またはシンクが
  まだ送信していない場合）は従来の `ターゲット − Full Load` の値にフォールバックします。この値はライブの
  進捗モニターであり、最終的な整合性判定ではありません: Kafka Connect が適用済みのバッチを再配信すると
  （at-least-once）わずかに過大カウントされる場合があるため、正確なソース対ターゲットの判定は引き続き
  Validation（ステップ 4）です。
- 反映するには、再ビルドされたコネクタプラグイン（`PLUGIN_VERSION` → `v18`）と CDC の再デプロイが
  必要です。それまではモニターは `COUNT(*)` ベースのフォールバックを使用します。テンプレート変更で、
  シンクのコネクタ実行ロールに名前空間条件でスコープした `cloudwatch:PutMetricData` を付与し、アプリの
  タスクロールの `cloudwatch:GetMetricData`（v0.1.97 で追加）がそれを読み戻します。

## v0.1.97

### Fixed

- **ライブ CDC パイプラインヘルスのスループットが実際に表示されるようになりました。** UI は
  change-flow パネルでコネクタの `AWS/KafkaConnect` CloudWatch メトリクス（`SinkRecordSendRate`、
  `SourceRecordPollRate`、running/errored タスク数）を読みますが、アプリのタスクロールに
  `cloudwatch:GetMetricData` が付与されていなかったため、すべての読み取りが `AccessDenied` で失敗し
  best-effort で握り潰され、スループットが空/unknown のままでした。`cloudwatch:GetMetricData`
  （Resource `*` — この API はリソースレベルのスコープ不可）を付与し、実際の send/poll レートが
  表示されるようにしました。**ソーススキャン不要**の軽量な CDC アクティビティ信号であり、次に追加する
  テーブル別 net-rows カスタムメトリクスの読み取り準備も整います。テンプレートの IAM 変更のみ —
  デプロイでロールを更新（イメージの再ビルドは不要）。

## v0.1.96

### Fixed

- **CDC の開始/停止が `kafkaconnect:ListConnectors` の `AccessDeniedException` で失敗しなくなりました。**
  CDC デプロイロールは `ListConnectors` をコネクタ ARN（`connector/mysql-dsql-cdc-*/*`）にスコープして
  いましたが、`ListConnectors` は **アカウントレベル** の list 操作で `.../v1/connectors` に対して認可される
  ため、ARN スコープでは何の権限も付与されていませんでした。デプロイヤーは 2-pass の Start CDC（および
  Stop）中にソース/シンクの状態を読むためコネクタを list しますが、その読み取りが AccessDenied で阻まれ、
  操作がエラー（"could not read … state"）で終了していました。v0.1.86 でコネクタ状態の読み取りが（静かに
  `None` を返す代わりに）例外を投げるようになったことで顕在化しました。`ListConnectors` は独立した文で
  `Resource: "*"` に付与し（タスクロールの discovery 権限と同じ）、他のコネクタ操作は `mysql-dsql-cdc-*`
  ファミリーにスコープしたままにしました。ロール更新のためアプリスタックのデプロイが必要（イメージの再ビルドは不要）。

## v0.1.95

### Fixed

- **スナップショットのシリアライズエラーが UI を壊さないよう S3 セッションストアを強化。**
  `S3SessionStateStore.save()` で `model_dump_json()` のシリアライズが、「呼び出し元に例外を決して
  投げない」ことを保証する `try`/`except` のすぐ外で実行されていたため、(通常は起きないが理論上あり得る)
  シリアライズ失敗がすり抜けて、セッション状態を保存するライブ UI リクエストを壊す可能性がありました。
  シリアライズをガード内に移動し、あらゆる場合にベストエフォート契約を守るようにしました — 通常経路の
  挙動は変わりません。(v0.1.93 変更の自己敵対的レビューで発見。)

## v0.1.94

### Fixed

- **CDC 停止がもう誤った「Stack operation timed out」を報告しません。** Stop CDC は
  `MskBootstrapServers` を空にしてコネクタを削除しますが、以前は in-VPC のオフセット seeder
  Lambda まで削除しており、その Lambda の Hyperplane ENI 回収に約 20〜40 分かかって、コントロール
  プレーンの 10 分の停止待ちを大きく超えていました。その結果、コネクタはすでに削除済み(= CDC は
  実際に停止)なのに停止が失敗と報告され、スタックは数分後に自力で `UPDATE_COMPLETE` に到達して
  いました。cdc-stack テンプレートは新しい `DeploySeederFunction` 条件(seeder キー + ウォーター
  マークのみに依存し、`MskBootstrapServers` とは無関係)により**停止中も seeder Lambda(+ ロール)を
  維持**し、停止時には高速な `OffsetSeedResource` invoker のみを削除します。これにより停止時の
  クリーンアップはコネクタ 2 個 + invoker(いずれも高速)だけになり、タイムアウト内で収束します。
  VPC-Lambda の ENI 撤去はスタック全体の削除時(そちらのタイムアウトは既に許容)にのみ発生します。
  更新後のテンプレートがデプロイされてから — つまり次回の Start CDC から — 有効になります。

## v0.1.93

### Added

- **再デプロイを越えて保持される durable なセッション再開(S3 ベースのセッションストア)。** 再接続した
  ブラウザが、セッションごとのワークベンチ(ワークフロー進捗・Step-1 Evaluation 結果・Schema Conversion
  の選択・CDC 開始点/アタッチしたスタック)を Evaluation の再実行なしに再開します。このスナップショットは
  従来コンテナの**エフェメラル**ディスク上のローカル SQLite ファイルにあったため、Fargate の**タスク置換**
  (あらゆる再デプロイ)で消え、デプロイのたびに Evaluation をやり直す必要がありました。新しい
  `S3SessionStateStore`(既存の `SessionStateStore` プロトコル実装)が、各非機密スナップショットをツール管理の
  プラグインバケット(`mysql-dsql-migrator-plugins-<account>-<region>`、自動プロビジョニング — 新しい
  パラメータや顧客設定は不要)の `sessions/` プレフィックスに書き込むため、再デプロイを越えて保持されます。
  Fargate デプロイでは新しい `DSQL_MIGRATOR_SESSION_STATE_BUCKET`(テンプレートが管理バケットを指定)で
  自動選択され、ローカル開発は SQLite パスのまま。非機密の状態のみ保存(Property 7 — ソース DB パスワードは
  Connect で再入力);永続化はベストエフォート(一時的な S3 エラーはログのみ・UI を壊さない)。タスクロールに
  セッション削除/prune 用の `s3:DeleteObject` を追加。

## v0.1.92

### Fixed

- **既存の CDC パイプラインをアタッチすると、テーブルセットも reconcile され、CDC ステップが
  「テーブル未選択」ではなく実際に稼働中のパイプラインを反映するようになりました。** セッションが
  既存の cdc-stack にアタッチ(「Attach to &lt;stack&gt;」、例: セッションリセット後)した場合や、
  パイプラインがセッション外で開始された場合、セッションには Full Load ウォーターマークもセッション内の
  テーブル選択もないため、パイプラインが実際に複製中でも CDC ステップは
  「Select at least one table before starting CDC」を表示し、空のセットから設定プレビューを作り、
  テーブル別ステータスを表示できませんでした。今後は描画時のスタックプローブがライブスタックの
  `TableIncludeList`(ソースコネクタの `table.include.list` = 各テーブル名)を読み取ってセッションに
  reconcile し、`_cdc_tables_for_config` がそれを(セッション内のウォーターマーク/選択の次の)最終
  フォールバックとして使用します。これにより adopt/セッション外パイプラインもどのテーブルを複製中か
  正確に解決し、「テーブル選択」警告が消え、設定プレビュー・テーブル別ステータスが実態と一致します。
  一方、通常のセッション内 Full Load → Start CDC フローは変わりません。別のスタックを再 adopt すると
  以前の reconcile セットはクリアされ、新しいプローブが再度埋めます。

## v0.1.91

### Fixed

- **CDC「Deploy log」がライフサイクルジョブ実行中に数秒ごとに勝手に閉じる問題を修正。** ライブ
  CDC パネルは新しいデプロイログ行とコネクタ状態をストリーミングするため ~5 秒ごとに再描画され
  ます。「Deploy log」展開パネルの開閉状態がパネル描画関数の**ローカル変数**に保持されていたため、
  全体の再描画のたびに折りたたまれた状態で作り直され、Start/Stop/Deploy を見るために開いたログが
  数秒後に自分で閉じていました。開閉状態を**セッションスコープのマイグレーション状態**に保持する
  ようにし、あらゆるレベルの再描画(内側の refreshable と外側のパネルポーリング)を通じて維持され、
  ユーザーが閉じるまで開いたままになります。

## v0.1.90

### Fixed

- **Schema Conversion ステップの「CDC 実行中のためスキーマを適用できない」ブロックが、行き止まり
  ではなく実行可能な案内になりました。** CDC パイプラインがすでにターゲットへストリーミングして
  いる場合、スキーマ変換の適用は(正しく)ブロックされます — シンクがそれらのテーブルへ書き込んで
  おり、DDL は複製されないため、REPLACE はそれらを drop・破損させる恐れがあるからです。以前は
  この案内が Apply クリック時に一瞬表示されるトーストだけで「まず CDC を停止せよ」と伝えるのみ
  でしたが、CDC を停止できる唯一の場所である Data Migration は Schema Conversion を前提条件と
  してロックされているため、その画面から先へ進む手段がありませんでした。今後 Schema Conversion
  は、CDC が稼働中のときステップ上部に**常時表示の警告通知**を出します — ターゲットスキーマは
  (CDC がストリーミング中なので)**すでに適用済み**であることを説明し、安全な唯一の経路である
  **「Skip conversion & continue to Data Migration」**(スキーマ変換をスキップして Data
  Migration へ進む)を提示します。このボタンは前進すると同時に Data Migration のロックを解除し、
  スキーマを本当に変更する必要がある場合はそこで CDC を停止できます。Apply 時のトーストも同じ
  実行可能な案内(続行するには Skip、スキーマを変えるには Data Migration で CDC 停止)を含むよう
  になりました。

## v0.1.89

### Fixed

- **「既存の CDC インフラにアタッチ」バナーが、実際に CDC を選ぶ Migration Plan ステップにも表示される
  ようになった。** v0.1.88 でバナーを追加したが、**Data Migration ステップのマイグレーションタイプ
  セレクタにのみ**あった。**Migration Plan** ステップは別画面(「CDC を含める? — はい、同期を保つ」を
  答える)で、そこではバナーが出ず、keep-in-sync を選んでも既存パイプラインがあるのに依然として
  「CDC インフラをデプロイ」フロー(および「already exists」エラー)に落ちていた。現在は Migration Plan
  の CDC インフラセクションが、別のスタック名で既存の `mysql-dsql-cdc-*` パイプラインを検出すると
  **「Attach to &lt;スタック&gt;」**バナーを表示する(そのステップで既に走る phase probe が検出結果も
  埋める)。そこでアタッチするとセッションがそのスタックを対象にし、次の probe がデプロイ済みと認識して
  「CDC infrastructure ready」を表示する。Data Migration 側の表示も維持される。

## v0.1.88

### Added

- **既存の CDC インフラを、CDC ステップの奥ではなく Migration Plan の段階で提示するようになった。**
  前リリースでアカウント全体の CDC 検出と「既存にアタッチ」の選択を追加したが、その導線は**アクティブな
  CDC サブステップ内でのみ**描画され、セッションリセット後はそこに到達しづらかった(先行ステップを先に
  通過する必要がある)。現在は、**プランに CDC が含まれた瞬間**に、マイグレーションタイプ選択の隣へ
  バナーが表示され、既存の `mysql-dsql-cdc-*` パイプラインを名前とともに **「Attach to &lt;スタック&gt;」**
  アクションで示す。CDC サブステップまで行って重複デプロイのリスクに直面することなく、その場でアタッチ
  できる。検出はプラン時点で既に走っており(プランに CDC が含まれるかでゲート)、本変更はその結果を
  ユーザーのいる場所に出すだけ。アタッチは read/attach-only で、意図的な2つ目のパイプライン(別のスタック
  名サフィックス)のデプロイは引き続き CDC ステップから可能 — つまり選択であり、ハードブロックではない。

## v0.1.87

### Added

- **CDC 画面が既存の CDC インフラを検出して「アタッチ」を提案するようになった — やみくもに再デプロイ
  しない。** ツールはマイグレーションが使う CDC スタック名をセッション状態にのみ保持しており、単一
  タスクのアプリ再起動(ECS/Fargate のタスク置き換え)でそれが失われると、再接続したセッションは
  **別名で既に CDC が稼働していても**デフォルトの「CDC インフラをデプロイ」フローに落ち、2つ目の
  (課金される)Amazon MSK クラスタを作るリスクがあった。CDC ステップはアカウント内の
  `mysql-dsql-cdc-*` スタックを走査し、セッションが対象としていないスタックがあれば主アクション
  **「&lt;スタック&gt;にアタッチ」**として提示する(新規デプロイは expansion 内に控えめに)。
  アタッチするとパイプラインの**ライブ状態を AWS から再導出**(running/provisioning/infra)するため、
  稼働中ならそのまま監視ビューに着地する。スタックやコネクタを一切変更せず、最初からやり直すには
  **Stop CDC**(コネクタのみ削除・MSK は維持)または **Delete CDC infrastructure** の明示的な経路を使う。
  アカウント範囲の走査には CDC デプロイロールに `cloudformation:ListStacks` が必要(アプリスタックに
  追加)。走査はベストエフォートで、権限が無ければ何も表示しないだけ。

## v0.1.86

### Fixed

- **コネクタの状態を読めないときに CDC が黙って停止せず、原因を表面化するようになった。** CDC 開始時、
  ツールはソースコネクタが `RUNNING` になるのを待ってからシンクコネクタを要求する。その待機がコネクタ
  状態を読むヘルパーで**あらゆるエラー**(認証情報の失効、スロットリング、一時的なネットワーク障害)を
  `None` に握りつぶしていた。これは「まだ作成中」と区別できないため、読み取り失敗時に「creating…」を
  延々とログするだけで、シンクは要求されず、エラーも表示されなかった(デプロイが停止したように見える)。
  復旧にはアプリのタスク再起動が必要で、Fargate ではこれが進行中のセッションを消去し、全ワークフロー
  ステップをやり直す羽目になった。現在は状態読み取りのエラーが**伝播**し、`RUNNING` 待機は一時的な
  読み取り失敗を数回だけ許容してから**実際の原因とともに失敗**し、回復不能な認証/認可エラーには
  「Start CDC を再試行」のヒントとともに**即座に失敗**する。コネクタが本当に存在しない場合は引き続き
  `None` として読まれる(変更なし)ため、通常のプロビジョニング中ポーリングには影響しない。

## v0.1.85

### Fixed

- **CDC のデプロイが失敗: CDC デプロイ用ロールに CloudWatch アラーム権限が欠けていた。** v0.1.84 で
  CDC スタックにコネクタごとの CloudWatch アラーム(`ErroredTaskCount`)を追加したが、アプリの
  `cdc-deploy` ロールに `cloudwatch:PutMetricAlarm` / `DeleteAlarms` / `DescribeAlarms` を付与して
  いなかった。そのため CDC 開始時にアラーム作成で `AccessDenied` となって失敗し、CDC スタックが
  ロールバック(ロールバックも `cloudwatch:DeleteAlarms` で失敗)して、コネクタが1つも作成されなかった。
  ロールは CDC スタック系列のアラーム ARN にスコープされたアラーム権限を持つようになった。**app-stack を
  再デプロイして権限を反映してから CDC 開始を再試行**すればよい(新しいイメージのビルドは不要 — IAM のみの
  テンプレート変更)。

## v0.1.84

### Fixed

- **CDC シンクが一時的な DSQL 接続エラーで停止せず自己回復(コネクタ再ビルド、`PLUGIN_VERSION` v17)。**
  一時的な失敗(OCC 再試行予算の枯渇、または DSQL の1時間アイドルクローズ・IAM トークン失効・MSK
  Connect ワーカー再生成による接続切断)の際、シンクは通常の `ConnectException` を再スローしていたが、
  Kafka Connect の `WorkerSinkTask` はこれを**致命的**とみなしてタスクを停止し、オフセットは前進せず、
  人手でコネクタを再起動するまで CDC が停止していた。これらの一時的ケースでは `RetriableException` を
  スローするようになり、`WorkerSinkTask` がそれを捕捉して同じバッチを再配信(一時停止して再試行)する
  ため、再接続を経てパイプラインが自己回復する。適用は冪等なので同じオフセットの再生は安全。一時/恒久の
  分類は変更なく、本物の poison 行は引き続き DLQ に送られる。
- **低トラフィックなソースでの gapless 再開: Debezium ソースコネクタに `heartbeat.interval.ms` を追加。**
  Debezium はレコードを送出したときのみコミット済み binlog オフセットを前進させる。キャプチャ対象テーブルが
  アイドルで他テーブルが binlog を更新している場合、コミット済みオフセットがライブ binlog ヘッドから遅れ、
  その後ソース binlog 保持期間がその位置を越えて削除(purge)すると、再起動時に再開できなくなる(ギャップ
  → 強制的な再 Full Load)。定期的なハートビートがオフセットを前進させ続ける。`heartbeat.action.query`
  は意図的に設定しない — 読み取り専用ソースへの書き込みになるため。MySQL ではハートビートレコードの送出で
  十分である。

### Added

- **CloudWatch アラームで失敗した CDC コネクタを自動検知。** 各コネクタ(Debezium ソース、DSQL シンク)に
  `AWS/KafkaConnect` の `ErroredTaskCount` メトリクスに対するアラームを追加し、人がコンソールを見て
  いなくても errored タスクが可視化される — 従来は復旧が完全に誰かが FAILED コネクタに気づくことに依存し、
  その空白がソース binlog 保持期間を超える可能性があった。アラームは常に作成される(CloudWatch で確認可能)。
  新しい任意パラメータ `AlarmNotificationTopicArn` に SNS トピック ARN を指定すると通知も受け取れる。デプロイに
  SNS の配線は不要(デフォルトは空)。

## v0.1.83

### Fixed

- **米国以外のリージョンでも AI アシストが動作: デフォルトの Bedrock モデルをリージョン非依存の
  `global.*` プロファイルに変更。** コードレベルのデフォルトモデル id が
  `us.anthropic.claude-sonnet-4-6`(米国ジオ限定のクロスリージョン推論プロファイル)で、非米国
  リージョンからは `InvokeModel` が拒否していた。例えば ap-northeast-2(ソウル)にデプロイして
  AI アシストを有効化し、モデル id を空のままにする(自然な経路)と失敗していた。デフォルトは
  `global.anthropic.claude-sonnet-4-6`(全ての商用リージョンから呼び出し可能)になり、
  CloudFormation テンプレートの推奨と一致する。`BEDROCK_MODEL_ID` で上書き可能。(リージョン
  移植性監査で発見 — デプロイテンプレート・リージョン解析・STS/トークンリージョン・S3
  エンドポイント/LocationConstraint は既にリージョン正常で、このコードデフォルトのみ米国固定だった。)

## v0.1.82

### Fixed

- **AI アシスト: 期限切れ/無効な AWS 認証情報が実行可能なメッセージを返すようになった。**
  セッション期限切れや署名エラー(`ExpiredTokenException`、`InvalidSignatureException`、
  `InvalidClientTokenId` など)が汎用的な「unavailable」/「unknown」に誤分類され、再認証のヒントも
  なく「AI なしで続行」とだけ案内していた。これらは提案経路と「Verify AI access」経路の両方で
  `ACCESS_DENIED` に分類され、両メッセージとも認証情報/セッション期限切れ時は再認証するよう案内する
  ようになった。
- **クラスタ全体のスキーマ調査: クロススキーマ外部キーの参照先がスキーマ修飾されるようになった。**
  クラスタ全体(複数スキーマ)を調査する際、テーブル名は `schema.table` に修飾されるのに外部キーの
  参照テーブルは未修飾のままで、後続の孤児チェック/DDL クエリが親を search_path(または別スキーマの
  同名テーブル)で誤って解決していた。FK の参照先は FK 自身の `referred_schema`(同一スキーマ FK は
  調査中のスキーマ)で修飾され、テーブル名の修飾方法と一致するようになった。

### Changed

- **AI アシストの堅牢化。** Bedrock クライアントに接続/読み取りタイムアウト(10秒/60秒)を設定し、
  接続がハングしても「AI is writing…」/「Verifying…」状態が無限に回り続けないようにした(停止した
  ソケットは分類されたネットワーク/タイムアウトエラーとして表面化)。「Verify AI access」は
  クライアント*構築*中のエラー(例: リージョン解決不可)も捕捉し、例外を UI に流さず実行可能な結果
  として報告する。接続画面の常設 AI ステータス行は、灰色のプレーンテキストではなくデザインシステムの
  パレットで深刻度を示す。
- **ソース概要: リーダーではなく Aurora ライターのインスタンスクラスを表示。** Aurora クラスタ
  エンドポイントのソースメタデータ取得が、任意のメンバーではなく `DescribeDBClusters`
  (`IsClusterWriter`)でライターを解決するようにし、ライター/リーダーのサイズが非対称な構成で
  ソース容量を誤表示しないようにした(ベストエフォート。失敗時は先頭メンバーにフォールバック)。
- **スキーマ適用: `CREATE SCHEMA` が重複レースを self-heal。** スキーマ作成時の `42P07` を
  テーブル/ビュー/インデックス経路と同様に `CREATED`(スキーマは存在)として吸収し、誤った `FAILED`
  ではなく収束するようにした。

## v0.1.81

### Fixed

- **Evaluation: `TINYINT(1)`・`BIT(n)`・`YEAR` が「完全に自動互換」と報告されなくなった。**
  互換性アセッサにこの3つの型のルールがなく、これらの列だけを持つテーブルが `AUTO`/`COMPATIBLE`
  (リスクゼロ)に分類されていた — スキーマコンバータは3つとも意味の変わる*別の* DSQL 型
  (`MANUAL`)にマッピングし、特に `TINYINT(1)` の値が `{0,1}` 以外だと Full Load が中断されるにも
  かかわらず。つまり Evaluation がロードで失敗しうるテーブルを「完全互換・リスクゼロ」と表示し、
  アセッサ自身の「暗黙に互換扱いしない」保証と矛盾していた。新しい `TINYINT_BOOLEAN`/`BIT_TYPE`/
  `YEAR_TYPE` ルールが、コンバータの分類に合わせて各々を具体的なリスクとともに `MANUAL` として
  表面化する。

### Changed

- **Evaluation: 空間(spatial)列が `UNSUPPORTED` ではなく `MANUAL` に分類される。** 空間型
  (`GEOMETRY`、`POINT`、`POLYGON` など)は「型を置換または列を再設計」という推奨とともに
  `UNSUPPORTED` に分類され、テーブルがブロックされたように見えていた。しかしコンバータは既に各空間列を
  `bytea` に自動置換(生の WKB バイトを Full Load・CDC 全体で保持)するため、テーブルは移行できる。
  新しい `SPATIAL_TYPE` ルールがこれを `MANUAL`(生の `bytea` で十分か確認。空間演算子/インデックスは
  失われる)に再分類し、既に移行できるテーブルの再設計へユーザーを誘導しないようにした。

## v0.1.80

### Changed

- **UI: ステータス表示をアドホックなグリフ/色ではなくデザインシステムのパレットで統一。**
  Data Migration・Evaluation 画面全体のデザインシステム統一パス:
  - Full Load の「CDC ストリーミング中」警告カードと CDC 整合性 / ストリーム遅延カラムから、
    リテラルの `✓`/`✗`/`⚠` グリフを削除(該当グリフを持たないフォントで豆腐(tofu)ボックスに
    化けるリスク)。深刻度は既存の色付き通知ボックス / ステータスバッジが伝え、ヘルステーブルの
    凡例はグリフではなく色付きバッジを説明する文言に変更。
  - ビジー状態のボタン(Fetch current position、Start CDC、Apply to target)は、デザイン
    システムが禁止する Quasar の `loading` prop ではなく、ボタンの無効化 + ラベル差し替え
    (例:「Applying…」)で進行状態を示すようにした。
  - 警告/破壊的な合図は orange ではなくデザインシステムの amber を使用(Stop CDC ボタン、
    スコアゲージ、労力/競合バッジ)。
  - 描画されないデッドコードの `_format_complete_cell` ヘルパーを削除。

## v0.1.79

### Fixed

- **アプリシェル: ステップ描画のクラッシュが青い info ではなく赤い error 通知で表示される
  ようになった。** トップレベルの「ステップを表示できません」フォールバックが
  `render_notice(tone="negative")` を呼んでいたが、`negative` は定義済みの通知トーンでは
  ないため、実際にはアプリ内で最も警戒すべき状態(未処理の描画例外)であるにもかかわらず、
  静かに穏やかな青い `info` スタイルにフォールバックしていた。深刻度に合わせて
  `tone="error"`(赤)を使用するようになった。

### Housekeeping

- オープンソース公開のための衛生対応: 内部セッションのハンドオフノートと、発表資料内の
  内部作成者/リポジトリ識別子を削除し、コネクタのソース・CloudFormation テンプレート・
  CDC read-model 内の内部(未公開)設計/仕様ドキュメントへのダングリング参照をインライン
  要約に置き換えた。`CONTRIBUTING.md`、`CODE_OF_CONDUCT.md`、`SECURITY.md` を追加した。

## v0.1.78

### Fixed

- **スキーマ変換: `DOUBLE(M,D)` が有効な DSQL DDL を生成するようになった。** MySQL の
  `DOUBLE(M,D)` カラム(例: `DOUBLE(10,2)`)が型マッパーをすり抜けていた(sqlglot はこれを
  `DOUBLE` 種別としてパースするが、`UDOUBLE`/`FLOAT` の特別扱いはいずれも見逃す)。その結果、
  2 引数の `FLOAT(10, 2)` としてレンダリングされた。PostgreSQL/DSQL の `double precision` は
  引数を取らないため、これは構文エラーとなり、適用時にテーブル**全体**の `CREATE TABLE` を
  失敗させた。`DOUBLE(M,D)` は(表示用の `(M,D)` 指定はストレージ上の意味を持たないため)既存の
  `FLOAT(M,D) -> real` と同様に、単純な `double precision` にマッピングされるようになった。
- **検証: 大きな `BIGINT UNSIGNED` / `DECIMAL` 値が誤ったチェックサム不一致を生じなくなった。**
  PostgreSQL 側の `to_char` 数値マスクは整数桁を 18 桁分しか用意していなかったが、MySQL 側は
  `CAST(... AS DECIMAL(65, scale))` でレンダリングし、`BIGINT UNSIGNED` は `numeric(20, 0)`
  として格納される。約 10^18 以上の整数(例: `18446744073709551615`)はマスクを溢れ、`to_char`
  が桁の代わりにオーバーフロー表示(`####...`)を出力したため、バイト単位で同一の値が
  **チェックサム不一致**として報告され、誤ってカットオーバーをブロックしうる状態だった。マスクは
  `DECIMAL(65,0)` の全 65 桁の整数範囲をカバーするようになった。

## v0.1.77

### Fixed

- **CDC がソースの再起動を手動介入なしで乗り切る。** ソースの RDS/Aurora インスタンスが再起動すると
  (メンテナンスパッチ、フェイルオーバー、インスタンスクラス変更)、Debezium ソースコネクタが retriable な
  binlog エラーに遭遇して 1 回再起動し、その再起動が "Error reading MySQL variables: Communications link
  failure"(ソースがまだ起動中)で失敗するが、`errors.retry.timeout` が既定値 `0`(再試行なし)のため
  Kafka Connect が **タスクを永久に停止**("will not recover until manually restarted")させていました —
  サイレントストール(`SourceRecordWriteRate=0`)で、Stop/Start でしか回復できませんでした。ソースコネクタも
  シンクと同様に `errors.retry.timeout=600000`(10 分)+ `errors.retry.delay.max.ms=60000` を設定し、
  再起動ウィンドウの間ずっと再試行し、ソースが復帰するとコミット済み binlog オフセットから gapless に再開します
  (人手不要)。2026-07-08 のソース 2→8 vCPU スケールアップ再起動で観測・修正。

### Changed

- **CDC: シンクの MCU をソースと分離して個別に指定 (`SinkMcuCount`)。** 行ごとの往復が除去された後、
  シンクは CPU バウンドになり(プラグイン v16: 4 MCU で CPU ~80% / ~21,000 rows/s)、一方シングルタスクの
  ソースは CPU に余裕があります。新しい `SinkMcuCount` CFn パラメータ(既定 4)でシンクを独立してスケール
  でき、`ConnectorMcuCount` はソースのみに適用されるようになりました。測定: シンク 4→8 MCU でスループット
  ~21,000 → ~26,200 rows/s、CPU 80% → ~34%。

## v0.1.76

### Changed

- **CDC シンク: パラメータメタデータをステートメントごとに 1 回のみ取得 (プラグイン `v16`) —
  シンクスループット約 9.7 倍。** `bind()` がすべての変更イベントで `getParameterMetaData()` を
  呼び出しており、pgjdbc ではこれがサーバー側の Parse/Describe 往復であるため、シンクは**適用行ごとに
  読み取り専用トランザクションを 1 つ**発行していました — DSQL の `ReadOnlyTransactions` メトリクスが
  ~115,000/分（書き込みレートの約 60 倍）である一方、`OccConflicts` は一貫して 0 であることで確認。
  つまり真の上限はサーバー側の書き込み競合ではなく、この隠れた往復であり、v13/v15 のバッチ化の利得の
  大半を打ち消していました。メタデータは同一 SQL ならすべての行で同一なので、prepared statement ごとに
  1 回だけ取得して `bind()` に渡すようにしました。測定 DSQL 適用レートは ~1,925 → **~18,672 rows/s**
  (8 パーティション/タスク) に上昇。読み取り専用トランザクションは約 150 倍減少、シンク CPU は
  10% → ~65%。シンク JAR の変更のみ (`PLUGIN_VERSION` → `v16`)。

## v0.1.75

### Changed

- **CDC シンク: マルチ行 INSERT リライト (プラグイン `v15`) — シンクスループット +30%。** シンクの
  JDBC URL で pgjdbc `reWriteBatchedInserts=true` を有効化し、単一行 `INSERT` のバッチを 1 つの
  マルチ行 `INSERT ... VALUES (..),(..) ON CONFLICT ..` 文にまとめます — N 回の execute 往復が
  1 回に。DSQL はレイテンシバウンドなので、これにより測定シンクスループットが ~1,500 → ~1,925 rows/s
  (8 パーティション/タスク) に向上し、DSQL 適用レートでクロスチェックしました。リライトを安全にする
  ため、`applyChunkBatched` はまず各同一 SQL ランをプライマリキーごとの最終行に dedup します
  (last-write-wins — 冪等・順序保持)。これがないと、リライトされたマルチ行 `ON CONFLICT` が重複する
  競合キーを拒否します ("cannot affect row a second time")。シンク JAR の変更のみ
  (`PLUGIN_VERSION` → `v15`)。

## v0.1.74

### Changed

- **CDC コネクタのスケーリングをハードコードではなく推論。** ツールはテーブルごとのトピックパーティション数、
  シンク `tasks.max`、MSK Connect MCU 数を、キャプチャ対象テーブル数から計算し
  (`compute_cdc_scaling_defaults`)、cdc-stack 作成時に渡します。総シンク並列度 (`パーティション × テーブル`)
  が上限 8 に達する最小のトピックあたりパーティション数を選びます — 例: テーブル 1 個 → パーティション 8、
  4 個 → 各 2、≥8 個 → 各 1 — シンクが DSQL 書き込みレイテンシバウンドで、その点を超えるとサブリニアにしか
  増えないためです。パーティション数は不可逆 (トピックのパーティションは増やすことのみ可能) なので作成時に
  設定します。以前は `topic.creation.default.partitions` が `4` にハードコードされていましたが、現在は
  `TopicDefaultPartitions` CFn パラメータです。上級者は `DSQL_MIGRATOR_CDC_TOPIC_PARTITIONS`、
  `DSQL_MIGRATOR_CDC_SINK_TASKS_MAX`、`DSQL_MIGRATOR_CDC_MCU_COUNT` で推論を上書きできます。
  マニュアル §7.2 (CDC) にモデルを文書化。

## v0.1.73

### Changed

- **CDC ソースのスループットチューニング (プラグイン `v14`)。** v0.1.72 のシンクバッチ化の後、
  ボトルネックが Debezium ソースへ移りました(~2,000 rec/s、CPU ~12% — binlog パースではなく
  produce/キューバウンド)。ソースパイプラインのノブを CFn パラメータとして公開し、再デプロイで
  広げられるようにしました: `SourceMaxBatchSize`(8192)と `SourceMaxQueueSize`(32768)は
  ストリーミング反復あたりに排出する binlog イベント数を増やし、`SourceProducerBatchSize`
  (256 KiB)・`SourceProducerLingerMs`(20)・`SourceProducerCompression`(`lz4`)は Kafka の
  produce バッチを拡大・圧縮します。producer ノブは**ソースワーカー設定**に `producer.*` として
  設定します — MSK Connect はコネクタ単位の `.override.` キーを拒否するためです — したがって
  不変のワーカー設定は `PLUGIN_VERSION` を `v14` に更新して名前を差し替えます(コネクタ JAR の
  変更なし)。

## v0.1.72

### Changed

- **CDC シンクのスループット: バッチ適用 (プラグイン `v13`)。** DSQL シンクコネクタは、
  同一 SQL にレンダリングされる*連続した*変更イベントの最大ランを、行ごとの
  `executeUpdate()` ではなく 1 回の JDBC `executeBatch()` にまとめて適用するようになりました。
  DSQL はレイテンシバウンドです — 各ステートメントが分散ラウンドトリップであり、シンクタスクは
  CPU ~5% / ~550 rec/s で観測されました(計算ではなくラウンドトリップに律速)。行ごとの
  ラウンドトリップをバッチ送信に畳むことがスループットの主要なレバーです。**適用順序は
  保持されます**: 連続する同一 SQL イベントのみがまとめられるため、同一 PK に対する upsert の
  後の delete も到着順に適用され、テーブル/カラムセット/種別が変わるとランが分割されます。
  poison-row の隔離、OCC リトライ、冪等な再適用は変更されません(恒久的失敗時は引き続き
  行単位の適用にフォールバック)。`PLUGIN_VERSION` を `v13` に更新。
- **CDC シンクの `consumer.max.poll.records` のデフォルトを 3000 に変更。** 新しい
  `SinkMaxPollRecords` CFn パラメータ(デフォルト 3000、シンクワーカー設定に指定)。Kafka の
  デフォルト(500)が 1 回の `put()` 呼び出しに届くレコード数 — つまり 1 つの ≤3000 行 DSQL
  トランザクションにバッチできる数 — を制限し、バッチ適用が埋まりきりませんでした。
  トランザクション上限に合わせることで 1 回のポーリングが 1 回のラウンドトリップを満たします。
- **大規模ソース向けに CDC スループットのデフォルトを引き上げ:** `ConnectorMcuCount` 4、
  `SinkTasksMax` 4、テーブルごとの `topic.creation.default.partitions` 4 に設定し、シンクが
  データトピックを 4 パーティションにわたり並列に消費できるようにしました(実効シンク並列度は
  パーティション数で制限)。アプリスタックは 8/16 vCPU のタスクサイズも許可するようになりました。

### Fixed

- **CDC: 非 GTID ソースが file:position モードへ確実にフォールバック。** Debezium にすべての
  GTID を除外(`gtid.source.excludes=.*`)し、GTID 不在時に DML をフィルタしない
  (`gtid.source.filter.dml.events=false`)よう指示することで、`gtid_mode=OFF` のソース
  (GTID を有効化できない RDS MySQL など)が 0 件ではなく binlog file:position で変更を
  キャプチャします。

### UI

- **Start CDC の即時フィードバック:** デプロイ要求の処理中にボタンが無反応に見えていたのを、
  クリック時にローディング状態とトーストを表示するように変更。
- **中断された CDC ステージに FAILED アイコンを表示:** ジョブ終了後も進行中スピナーのまま
  だったのを、失敗アイコンで表示。

## v0.1.71

### Fixed

- **CDC: `SnapshotMode` が実際に CloudFormation テンプレートに伝達されるよう修正。**
  v0.1.70 で Python は正しいモードを計算していたが、CFn テンプレートに
  `snapshot.mode: recovery` がハードコード。`SnapshotMode` パラメータを追加し、
  Start CDC 時に新テンプレートも渡して既存スタックでも新パラメータを認識。
  "Could not find existing redo log information" 失敗の実際の原因を修正。

- **CDC: ソース DB ポートがセッションから実際の値を読み取るよう修正。** 以前は常に
  3306 デフォルト使用、非標準ポートでコネクタータイムアウト失敗を引き起こした。

## v0.1.70

### Fixed

- **CDC: 新しいコネクターに `snapshot.mode=schema_only` を正しく適用。** 以前は
  `recovery` にハードコードされ、schema-history トピックが存在しない場合に "Could not
  find existing redo log information" エラーで失敗。実際の Full Load ウォーターマーク
  （binlog 座標付き）がある場合のみ `recovery` を使用し、それ以外（手動開始、セッション
  リセット、CDC-only）はすべて `schema_only`。

- **CDC: デプロイ前のサブネット NAT egress 事前検証。** MSK Connect はプライベート IP
  のみ割り当てるため、NAT なしのサブネットでは Secrets Manager にアクセス不可。ユーザー
  入力および自動検出サブネットの両方をデプロイ前に検証。他スタック削除による NAT 消失の
  レースコンディションも防止。

- **CDC: deploy/start 中に prerequisites ボタンを無効化。** CDC スタック操作中も
  Check ボタンがロックされるように改善。

## v0.1.69

### Added

- **CDC: Manualモードに「Fetch current position」ボタン追加。** Full Loadウォーターマークが
  ない CDC-only フローで、ソースMySQLに `SHOW MASTER STATUS` を実行し現在の
  GTID/binlog位置を自動入力するボタンを追加。ユーザーがソースで手動SQLを実行して
  コピー＆ペーストする必要がなくなりました。

## v0.1.68

### Changed

- **Full Load: マルチプロセス並列化（GILバイパス）。** テーブルを
  `ProcessPoolExecutor`で別OSプロセスにロードし、各テーブル（またはシャード）が
  独自のGIL+CPUコアを使用。単一整数PKの大規模テーブルは自動的にPKレンジシャード分割。
  ECS Fargate 8 vCPUでの測定結果：
  - 4テーブル混合（tp=8）: **34,800 rows/s**、CPU 561%
  - 単一3360万行テーブルシャード（tp=8）: **51,000 rows/s**、CPU 777%
  - 200GBテーブル推定: **約2.5時間**（従来約46時間、**18倍高速化**）

## v0.1.67

### Changed

- **Full Load単一テーブルスループット最適化（GIL対応）。** GIL保持時間と
  ネットワークラウンドトリップを削減する5つの変更を複合適用：
  1. MySQLキーセットページサイズを1,000→5,000行に拡大 — ソースラウンドトリップ5倍削減。
  2. `build_insert_statement` SQLテンプレートをバッチshape別にキャッシュ — バッチ
     あたり約40,000オブジェクト割り当てを排除。
  3. `_iter_batches`バイト推定をlazy化 — バッチ先頭行のみサンプリング、
     `_estimate_row_bytes`呼び出し90%以上削減。
  4. `_flatten_params`をリスト内包表記に変換（CPythonで約40%高速化）。
  5. `convert_row`パススルー高速パス — 型変換不要な列はfrozenset参照のみ。

### Fixed

- **"Retry unfinished tables"ボタンに即時フィードバック追加。**
- **Schema Conversion個別"Apply to target"が既存テーブル検出後Replace/Skip
  ダイアログ表示。**
- **"Keep integer PK" → "Keep source PK"** ラベル修正。
- **"Apply converted to target" → "Apply all to target"** ラベル修正。

## v0.1.66

### Changed

- **Migration overviewダイアグラムを単一の統合パネルに再設計。** これまで3つに
  分かれていたボーダー付きカード（Source / Migration Tool / Aurora DSQL）を、
  1つの共有サーフェス内のボーダーなしカラムセグメントに統合。ステータス表示は
  ボーダー付きチップバッジの代わりに軽量なドット＋テキストパターン（Cloudscape
  「StatusIndicator」）を使用し、フローコネクタは破線矢印＋プレーンテキスト
  キャプションに簡素化。全体的なクロームを大幅に削減しつつ、すべての情報
  （endpoint、engine、region、connection state）は保持。デザインシステム
  （`ui/design.py`）に再利用可能な `render_status_dot` コンポーネントを追加。

## v0.1.65

### Changed

- **既に存在する単一オブジェクトを適用する際、黙ってスキップせず、どう扱うかを尋ねるように
  なりました。** スキーマ変換(ステップ2)で、ターゲットに既に存在するテーブル(かつ DDL を
  編集していない場合)に対してオブジェクト単位の **Apply to target** を押すと、以前は単に
  `SKIPPED` と報告してターゲットを変更しませんでした — 気づきにくく、そのボタンから判断を
  変える手段もありませんでした。今後は **Replace / Skip / Cancel** ダイアログを開き、適用の
  その瞬間に明示的に選べるようにします。これは選択を**元に戻す**ときに特に重要です — 例:
  複合キーにしたテーブルを整数キーに戻す場合、SKIP では既存の複合キーテーブルがそのまま
  残りますが、Replace は新しい DDL でドロップして再作成します。(オブジェクトの DDL を編集
  した場合や、グローバル REPLACE モードの場合は、従来の破壊的置換の確認フローを通ります。)
- **オブジェクト単位の Apply to target ボタンが、処理中であることを示すようになりました。**
  適用の実行中(少し時間のかかるターゲットへの往復、または確認ダイアログの待機)、ボタンは
  無効化されたローディングスピナー状態に変わり、適用が完了すると元に戻ります — 遅い適用が
  もはや反応のないクリックのように見えることはありません。適用が失敗しても、この状態は常に
  解除されます。

## v0.1.64

### Added

- **テーブル単位で選択できる複合主キー(書き込みホットパーティションの解消)。**
  Aurora DSQL は主キー順に行を格納するため、単調増加する `AUTO_INCREMENT` キーは
  すべての挿入を 1 つのパーティションに集中させ(書き込みホットパーティション)、
  スループットの上限を作ります。Schema Conversion(ステップ 2)にテーブル単位の
  **主キー**選択が追加されました:整数キーを維持(既定、変更なし)するか、選んだ
  高カーディナリティ列を先頭に付けた**複合キー**(例:`(customer_id, id)`)に切り替えて
  書き込みを複数パーティションに分散します。ソース MySQL スキーマは決して変更せず、
  DSQL ターゲットキーのみが変わります。先頭列の候補は NOT NULL かつ既存キーに含まれない
  列のみを提示し、結果を DSQL のキー制限(列 8 個以下、1 KiB 以下)に照らして検証し、
  元のキーの一意性を保つために `CREATE UNIQUE INDEX ASYNC` を生成します。選択時に
  影響を明示します:カットオーバー後はアプリケーションのクエリ・結合・アップサートが
  新しい複合キーを使う必要があり、先頭列は不変でなければなりません。
  - **Full Load** は複合キーテーブルを正しくロードします:冪等な `INSERT ... ON
    CONFLICT` が**ターゲット**主キーで動作するようになり(従来は常にソースキーを使用)、
    キーが変わってもターゲット制約と食い違いません。ターゲットキーが異なる既存テーブルへの
    追記(append)は明確なメッセージで拒否されます(新しいキーを適用するため先に再ロード)。
  - **CDC** はコネクタ/プラグインの変更なしで複合キーテーブルをレプリケートします:
    Debezium ソースを `message.key.columns` で再キー化し、各変更レコードのキーが
    ターゲットの複合キーに一致するようにするため、シンクの冪等なアップサート/削除が
    そのまま適用されます。複合キー列を LOB 除外対象にも選んだ場合のみ(キー生成のために
    必ずキャプチャが必要)、CDC 開始を実行可能なメッセージ付きで停止します。

## v0.1.63

### Changed

- **Full Load が大きなテーブルを複数の並行リーダーで読み込めます(reader range
  sharding)。** 単一の keyset リーダーは CPU バウンド(行ごとの型変換)で 1 コア
  付近が上限になるため、大きなテーブルの読み込みを K 個の互いに素な主キー範囲に
  分割して並行ストリーミングし、すべてを 1 つの書き込みプールに供給します。既定で
  無効(`DSQL_MIGRATOR_FULL_LOAD_READER_SHARDS=1`)。単一の**整数**PK かつ推定行数が
  `DSQL_MIGRATOR_FULL_LOAD_SHARD_MIN_ROWS`(既定 1,000,000)以上のテーブルにのみ
  適用され、複合/非整数 PK や小さいテーブルは常に単一リーダーです。ソースリーダー
  総数(`table_parallelism × shards`)が安全な上限を超えないよう制限されます。単一の
  一貫スナップショットを保つ必要があるクリーンな置換ロード(plain INSERT、CDC なし)
  にはシャーディングを適用せず、watermark + 冪等な再ロードによりシャードごとの
  スナップショット時刻差が安全な既存データ/CDC 経路にのみ適用します。再開・OCC 処理・
  書き込み側の動作に変更はありません。

## v0.1.62

### Changed

- **Full Load が書き込みプールに先行して読み込みます(bounded prefetch キュー)。**
  ソースリーダーが専用のバックグラウンドスレッドで bounded キューを満たすため、
  page N+1 の読み込みが page N の書き込みと重なります(従来は読み書きが直列)。
  キュー上限を書き込み並列度の約2倍に抑えているためメモリは引き続き bounded で、
  ロード順序は不変(バッチは固定 PK 範囲にマッピング)、停止/キャンセル時には
  リーダースレッドを join するのでスレッドリークもありません。既定で有効。計測用の
  seam(`DSQL_MIGRATOR_FULL_LOAD_PREFETCH=0`)で無効化すると従来経路を再現でき、
  A/B ベンチマークが可能です。ロードの正確性・再開動作に変更はありません。

## v0.1.61

### Changed

- **Full Load の進捗テーブルを簡素化。** 9 列 → 6 列に減らし、一目で読めて折り返しが
  なくなりました:「Rows on target」と「Source rows」を **Rows (target / source)** の
  1 列に統合し、大きな数値は省略表記(`1.18M / 33.6M`)、正確な数値と new/already-there の
  内訳はホバーのツールチップに移動しました。**Errors** 列は **Attempts** に統合し(例:
  `5 · 1 err`)、冗長な **Complete** 列は削除しました(Status + Progress ですでに分かる)。
  **Time** ヘッダーも折り返さなくなりました。小さい数値は桁区切り付きで全桁表示されます。

## v0.1.60

### Changed

- **Full Load の実行中は前提条件チェックを再実行できないように。** 以前はロード中に
  Prerequisites ステップへ戻って「Check」を押せました。チェックは読み取り専用で実行中の
  ジョブには触れない(新しい結果は *次回* の実行にのみ適用)ため無害でしたが、無意味で
  紛らわしいものでした — 新たに失敗したチェックがロード実行中に赤い「ブロック」判定を
  表示し、ソースに不要な読み取り負荷を追加していました。今は Full Load が IN_PROGRESS の
  間は Check ボタンが無効になり、チェックは次回の実行に適用され実行中のロードには影響しない、
  という短い注記が表示されます — migration-type セレクターが実行中にロックされるのと同じ
  方式です。チェックを再実行するにはロードを停止してください。

## v0.1.59

### Changed

- **Full Load の「Failure details」をすっきりさせ、長いエラーでもボタンがずれないように。**
  各失敗行の行内「Reload」ボタンを削除しました — 再試行は下部の単一の
  「Retry unfinished tables」チェックリストだけで制御されるようになり、競合する 2 つの
  コントロールではなく一貫した 1 つの方法になります。各失敗行は安定したレイアウト(左に
  テーブル名 + 折り返すエラーメッセージ、右に「AI Assist」アクションを固定)になり、長い
  エラーメッセージがボタンを 2 行目に押し出したり、行ごとに整列が崩れる問題が解消されました。
  隔離(quarantine)行は独自の「Reload」を維持します(隔離されたテーブルは「未完了」ではなく
  DONE なので、再試行チェックリストの対象外)。

## v0.1.58

### Fixed

- **ロード実行中に Full Load の進捗テーブルが 1 ページ目に戻らないように。** テーブル
  ごとの進捗テーブルは約 1.5 秒ごとに更新され、そのたびに一から再構築されていたため、
  2 ページ目以降に移動しても次のティックで 1 ページ目に戻っていました。選択したページが
  更新をまたいで保持されるようになりました(テーブルが縮小しても空のページに取り残され
  ないよう範囲を補正)。

## v0.1.57

### Fixed

- **失敗した実行が PENDING のまま残したテーブルを再試行できるように(以前は取り残されて
  いた)。** Full Load が一部のテーブルを試行する前に失敗で終わると、それらは `FAILED`
  ではなく `PENDING` のまま残り、復旧 UI が `FAILED` チャンクだけを見ていたため「Retry」
  操作が表示されず、唯一の脱出手段が全体の「Re-run Full Load」だけでした。復旧は
  **未完了** のテーブル全体(FAILED または PENDING)を対象にするようになりました:
  終了した実行に未完了テーブルがあれば再試行の行が表示され、ボタンは
  「Retry unfinished tables (N)」と表示され、チェックリストが各テーブルを理由とともに
  示します(エラーメッセージ、または PENDING テーブルは「Not loaded yet — the previous
  run ended first.」)。すでにロード済み(DONE)のテーブルは保持され、不要に再実行され
  ません。(v0.1.56 のクラッシュの復旧経路です — 更新後、「Retry unfinished tables」を
  押して PENDING のテーブルを続けてロードしてください。)

## v0.1.56

### Fixed

- **「Drop & reload」が `SchemaApplier` の TypeError で実行全体をクラッシュさせる問題
  を修正。** 依存ビューをドロップ/再作成する必要があるテーブルに Drop & reload を選ぶと
  `TypeError: SchemaApplier.__init__() missing 1 required positional argument:
  'introspector'` が発生し、Full Load 全体が中断され、テーブルごとの進捗ビューが消えて
  「Migration failed」だけが表示されていました。依存ビューの pre-drop/recreate は
  `SchemaApplier` を誤って構築せず、introspector 不要の DDL ヘルパー(`drop_object` /
  `recreate_table`)を使うようになり、クリーンリロードが成功します。さらに、オプション
  であるビューの pre-drop/recreate パスは防御的になり、そこで予期しない失敗が起きても
  ログを残してスキップし、実行を失敗させません — ビュー処理の問題が二度と Full Load の
  進捗を消さないようにしました(本当にドロップできないテーブルは、従来どおり通常の
  テーブル単位の失敗として表示され対応できます)。
- **CDC が稼働中かつ実行可能な状態のとき、Full Load ステップがエラーになる問題を修正。**
  CDC がストリーミング中で Start/Re-run ボタンが有効な特定のケースで Full Load ステップ
  のレンダーが壊れていた `NameError`(古い `cdc_live` 参照)を修正しました。

## v0.1.55

### Changed

- **「Retry failed tables」で、再試行する失敗テーブルを選べるようになりました。**
  再試行ダイアログが各失敗テーブルをチェックボックス(すべて事前チェック済み)と失敗
  理由とともに一覧表示するので、まだ再試行の準備ができていないテーブル(例: まだ直して
  いないソース値、未解決の依存関係)のチェックを外し、残りだけを再試行できます。確認
  するとチェックされたサブセットだけを再試行し、「すでにデータあり」の読み取り専用
  プローブと Append/Drop の選択もそのサブセットにスコープされます。何もチェックされて
  いないと確認ボタンは無効になります。すべて再試行(最も一般的なケース)は従来どおり:
  すべてチェックしたまま確認します。テーブル単位の「Reload」ショートカットも従来どおり
  です。

## v0.1.54

### Changed

- **「Retry failed tables」とテーブル単位の「Reload」も、Start と同じ Drop vs Append
  の選択を提供するようになりました。** 以前は初回の Start Full Load でのみ選択でき、
  リトライはその選択を黙って再利用していたため、append で失敗した後にクリーンな
  リロードへ切り替えるには全体の Start over しかありませんでした。今はリトライと
  Reload も同じ読み取り専用プローブ(リトライ対象のテーブルのみ)を実行し、同じ確認
  ダイアログを開くので、リトライ時にも **Append** または **Drop & reload** を選べます。
- **Drop & reload の選択肢が「編集したスキーマが反映される」ことを明示します。**
  ダイアログに、drop & reload は各テーブルを **適用済みの Schema Conversion(変換画面
  で加えた編集を含む)** から再作成し、ロード後にセカンダリインデックスを再構築する、
  という説明を追加しました。スキーマ変更がクリーンリロードに反映されることが明確に
  なります(元々反映されていましたが、可視化しました)。

## v0.1.53

### Fixed

- **Full Load で、既にデータがあるテーブルに対して Drop か Append を選べるようになり、
  リトライもその選択を保持します。** 以前は、選択したターゲットテーブルに既に行がある
  場合ツールが勝手に判断し(初回実行で DROP+recreate)、**リトライは黙って append に
  戻り**、stale なデータの上に「0 new + N already there」と報告していました — 失敗した
  ロードが実際には何も更新していないのに、きれいに終わったように見えていました。今は
  Start Full Load ダイアログが実行ごとに一度尋ねます: **Append**(既存行を保持し、不足
  している行だけをロード — 冪等、デフォルト)または **Drop & reload**(各テーブルをまず
  DROP+recreate してクリーンにロード)。この選択は保存され、**リトライとテーブル単位の
  Reload も同じ挙動に従います**。
- **ビューがテーブルに依存していても「Drop & reload」が失敗しなくなりました。** 依存
  ビュー(例: `customer_order_summary`)が `DROP TABLE` を `DependentObjectsStillExist`
  でブロックし、古い行が残っていた問題を、drop パスが依存ビューを先にドロップし(ビューは
  並列ロードされる複数テーブルにまたがりうるため実行レベルの事前パス)、**ロード後に
  再作成** するようにしました。これによりクリーンなリロードが成功し、ビューも保持されます
  — 大雑把な `DROP … CASCADE` なしで。CDC ストリーミング中は抑制されます(DROP がライブ
  sink と競合するため)。

## v0.1.52

### Added

- **Full Load の失敗テーブルごとに AI Assist。** Full Load の「Failure details」
  リストの各テーブルに、「Reload」の隣に「AI Assist」ボタンが追加されました。押すと
  AI チャットドロワーが開き、その失敗の原因と対処方法を説明します — 実際のエラー
  テキスト(例: 依存ビューによる `DependentObjectsStillExist` のドロップ競合、または
  一時的な `InternalError_: server unavailable`)だけでなく、**この移行の状況**も
  理解します: 移行タイプ(Full Load のみ vs Full Load + CDC)、そのテーブルが既存
  ターゲットの DROP+再作成だったか、CDC がすでにストリーミング中か。そのため汎用的な
  回答ではなく、この移行に即した案内を行い、正しいリカバリ(スキーマ依存関係の修正、
  ソース値の修正、または一時的エラーなら Reload)を指し示します。オプトイン —
  Connect で AI Assist を有効にした場合のみ有効になり、無効時はそこへ誘導する
  無効化ボタンが表示されます。既存のチャットドロワー / Bedrock スタックを再利用
  します(新しい認証情報パスなし)。

## v0.1.51

### Fixed

- **再接続したセッションで「Check」を押しても Prerequisites セクションが閉じない
  ように。** アプリ再起動後は前提条件チェックを(再)実行する必要がある場合が
  あります。ところが Prerequisites セクションを展開して Check を押すと即座に
  閉じていました — クリックが再レンダーを引き起こすのに、セクションは「アクティブ」
  なサブステップのときだけ開いた状態で、再接続後はアクティブなステップがより後の
  ステップになるためです。今は、このセクションが対応の必要な状態である間(チェック
  実行中、または実行をまだブロックしている間)は展開されたままになり、実行中の
  スピナーと結果が表示され続けます。

## v0.1.50

### Changed

- **Schema Conversion の object browser を「Tables to migrate」と同じスタイルに統一。**
  ステップ 2 のソース/ターゲットブラウザが、ステップ 3 のテーブルピッカーと同じ
  見た目になりました: 白背景・枠線付きのスクロールパネルと、コネクタ線のない
  ツリー。各ソーステーブルのリーフに同じ主キーインジケーターが付きます(あれば緑の
  チェック、なければ琥珀色の警告 — Aurora DSQL は主キーが必須)。フィルターの下に
  凡例もあります。ビュー/トリガー/ルーチンには主キーインジケーターは付きません
  (主キーの概念がないため)。選択および DDL 生成の動作は変更ありません。

## v0.1.49

### Fixed

- **「Tables to migrate」のフィルターが動作するようになり、主キーアイコンに凡例を
  追加。** テーブルツリー上部の名前フィルター入力欄は表示されるものの、ツリーに
  バインドされておらず、入力しても何も絞り込まれませんでした。今はツリーのフィルター
  に接続され、入力すると一致するテーブルリーフだけに絞り込まれます。また、ヘッダーの
  下に各テーブルのアイコンを説明する小さな凡例を追加しました: 緑のチェックは主キー
  あり、琥珀色の警告は主キーなし(Aurora DSQL は主キーが必須)を意味します。

## v0.1.48

### Changed

- **「Tables to migrate」ピッカー: スキーマツリーに戻し、モダンなスタイルは維持。**
  フラットなデータテーブル(v0.1.47)を、スキーマ → Tables → リーフの
  object browser ツリーに戻しました。ただし同じ AWS/Cloudscape スタイルで包んで
  います — 名前フィルター、Select all / Unselect all、そして白背景・枠線付き
  スクロールパネル上のリアルタイムな「N / M 件選択」カウンター。各テーブルリーフ
  には主キーのインジケーターが小さく付きます(あれば緑のチェック、なければ琥珀色
  の警告 — Aurora DSQL は主キーが必須)。テーブルビューにあった他のメタデータ列
  (カラム数・インデックス・ターゲットバッジ)は、ツリーを軽く保つために削除しま
  した。主キー表示はクライアント側の Quasar スロットなので、ノードごとの追加負荷は
  ありません。選択の動作とロック(淡色化・操作不可)状態は変更ありません。

## v0.1.47

### Changed

- **「Tables to migrate」(移行するテーブル)ピッカーをコンパクトな AWS コンソール
  (Cloudscape)データテーブルに刷新。** ステップ 3 のテーブルピッカーは、スキーマ
  → Tables → テーブルリーフと続き、各階層にチェックボックスがあるツリーでした。
  今はチェックボックス列が 1 つ、テーブルごとに 1 行のソート可能なデータテーブルに
  なり、一目でより多くの情報が分かります: スキーマ、カラム数、主キーの有無(あれば
  緑のチェック、なければ琥珀色の警告 — DSQL は主キーが必須)、セカンダリインデックス
  数、そして「exists」/「new」のターゲット状態チップ。上部には名前フィルターと
  リアルタイムの「N / M 件選択」カウンターがあります。チェックボックスは減り、情報
  密度は上がりましたが、選択の動作は同じです — 選択したセットがそのまま Full Load /
  CDC / 前提条件チェックの対象になり、チェック実行後や CDC 稼働中はピッカーは
  引き続きロックされます(淡色化・操作不可)。

## v0.1.46

### Changed

- **アプリ再起動後の「前提条件を再実行」メッセージを分かりやすく改善。** データ
  移行の前提条件(prerequisite)をすでにクリアしていたものの、まだ Full Load を
  開始していない状態でアプリが再起動すると、初回ユーザーに表示されるのと同じ
  そっけない「まず前提条件チェックを実行してください」という文言で実行がブロック
  され、まるで進捗が失われたかのように見えていました。チェックは依然として再実行
  が必要ですが(読み取り専用であり、再接続時にソース接続が張り直されるため古い
  結果は信頼できません)、メッセージが状況を正しく説明するようになりました:
  「再接続しました — 前提条件チェックを再実行すると続きから再開できます。読み取り
  専用で短時間です。進捗は失われていませんが、チェック結果はアプリ再起動をまたいで
  は保持されません。」実際の初回ユーザーには従来どおりの文言が表示されます。
  (チェック合格後にのみ到達できる、永続化されたアクティブサブステップで両者を
  区別します。)

## v0.1.45

### Changed

- **パフォーマンスチューニングのコントロールをコンパクトな AWS コンソール
  (Cloudscape)フォーム風に刷新。** サイドバーの「Performance tuning」パネルは、
  数値入力を 4 つ並べるだけではなくなりました。まず運用上の注意事項を示す 1 行の
  info Alert(次回実行から適用、ライブ・アプリ全体、再起動でリセット、接続数 ≈
  テーブル数 × バッチ数)を表示し、その下にノブを「Full Load」/「Validation」の
  セクション見出しでまとめてフォームフィールドとして配置します。各ノブは 1 行で
  表示されます — ラベル、長い説明をツールチップに収めた info アイコン、許容範囲、
  範囲が制限された数値入力 — 狭いサイドバーでもパネルが窮屈にならないようにして
  います。ノブのメタデータ(グループ / ラベル / 説明 / 範囲)はすべて `config.py`
  にあり、UI と検証メッセージが単一の情報源を共有します。ノブの動作自体は変更
  ありません。

## v0.1.44

### Fixed

- **「Start Full Load」を二重クリックしても確認ダイアログが 2 つ開かないように。** 確認ダイアログを
  開く前に、どのターゲットテーブルに既にデータがあるかを調べる約 1〜2 秒の読み取り専用プローブが実行
  されます。素早く 2 回クリックするとダイアログが 2 回開いていました。今後はプローブ実行中の 2 回目の
  クリックを無視し(再入ガード)、クリックしたボタンは無効化され「Checking…」(砂時計)に変わり、
  ダイアログが開くと元に戻ります。初回の Start ボタンと終了後の Re-run ボタンの両方に適用されます。

## v0.1.43

### Changed

- **デプロイログのタイムスタンプに `UTC` タイムゾーンを表示。** CDC のデプロイ/削除ログの各行が
  `HH:MM:SS UTC - …` と表示されます(以前はタイムゾーンなしの `HH:MM:SS - …`)。曖昧さがなくなり、
  ダウンロードされるアクティビティログ・CloudWatch・CloudFormation イベント(すべて UTC)と一致します。

## v0.1.42

### Fixed

- **CDC スタック名フィールドの配置を修正。** 固定の `mysql-dsql-cdc-` プレフィックスを、別の左ラベル
  としてではなく Quasar の `prefix` prop で入力欄の**内側**(入力テキストと同じベースライン、金額の前の
  `$` のように)にレンダリングするようにしました。以前は別ラベルが入力欄自身のラベルと揃わずズレて
  いました。生成される完全なスタック名を示す 1 行のヘルプも追加しました。

## v0.1.41

### Changed

- **CDC スタック名フィールドをサフィックス専用にし、カスタム名が黙って拒否されないように。** 必須の
  `mysql-dsql-cdc-` プレフィックスは読み取り専用の固定アドオンとして表示され、ユーザーはサフィックスのみを
  入力します(例: `orders` → `mysql-dsql-cdc-orders`)。以前はプレフィックスなしの名前(例: `abcde`)を
  入力すると拒否されて `mysql-dsql-cdc-stack` に戻り警告が出ましたが — プレフィックスはデプロイロールの
  IAM スコープ上必須のため紛らわしいものでした。今後 `abcde` はそのまま有効な `mysql-dsql-cdc-abcde` に
  なり、文字規則に違反するサフィックスのみが拒否されます。

## v0.1.40

### Changed

- **「Start over」が CDC をプローブする間「Checking…」状態を表示。** Start over を開くと、CDC の
  stop/delete タイルを出すかどうかを判断するため、約 1〜2 秒の読み取り専用 AWS プローブが実行されます。
  今後はその間ボタンが無効化され、ラベル/アイコンが「Checking…」(砂時計)に切り替わり、ダイアログが
  開くと元に戻ります — 進行状態を可視化しつつ二重オープンも防ぎます。(アプリの慣例に合わせたラベル/
  アイコンの差し替え方式で、flat ボタンで枠のアーティファクトを生じる Quasar の `loading` prop は
  使用しません。)

## v0.1.39

### Fixed

- **CDC インフラがすでに存在しないのに Start over が「CDC 課金継続」と警告する問題を修正。** ライブ
  プローブが CDC インフラの不在を確認した場合(例: スタックの削除を終えた直後)、Start over ダイアログ
  は「リセットしても CDC インフラは削除されません — MSK/NAT の課金が継続します」という注意を表示
  しなくなりました。すでに撤去されたインフラについて誤解を与える文言でした。プローブが不確実な場合は
  (安全のためのヘッジとして)引き続き表示され、CDC が実際にデプロイされている場合は当然 stop/delete
  タイルの経路が代わりに表示されます。

## v0.1.38

### Changed

- **CDC カードがテアダウン中に「削除中」であることを明確に表示。** cdc-stack が
  `DELETE_IN_PROGRESS` の間、パイプラインカードは以前は曖昧な「Busy」/「cdc-stack needs cleanup —
  現在の操作の完了を待ってください」と表示されていました。今後は **「Deleting…」** バッジと安心できる
  通知 — *「CDC インフラを削除中です(約 15〜25 分 — VPC 内 Lambda のネットワークインターフェイスの
  detach に時間がかかります)。完了すると MSK / NAT の課金が停止します」* — を表示し続け、ポーリングを
  継続して完了時に自動的に「Not deployed」へ切り替わります。落ち着いたが行き詰まったスタック
  (`ROLLBACK_COMPLETE` / `DELETE_FAILED`)は引き続き「クリーンアップが必要 — 削除して再デプロイ」の
  案内を表示します。(新しい純粋ヘルパー `cdc_unstable_message` がバッジと通知を一元的に決定。)

## v0.1.37

### Fixed

- **「Start over」が進行中の CDC 削除と競合しないように。** Start over で CDC パイプラインの
  stop/delete を選ぶと、CloudFormation スタックは約 15〜25 分間 `DELETE_IN_PROGRESS` になりますが、
  その間ヘッダーの「Start over」ボタンはクリック可能なままで、リセットがすでにセッションを消去して
  いたため、2 回目の試行では進行中の削除を認識できませんでした(紛らわしく、カスタムスタック名の場合は
  MSK/NAT の課金が孤立するリスク)。今後は **CDC の stop/delete が実際に進行中の間は Start over を
  ブロック**します: ダイアログは削除が進行中であることを説明し、Close のみを提供します(RESET なし)。
  検出は狭く — ライブの `*_IN_PROGRESS` スタックステータス、または PENDING/RUNNING の stop/delete
  ジョブ — なので、落ち着いたが行き詰まったスタック(`ROLLBACK_COMPLETE` / `DELETE_FAILED`)は引き続き
  リセットして片付けられます。`run_cdc_delete` の already-deleting バックストップは変更ありません。

## v0.1.36

### Added

- **UI からの実行時パフォーマンスチューニング。** サイドバーフッターに **Performance tuning**
  コントロール(Diagnostics の隣)を追加し、Full Load / Validation の並列度 4 つ
  (`FULL_LOAD_TABLE_PARALLELISM`、`FULL_LOAD_BATCH_PARALLELISM`、`FULL_LOAD_BATCH_ROWS`、
  `VALIDATE_MAX_WORKERS`)を**再デプロイ・再起動なしで実行間に**再調整できます — ローダーと
  バリデータは実行ごとに設定を読み直すため、ここで設定した値が次の Full Load / Validation に
  反映されます。各フィールドは config と同じ上限で制限され(単一の情報源)、アプリ全体(単一タスク)に
  適用され、再起動でデプロイ/起動時の値にリセットされます。永続化したい値はタスク定義の
  `environment` に、実験にはこのコントロールを。

## v0.1.35

### Fixed

- **US 以外のリージョン(例: ソウル / ap-northeast-2)でも AI アシストをデプロイ可能に。**
  `BedrockModelId` デプロイパラメータが `us.` 推論プロファイルのみを許可し、タスクロールの
  `bedrock:InvokeModel` スコープを `"us."` で分割して US メンバーリージョン(us-east-1/2,
  us-west-2)にハードコードしていたため、US 外では AI を有効化できませんでした(`us.` 以外の ID は
  パラメータ検証で拒否され、派生した IAM スコープも他リージョンでは不正)。パラメータは `global.`
  プロファイル(全リージョンで利用可)も提供するようになり、foundation-model ID は `"anthropic."`
  で分割(すべての `us.`/`global.`/`apac.` プロファイル ID に存在)、foundation-model ARN は
  メンバーリージョンを列挙する代わりに**リージョン非依存(`*`、正確なモデル ID)** でスコープします。
  引き続き最小権限 — `*` はリージョンフィールドのみ、モデル ID は正確なまま。
- **CDC デプロイがデフォルトでソース DB への `0.0.0.0/0` egress を開かないように。** CDC インフラ
  デプロイ時にソース DB のセキュリティグループを自動検出(RDS `DescribeDBInstances`、読み取り専用)
  し、コネクタのソース向け egress をその SG にスコープします。ベストエフォート — 非 RDS ホストや
  権限不足の場合は空のまま(ドキュメント化されたフォールバック)。
- **CDC シンクのログ修正 + 未使用のインメモリ S3 CSV export の削除。** DLQ なしで恒久的に拒否された
  レコードは "logged and skipped" ではなく**タスクを失敗させる**(実際の動作)という文言に修正し、
  到達不能な「ファイル全体をメモリに載せる」S3 CSV export パスを削除(実際のパスはページ単位の
  ストリーミング)。

### Changed

- **デフォルトのコンテナイメージを `0.1.34` に更新。** app-stack のデフォルト `ContainerImageUri`
  が `0.1.31` を指したままで、新規デプロイが古いイメージを実行していた問題を修正。

### Docs

- **日本語マニュアル・ドキュメント**を追加し、マニュアル/README/デプロイガイド/変更履歴全体に
  英語 / 한국어 / 日本語 の 3 言語スイッチャーを追加。
- **韓国語マニュアルの自然さ改善**(文・用語の統一)、テスト章の書き直し、パフォーマンス章に実測例を追加。
- **アーキテクチャ図を PNG** として README に埋め込み(完全なトポロジーはクリックで拡大)、編集可能な
  `.drawio` ソースは共有しないように変更。
- **デプロイガイド**: AWS CLI 例に AI アシストをインラインで設定(`EnableAiAssist`/`BedrockRegion`/
  `BedrockModelId`)、Apache-2.0 `LICENSE` の著作権行を記入、内部作業ドキュメントをリポジトリから削除。

## v0.1.34

### Added

- **Query Playground の AI DBA クエリチューニング。** 変換された `SELECT` が
  「Test on target」を通過すると、新しい **Tune with AI DBA** アクションで共有 AI
  チャットドロワーが開き、Aurora DSQL の効率性に合わせてクエリを書き換えます。書き換えは、
  そのクエリの実際に取得された EXPLAIN プランと DPU コスト、および Aurora DSQL 自身の
  実行モデル(主キーが *そのまま* テーブルであること、3 つのフィルタ層を通したフィルタ
  プッシュダウン、`Full Scan` と `Index`/`Index Only Scan` の違い、コスト単位としての
  DPU)に基づいています。何を・なぜ変更したのか、そしてなぜ DSQL で安価になるのかを説明し、
  DSQL に当てはまらない素の PostgreSQL 向けチューニング助言からは明示的に遠ざけられます。
  各書き換え提案には **Test rewrite on target** アクションがあり、ターゲット上で読み取り
  専用として再実行し、AI が同じチャット内で実測した前後の DPU 改善を報告します。オプトイン
  (AI はデフォルトでオフ)であり、助言のみ — 何も自動適用はされず、改善の証拠はモデルの
  文章ではなく実測 DPU です。

## v0.1.33

### Fixed

- **「Start over」が、どの手順にいても、配備済みの CDC パイプラインの解体を確実に提案する
  ようになりました。** リセットダイアログは、検出された CDC 配備から stop/delete の選択肢を
  表示するかどうかを決めますが、その検出は CDC 手順を開いたときにしか更新されませんでした —
  そのため別の手順から(または再接続したセッションから)やり直すと、実際には CDC が配備されて
  いても、解体アクションのない受動的な「リセットしても CDC インフラは削除されません」という
  警告に後退することがありました。Start over は開いたときに読み取り専用の AWS プローブを実行
  するようになり、実際の配備状態を反映します。
- **解体は、稼働中のものだけでなく、任意の状態の CDC リソースに対して提案されます。** 失敗
  している/まだプロビジョニング中のコネクタ、スタックしたまたはロールバックされた cdc-stack、
  あるいはインフラのみのスタック(コネクタがまだ無い MSK クラスター + NAT)は、いずれも課金され
  続けますが、常に解体の対象として提案されるわけではありませんでした。いまや健全性ではなく存在
  そのものが提案を駆動し、CDC 手順(スタックしている/不安定なスタックに対して既に Delete を提供
  している)と挙動を合わせています。
- **カスタムの cdc-stack 名が Start over の警告に明示されます。** カスタムのスタック名で CDC を
  配備した場合(CDC 手順の「Advanced — CDC stack name」、例えば 2 つ目の並行マイグレーション用)、
  新しいセッションではそれを再発見できません(デフォルト名に戻ります)。警告が正確なスタック名を
  示すようになったので、(ツール内でも AWS コンソールでも)何を削除すればよいかを正確に把握できます。
- **CDC インフラの削除が、操作の途中にあるスタックに対して失敗確定の削除を送信しなくなりました。**
  CloudFormation の操作がまだ実行中の場合、削除がそれと競合して不透明に失敗することがありました。
  Delete は、操作が進行中のときは明確な「待って再試行してください」というメッセージで停止し(そして、
  すでに削除が進行中の場合は単にその完了を待ちます)、一方で安定・失敗・ロールバック済みのスタックは
  従来どおり削除します。

## v0.1.32

### Fixed

- **Validation のチェックサムが、NULL を含む行で誤って不一致になることがなくなりました。** 行単位の
  チェックサムは、NULL に対して `'\0'`(NUL)センチネルを使って列を連結していましたが、そのバイトは
  エンジンごとに異なって表現されます(MySQL では単一の NUL、PostgreSQL の
  `standard_conforming_strings`(DSQL のデフォルト)では 2 文字の文字列 `0x5C30`)。そのため NULL を
  含む行はソースとターゲットで異なるハッシュになり、誤った差分として報告されていました。センチネルは
  いまやプレーンテキストの `<NULL>` になり(PG のテキストで無効な NUL も回避)、同一のデータは両エンジン
  で同一にハッシュされます。
- **Validation のチェックサムが、バイナリ列と BIT 列で一致するようになりました。** MySQL は
  `BINARY`/`VARBINARY`/`BLOB`(および空間型)を生バイトとして表現する一方、ターゲット側は 16 進数を
  使い、`BIT` は生ビット対整数で比較されていました — どちらも、格納データが同一であってもエンジン間の
  不一致が必ず発生していました。いまやバイナリ列は両側で小文字の 16 進数としてハッシュされ(MySQL の
  `LOWER(HEX(…))` が PG の `encode(…, 'hex')` と一致)、`BIT(n)` はその整数値として比較されます
  (`CAST(… AS UNSIGNED)` 対 `::text`)。
- **範囲外の MySQL `TIME` 値が、ターゲット列を破損させる代わりに明示的に失敗するようになりました。**
  MySQL の `TIME` は `-838:59:59..838:59:59` の範囲を持ちますが、DSQL の `time` 列は
  `00:00:00..23:59:59.999999` しか保持できません。その範囲外の値には `time` 表現が無く、静かに
  interval(または time ではないテキストセル)としてバインドされ、列を破損させていました。Full Load は
  いまや、対象の列と値を明示し修正方法(Schema Conversion でターゲット型を `interval`/`text` に再マップ
  するか、ソース値を制限する)を示す明確な `ValueConversionError` を発生させ、既存の `TINYINT(1)` 範囲
  外ガードと同様に振る舞います — データが静かに壊されることは決してありません。

## v0.1.31

### Fixed

- **CDC のみの実行中に Validation に到達できるようになりました(もう「Complete Data Migration first」
  ではありません)。** Data Migration 手順は、完了した Full Load を経てのみ DONE に到達していたため、
  CDC のみのプラン — またはローカルで Full Load を一度も実行しなかった再接続セッション — では、CDC が
  ターゲットへ実際にレプリケートしているにもかかわらず Validation が永久にロックされたままでした。CDC が
  ストリーミング中のとき、Data Migration 手順は下流のゲーティングにおいて DONE として扱われるように
  なりました(新しい純粋関数 `data_migration_step_after_cdc`。昇格のみを行い、終端の DONE/FAILED を
  降格させることは決してありません)。

### Known issues

- **再接続した CDC のみのセッションで、オブジェクトブラウザが依然として「すべて選択」(ロック)を表示する
  ことがあります。** CDC が稼働中でも、このセッションに Full Load のウォーターマーク(watermark)も
  ローカルで確定したテーブル選択も無い場合(例: Connect から新規に開始した後に再接続)、ツールはローカルの
  状態から実際にストリーミングされているテーブル集合を解決できず、ロックされたブラウザは target-existing の
  デフォルトに後退します。これを完全に修正するには、CDC ステータスの検出中に、配備済みコネクタの実際の
  テーブル集合(`describe_connector`)をイベントループの外で読み取る必要があります — フォローアップとして
  追跡中です。(v0.1.30 で、ウォーターマーク/選択が既知である一般的なケースはすでに修正済みです。)

## v0.1.30

### Fixed

- **Data Migration のオブジェクトブラウザが、CDC 稼働中に「すべて選択」を表示しなくなりました。**
  ピッカーがロックされている(CDC ストリーミング中)とき、再接続すると汎用の「ターゲット上のすべて」という
  デフォルトに後退し、すべてのテーブルにチェックが付いていました — CDC が実際にレプリケートしている内容を
  誤って表示していました(しかもフリーズしているため修正もできません)。ロックされたブラウザは、いまや
  target-existing のデフォルトではなく、実際にストリーミングされている集合(CDC コネクタのテーブル集合、
  Full Load のウォーターマーク/確定した選択に基づく)を反映します。
- **Schema Conversion の「Apply to target」が、CDC 実行中はブロックされるようになりました。** 稼働中の
  CDC の最中にスキーマを適用すること — 特に、テーブルを DROP して再作成する破壊的な REPLACE — は、シンクが
  能動的に書き込んでいるテーブルを破損または切り詰め(Debezium は DDL を伝播しません)、データ損失/パイプ
  ライン破損のリスクがあります。一括適用とオブジェクト単位のインライン適用のいずれも、いまや警告とともに
  停止し、まず CDC を停止するよう操作者に伝えます。(アプリから注入された CDC ステータスのプローブで
  ガードされます。利用できない場合、適用は影響を受けません。)

## v0.1.29

### Added / Changed

- **Schema Conversion: ソースおよびターゲットの DDL をワンクリックでコピー。** 各 DDL ブロックに
  クリップボードへのコピーアイコンが追加されました — 横並びのソース/ターゲット差分(各サイドのヘッダー
  バー内)と、編集不可のビュー/トリガー/ルーチンのプレビュー(各「Source DDL」/「Target DDL」ラベルの隣)の
  両方で。コピーは肯定的なトースト表示で確認され、ブラウザのクリップボードが利用できない場合(例: 非 HTTPS)は
  「ブロックから選択してコピー」という落ち着いた案内に後退します。

## v0.1.28

### Fixed

- **CDC の解体が、offset-seeder Lambda の残存 ENI によってブロックされた `DELETE_FAILED` スタックを
  自動で回復します。** offset-seeder は VPC 内で実行されます(そうする必要があります — MSK Serverless の
  ブートストラップは VPC プライベートなので、VPC の外にあるものはギャップの無い seed レコードを生成できません)。
  そして VPC Lambda は AWS 管理のハイパープレーン ENI を残し、AWS はそれを非同期にのみ回収します(数分から
  数十分)。それらが残っている間は、コネクタのサブネット/セキュリティグループの削除が失敗し、スタック全体が
  `DELETE_FAILED` に陥ります — 従来はこれが行き止まりで、CLI から ENI を手動で削除し delete-stack を再実行する
  必要があり(このセッションで繰り返し発生)、その間 MSK/NAT は課金され続けました。`run_cdc_delete` はいまや
  `DELETE_FAILED` を検出し、失敗したサブネット/SG を固定している残存の *デタッチ済み*(`available`)ENI を削除
  し、削除を再発行(依然としてスタックしているものは保持)して解体を完了させます。使用中の(まだ回収中の)ENI は
  そのままにされ、全工程を通じてベストエフォートです。(これは offset-seeder ENI の既知の問題に対する現実的な
  解決策です。Lambda を VPC の外に移せないため、ツールが解体を自己修復するようになりました。)

## v0.1.27

### Fixed

- **CDC の配備が、行き止まりになる代わりに、動けなくなった `UPDATE_ROLLBACK_FAILED` の cdc-stack を自動で
  回復します。** コネクタの `UpdateConnector` が失敗すると、コネクタが RUNNING でない状態のまま残り、そのうえ
  CloudFormation 自身のロールバックもそのリソース上で失敗し(「only valid for RUNNING」)、スタックが
  `UPDATE_ROLLBACK_FAILED` に留まります — この状態からはそれ以上の更新を送信できません(従来は CLI から手動で
  `continue-update-rollback` が必要でした)。`discover_stack` はいまやその状態を検出し、スタックしたリソースを
  スキップしながらロールバックを継続するため、スタックは `UPDATE_ROLLBACK_COMPLETE` に戻り、次の Start/Retry が
  進行します。ベストエフォート: 回復呼び出し自体がエラーになった場合は、通常の「安定した状態ではありません」という
  エラーが表面化します。

## v0.1.26

### Fixed

- **CDC UI: 「テーブルが選択されていません」ガードを表面化し、再試行が Prerequisites に戻ってしまうのを
  止めます。** v0.1.25 のバックエンドガードに続き、CDC 手順はいまや明確な「少なくとも 1 つのテーブルを選択して
  ください」という通知を表示し(設定プレビューがクラッシュしたり、数分後にコネクタ作成で配備が失敗したりする
  代わりに)、Start CDC もジョブを送信する前に同じメッセージでブロックします。早期の「インフラをプロビジョニング
  する」配備は、まだコネクタを作成しないため、空の選択を依然として許可します(`build_sink_config(..., allow_empty=True)`) —
  `SinkTopics` は Start CDC 時に埋められます。
- **コネクタが配備された後、再試行/再描画で CDC サブ手順が Prerequisites に折りたたまれることがなくなりました。**
  アクティブなサブ手順のリゾルバには「cdc」を永続化する場所が無かったため、あらゆる再描画(CDC の再試行、再接続)は
  full_load/prerequisites に後退し、ユーザーを稼働中の CDC ビューから引き離していました。プランに CDC が含まれ、
  コネクタが存在する場合、CDC サブ手順はいまやピン留めされ永続化されます。

## v0.1.25

### Fixed

- **CDC の開始が、テーブルが選択されていないときに、壊れたシンクを配備する代わりに即座に失敗するようになりました。**
  `build_sink_config` はテーブルリストが空だと例外を投げます: Kafka Connect のシンクは空でないトピックリストを
  必要とするため、空の選択は `SinkTopics=""` を生成し、MSK Connect は配備開始から数分後に `POST /connectors` で
  不透明な HTTP 400 を返してコネクタを拒否していました(v0.1.24 のノートを参照)。このガードは、それを、遅く課金の
  発生する配備が試みられる前の早期で実行可能なエラー(「少なくとも 1 つのテーブルを選択してください」)に変えます。
  (*ソース* 側の設定は変更ありません — 空の `table.include.list` はそこでは有効で「すべてのテーブル」を意味します。)

## v0.1.24

### Fixed

- **CDC コネクタの配備: コネクタが実際に RUNNING に到達するよう、CdcDeployRole / タスクロールの IAM を完成させ
  ました。** MSK Connect コネクタの作成は一連の権限を必要とし、それらが少しずつ欠けていて、追加するまではそのたびに
  コネクタの CREATE が失敗していました(または UI がスタックしていました)。稼働中の cdc-stack に対してエンドツー
  エンドで検証済み — Debezium ソースコネクタがいまや RUNNING に到達します。追加した権限:
  - **CdcDeployRole** に `ec2:CreateNetworkInterface` / `DescribeNetworkInterfaces` / `DeleteNetworkInterface` —
    MSK Connect はコネクタの ENI を *呼び出し元の* 資格情報で配置します(CloudTrail で確認: `CreateNetworkInterface` は
    `kafkaconnect.amazonaws.com` によって呼び出されるが、権限付与は配備ロールに対して行われる)。コネクタの
    ServiceExecutionRole でも MSK Connect のサービスリンクロールでもありません。(cdc-stack の
    `ConnectorExecutionRole` に誤って追加された ENI 付与は削除しました — サービス実行ロールには不要です。)
  - CdcDeployRole に CloudWatch Logs の *delivery* アクション(`logs:CreateLogDelivery`、`ListLogDeliveries`、
    `PutResourcePolicy`、…) — コネクタは CloudWatch のワーカーログ配信を有効化し、これは配備ロールを使って
    vended-logs delivery API 経由で設定されます。これらが無いとコネクタは `InvalidInput.WorkerLogsError` で FAILED になり、
    ワーカーログは一切書き込まれませんでした。
  - CdcDeployRole に `kafkaconnect:DescribeConnectorOperation` / `ListConnectorOperations` を、`connector/*` と
    `connector-operation/*` の **両方の** ARN 形状にスコープして付与 — UpdateConnector は非同期であり、そのポーリングは
    どちらの ARN に対しても権限付与され得ます。両方が無いと CDC の再試行がスタックをロールバックさせていました。
  - **タスクロール** そのものに `kafkaconnect:ListConnectors` / `DescribeConnector` — アプリはコネクタの状態を
    ポーリングして CDC UI を駆動します(そしてソースパスからシンクパスへ進めます)。これが無いと AccessDenied が
    静かに握り潰され、実際には RUNNING のコネクタが永遠に「creating…」と表示されていました。
- **DSQL シンクコネクタが RUNNING に到達 — source→MSK→sink→DSQL の完全なパイプラインがエンドツーエンドで検証
  されました。** IAM/インフラが完成した後、シンクは `POST /connectors` で HTTP 400 で失敗していました。根本原因は
  **空の `SinkTopics`** パラメータでした(Kafka Connect のシンクは `topics`/`topics.regex` を必要とするため、空の値は
  登録時に拒否されます)。`SinkTopics` が空だったのは、2 パスの Start がそれを埋めなかったためです(下記の UI 既知の
  問題を参照)。それを `<TopicPrefix>.<db>.<table>` に設定すると、シンクコネクタが作成され稼働します。

### Known issues

- **UI: 「Retry CDC」が配備を実行せずにビューを Prerequisites にリセットすることがあり、** source→sink の 2 パスが
  長いスタックのクリーンアップの後に再開されず、テーブル選択をスキップした Start は `SinkTopics`/`TableIncludeList` を
  空のまま残します(ソースはそれを許容し — すべてのテーブルをキャプチャ — しますが、その後シンクは `POST /connectors` で
  HTTP 400 で失敗します)。フォローアップの UX/ガードレール対応では、テーブルが選択されていないときに CDC の開始を
  ブロックし、空のトピック状態をコネクタ作成時ではなく配備前に表面化すべきです。
  _更新: 空テーブルでの開始はいまやブロックされ、再試行時に CDC ビューが保持されます(v0.1.26)。長いクリーンアップの後の
  2 パス再開が残りの部分です。_

## v0.1.23

### Added / Changed

- **「CDC を開始する前に」という通知が、より親しみやすく、より適切なタイミングになりました。** いまや、どのテーブルが
  ストリーミングされるかを Start ボタンのすぐそばで表示します(例: 「Will stream 3 tables: …」)。そのため「選択を確定して
  ください」を、ユーザーに上へスクロールさせる代わりに、ひと目で確認できます。MSK 容量に関する注意は、新規配備後の最初の
  開始(ハッピーパス — 警報なし)では落ち着いた info のヒントとして表示され、コネクタが実際に以前に存在したことがある場合
  (以前の start/stop、または復元された実行)にのみ warning に昇格します。それは、繰り返しの作成/削除が MSK の返却され
  ない容量を実際に消費し始める時点だからです。文言も「partition quota … exhaust … force a full teardown」ではなく、平易な
  言葉(「MSK の限られた容量であり、再び解放されることはありません」)にしました。

## v0.1.22

### Fixed

- **CDC コネクタの配備が「Access denied for operation 'AWS::KafkaConnect::Connector'」で失敗しなくなりました。**
  `kafkaconnect:CreateConnector` はリソースレベルのサポートがありません(作成時点でコネクタの ARN は存在しません)が、
  CdcDeployRole はそれを `connector/mysql-dsql-cdc-*` の ARN にスコープしていたため、DebeziumSourceConnector の CREATE が
  拒否されていました。それ(および作成時の `TagResource`)は、いまや兄弟の CreateCustomPlugin / CreateWorkerConfiguration と
  同様に `Resource: "*"` に付与されます。他のコネクタ操作はスコープされたままです。
- **CDC コネクタの配備が「not authorized to perform ec2:CreateNetworkInterface」で失敗しなくなりました。** MSK Connect は
  コネクタの ServiceExecutionRole を assume してコネクタのサブネットに ENI を配置しますが、そのロール(cdc-stack の
  `ConnectorExecutionRole`)には EC2 ネットワークインターフェイスの権限がありませんでした。MSK Connect の `EC2NetworkAccess`
  セット(`ec2:CreateNetworkInterface` / `DescribeNetworkInterfaces` / `DeleteNetworkInterface` + attach/detach/permission、
  `Resource: "*"`)を追加し、コネクタが自身の ENI を作成/クリーンアップできるようにしました。(この 2 つは潜在的でした —
  それ以前の CDC の失敗はコネクタの CREATE 段階より前で止まっていたため、コネクタは実際に一度も作成されたことがなかった
  のです。)

### Added / Changed

- **Full-load-only の実行が完了した後、Full Load 手順が CDC を提案するようになりました。** Full-load-only のマイグレーション
  には CDC フェーズがない(「Continue to CDC」ボタンがない)ため、完了時に info の通知が継続的レプリケーションの追加方法を
  説明します: マイグレーションタイプを「CDC only」に変更する(この Full Load のウォーターマークから、すでにロード済みの
  ターゲットへストリーミングし、再スナップショットはしません)。CDC インフラをまず配備する必要があるかもしれない点も添えます。

## v0.1.21

### Added / Changed

- **Migration Plan は、3 択のマイグレーションタイプのタイル一式の代わりに、単一の「Include CDC?」という質問を尋ねる
  ようになりました。** この手順の唯一の永続的な効果は、CDC ストリーミングインフラ(MSK、約 15〜20 分)を早期にプロビジョニング
  するかどうかなので、コミットメントを誇張するのではなく、まさにそれ(Yes / No)を尋ねます — タイプは Data Migration で
  自由に変更でき、Full Load は常に実行されます。No → `FULL_LOAD_ONLY`、Yes → `FULL_LOAD_AND_CDC`。より細かい Full Load + CDC
  対 CDC のみの選択は Data Migration 手順に残ります(Yes を再選択しても CDC のみの選択を上書きしなくなりました)。基盤となる
  `migration_type` の enum、サブ手順、prerequisite、セッションのスナップショットは変更されていません。
- **「Migration type:」バナーは Migration Plan 手順では非表示になりました**(それ以降のすべての手順では連続性のために引き続き
  表示されます)。プラン手順では「Include CDC?」コントロールが真実の源であるため、2 値の決定の上に 3 値のバナー(「Full load +
  CDC」)があるのは冗長で、矛盾しているように読めました。

## v0.1.20

### Fixed

- **Aurora DSQL への接続が、IPv4 のみの Fargate タスクでタイムアウトしなくなりました。** DSQL クラスターのエンドポイントは
  デュアルスタックです(DNS は A レコードと AAAA レコードの両方を返します)が、IPv4 のみのサブネット/ENI(IPv6 CIDR なし、
  IPv6 SG の egress なし)上の Fargate タスクは IPv6 アドレスへの経路を持ちません。glibc は AAAA を先に返すことがあり、
  ドライバ(psycopg/libpq)は到達不能な IPv6 上で `connect_timeout` までブロックし、IPv4:5432 は到達可能であるにもかかわらず、
  UI には「Connection failed: connection timeout expired」として表面化していました。コンテナイメージはいまや、すべての外向き
  名前解決で IPv4 を優先するようになったため(`/etc/gai.conf`: `precedence ::ffff:0:0/96 100`)、`getaddrinfo` は到達可能な
  IPv4 アドレスを先に返し、接続は成功します。本物のデュアルスタックタスクでも無害です(単に IPv4 が先に試されるだけです)。
- **CDC のソースシークレットの再プロビジョニングが、解体後に AccessDenied で失敗しなくなりました。** タスクロールの
  `provision-cdc-source-secret` ポリシーには `secretsmanager:RestoreSecret` が欠けていましたが、upsert は、以前の解体が削除を
  スケジュール(復旧ウィンドウ)した同名のシークレットを、新しい値を書き込む前に復元します。削除後の CDC ソースシークレットの
  再プロビジョニングがいまや成功します。このアクションは `mysql-dsql-migrator/cdc/*` のプレフィックスにスコープされたままです。

### Added / Changed

- **デプロイガイド + スタック詳細フォームの明確化。** 「Specify stack details」は、いまや必須フィールドの表と自己署名証明書の
  1 行コマンドで始まります。デスクトップブラウザのアクセス用の組み合わせ(`AlbScheme=internet-facing` + パブリックな
  `AlbSubnetIds` + `AllowedIngressCidr=<your-ip>/32`)が明記され、`HttpsEgressCidr` は「`0.0.0.0/0` のデフォルトを維持する」
  (PrivateLink のときのみ絞る)と文書化されました。`ServiceSubnetIds` のガイダンスには、VPC にプライベート/NAT サブネットが
  ない場合は ALB のサブネット + `AssignPublicIp=ENABLED` を再利用できると記載しています。

## v0.1.19

### Fixed

- **Validation が、完了した実行を「in progress」(その後リフレッシュすると「not started」)と表示しなくなりました。**
  IN_PROGRESS→DONE への切り替えは Validation 画面でのみ動作するポーリングタイマーによって駆動されるため、実行の途中で
  別の画面へ移動する(例: Data Migration へ)と、ジョブが終了した後も手順が IN_PROGRESS のままスタックし、その後、
  孤立したステータスの照合が完了レポートを「not started」として破棄していました。いまや、実行が実際に終了した
  (レポートが存在する)のに手順が古い IN_PROGRESS で稼働中のジョブが無い場合、**DONE** に照合し、レポートを表示します。

### Added / Changed

- **CDC ライフサイクル + コネクタの状態遷移のアクティビティログ記録。** コントロールプレーンのアクション(CDC インフラの
  deploy / start / stop / delete)およびコネクタの RUNNING/FAILED の遷移は、いまや個別のマイルストーンとしてアクティビティログに
  追記されます(重複排除あり。連続的なラグ/スループットはログではなく、ライブパネルに残ります)。
- **Cut over: 「Steps to cut over」の 1〜6 のランブックが大きくなり、読みやすくなりました**(重要なガイダンスが小さすぎ
  ました) — cut-over のランブックのみにスコープしています。
- **デプロイガイド: 完全な解体順序。** Teardown セクションはいまや、完全な廃止シーケンスを列挙します — コストのかかる
  **cdc-stack** を最初に削除し(「Start over → Delete all CDC infrastructure」経由、または手動の `delete-stack`)、次に
  app-stack、その次に build-stack、そして `mysql-dsql-*` のスタック / Route 53 レコード / ビルドバケットが残っていないことを
  検証します — リソースやコストが後に残らないようにするためです。

## v0.1.18

### Fixed

- **Full Load の再実行が、「Full load + CDC」パターンであっても、CDC が開始する前であれば確定したテーブルを DROP +
  再作成するようになりました。** DROP + 再作成は、パターンが Full-load-+-CDC であるときは常に無効化されていました(そのため、
  CDC 開始前の「Re-run all tables」は、新規にリロードする代わりに冪等にマージし、以前の行を「すでにそこにある」ものとして
  残していました)。この抑制は、いまや CDC が **実際にストリーミングしているか** に応じてゲートされます: CDC 開始前の再実行は
  確定したテーブルを DROP + 再作成し(クリーンなリロード)、能動的にストリーミングしている CDC パイプラインのみが、稼働中の
  シンクとの競合を避けるために安全な冪等の `SKIP_EXISTING` ロード(DROP なし)を強制します。Start-Full-Load の確認は、DROP が
  実際に行われるとき(CDC が稼働中でない)にのみ「will be DROPPED」警告を表示します。(DROP なしのリロードでは行が重複することは
  決してありません — それは `INSERT ... ON CONFLICT (PK) DO NOTHING` です — が、ソースから削除された行が残る可能性があります。
  クリーンなリロードはその曖昧さを取り除きます。)

## v0.1.17

### Fixed

- **「Start / Re-run Full Load」の確認ダイアログが、数秒後に消えなくなりました。** それは定期的に再描画されるコンテンツの
  内部に構築され、ワンショットのフラグ経由で開かれていたため、約 1.5 秒ごとの進捗ポーリングの再描画が、表示された直後に
  それを破棄していました。いまやオンデマンドでトップレベルのクライアントコンテキストで作成・オープンされるため、
  Confirm または Cancel するまで表示されたままになります。

## v0.1.16

### Fixed

- **Full Load の再実行が、カスタマイズされたターゲットスキーマを元に戻さなくなりました。** オブジェクト単位の
  **編集されたターゲット DDL**(例: `TINYINT(1)` → `smallint` の再マップ)は、いまや永続的なセッションのスナップショットに
  永続化され、再接続/再起動時に復元されます。以前は編集がメモリ上にのみ存在していたため、再起動後に「Re-run all tables」を
  実行すると、決定論的な変換からテーブルを再作成し(例: `smallint` を `boolean` に戻す)、範囲外の値が再びロードに失敗して
  いました。再実行の DROP + 再作成は、いまやカスタマイズされた DDL を使用します。

> 注: 復元はセッション ID で照合されるため、再起動をまたいでセッション(とその編集)を安定させるには
> `DSQL_MIGRATOR_STORAGE_SECRET` を設定してください。コンテナの再配備は新しい一時ストレージを使用するため、
> 再配備後は編集を再適用してください。

## v0.1.15

### Fixed

- **Schema Conversion: 「Apply to target」が、いまや REPLACE の確認を確実に表示します。** 確認ダイアログはオブジェクト単位の
  エディタの(ネストされた)スロット内に構築されていたため、ページオーバーレイとして描画されないことがしばしばあり — ボタンが
  無反応に見えていました。いまやトップレベルのクライアントコンテキストで作成され、常に表示されます。
- **Schema Conversion: 遅い適用が「parent slot deleted」でクラッシュしなくなりました。** await 後の UI フィードバック
  (notify / refresh)は、いまや元のクライアントに再入し、ベストエフォートであるため、遅い適用の最中に破棄されたスロットが
  例外を発生させることはありません。
- **UI のバージョン(右上)が、実際にリリースされたバージョンを反映します。** `__version__` は、ハードコードされた値ではなく、
  インストール済みパッケージのメタデータから読み取られるため、ビルドされた各イメージがその真のバージョンを表示します。

### Added / Changed

- **Schema Conversion & Data Migration: Select all / Unselect all** を両方のオブジェクトブラウザに追加し、素早い一括選択を
  可能にしました。
- **Schema Conversion: 「Generate DDL for selected」が生成後にロックされ**、「Reset all」の後に再度有効になるため、再生成が
  常に明白になります(2 度目のクリックが、同じスコープを静かに再実行することがなくなりました)。
- **Data Migration: より明確な事前選択のキャプション** — いくつのテーブルがなぜ(すでにターゲット上に存在するため)事前選択
  されているかを、Select all / Unselect all コントロールとともに示します。
- **隔離(quarantine)された行は、テーブルの失敗として扱われるのではなく、再フレーミングされます。** ロードは成功したが、DSQL の
  ハード制限が拒否した行(例: 値あたり約 1 MiB の制限を超える値)を恒久的にドロップせざるを得なかったテーブルは「Done — quarantined」
  (アンバー)として表示され、実際の再試行可能な失敗(赤)とは区別されます。
- **テーブル単位の Reload。** ちょうど 1 つのテーブルに対して Full Load を再実行します(DONE のものでも) — 例: 過大なソース値を
  修正して、以前に隔離された行がロードされるようにした後 — 他のテーブルはそのまま維持します。
- **隔離された行を受け入れて続行(CDC のオーバーライド)。** Full Load が、恒久的に隔離された行のためだけに未完了である場合、その
  ギャップを承知したうえで、再実行せずに CDC のブロックを解除できます。ギャップは依然として Validation で報告されます。再試行可能な
  実際の失敗は依然としてブロックします(オーバーライドが復旧可能な失敗を隠すことは決してできません)。

## v0.1.14

### Fixed

- **Schema Conversion: 編集が、いまや REPLACE 経由で確実に適用されます(以前はまだスキップされることがありました)。**
  v0.1.13 は自動 REPLACE を、古かったり利用できなかったりする可能性のある UI 側の存在チェックにゲートしていたため、編集された
  オブジェクトが依然として「SKIPPED — already existed; left unchanged」として戻ってくることがありました。編集されたオブジェクトの
  適用は、いまや常に REPLACE の確認を経由します(REPLACE の `DROP ... IF EXISTS` は、まだ存在しないオブジェクトも安全に扱います)ので、
  確認すると編集が反映されます。
- **Schema Conversion: 適用が、開いていた Generated-DDL パネルを折りたたまなくなりました。** 適用後の再描画は、いまやすべてを
  折りたたむ代わりに、各展開の開/閉の状態をオブジェクトごとに保持します。

### Notes

- UI の修正。`:0.1.14` イメージで出荷されます。

## v0.1.13

### Changed

- **Schema Conversion: すでに存在する編集済みオブジェクトの適用が、静かにスキップする代わりに、いまや REPLACE(確認付き)を
  使用します。** 以前は、変換された DDL を編集(例: 列の型を再マップ)してデフォルトの SKIP モードで「Apply to target」をクリックしても、
  すでに存在するターゲットオブジェクトは手つかずのまま残され — 編集が静かに反映されず、唯一のフィードバックは短い SKIPPED トーストのみ
  だったため、「何も起きなかった」ように見えていました。オブジェクト単位の適用は、いまや既存オブジェクトへの編集を検出し、それを
  REPLACE の確認ダイアログ(「DROP して再作成 …」)経由でルーティングするため、確認すると変更が実際に反映されます。編集されていない
  既存オブジェクトは依然としてスキップされ(冪等)、まだ存在しない編集済みオブジェクトは通常どおり作成されます。

### Notes

- UI/挙動の変更。`:0.1.13` イメージで出荷されます。

## v0.1.12

### Changed

- **DSQL がサポートしないソース列は、いまや `bytea` として保存されます — ブロックされたり静かに NULL 化されたりすることは
  決してなく — Full Load と CDC の両方にわたって。** MySQL の空間列(geometry/point/…)を持つテーブルは、以前は Schema Conversion で
  全体が失敗していました(UNSUPPORTED の読み取り専用コメントのプレースホルダー)。いまや:
  - **Schema Conversion** は空間列を `bytea` にマップし、実際の編集可能な `CREATE TABLE` を生成します(MANUAL に分類され、
    「生バイト(WKB)として保存」のノート付き)。それを `text`(WKT)に編集する、列をドロップする、または `bytea` のまま保つことが
    依然として可能です。
  - **Full Load** は列を `ST_AsBinary(col)` 経由で読み取ります → WKB バイト → `bytea`。
  - **CDC**: カスタムの DSQL シンクが Debezium の geometry 論理型(`io.debezium.data.geometry.Geometry`/`Geography`/`Point`)を
    その WKB バイトに変換します → `bytea` — Full Load が書き込むのと **同じバイト**です(FL/CDC の一致。SRID は両方の経路で
    ドロップされ、プレーンな WKB)。予期しない形状はそのままバインドされるため、DLQ へと明示的に失敗します — 静かに NULL 化される
    ことは決してありません。
  - 共有の write contract(`converter.DSQL_WRITE_CONTRACT_CASES`)が geometry → `bytea` を記録するため、Full Load(Python)と
    CDC(Java)の書き込み経路が歩調を合わせたままになります。

### Notes

- CDC の geometry 処理が稼働中のパイプラインに反映されるには、DSQL シンクコネクタのプラグインを再ビルド/再公開する必要があり、
  次のイメージ + プラグインのビルドで出荷されます。

## v0.1.11

### Changed

- **Full Load の値変換が、いまや適用されたターゲットスキーマに従います。** 値変換器は以前、各列のターゲット型を *ソース* の
  MySQL 型から再導出していたため、Schema Conversion で再マップされた列(例: `TINYINT(1)` → `boolean` ではなく `smallint`)は無視され、
  0/1 でない値がテーブル全体を失敗させていました。Full Load はいまや、各値を *適用された* ターゲット型(変換/編集された DDL から
  パースされる)に合わせて変換するため、`smallint`/`integer` に再マップされた列は 0/1 でない値を整数としてロードします。本物の
  boolean 列は影響を受けません。
- **fresh/replace の再ロードが、カスタム再マップされたターゲットスキーマを保存します。** fresh-load の再作成ステップは、いまや
  決定論的な再導出ではなく、適用された(編集された)DDL から DROP + 再作成するため、フルの再ロード時にユーザーの再マップが静かに
  上書きされることはありません。

### Fixed

- boolean の値変換の競合メッセージが、いまやユーザーに、Schema Conversion で列のターゲット型を `smallint`/`integer` に再マップし
  (いまや有効)、テーブルを再試行するよう案内します(ソース側の変更のみを提案していたのから改善しました)。

### Notes

- 新しいコンテナイメージはまだ公開されていません(v0.1.10 とバッチにしています)。ローカルでは UI を再起動して取り込んでください。
  ECS では次のイメージビルドで出荷されます。

## v0.1.10

### Fixed

- **Schema Conversion のプレビュー: 自動変換できないオブジェクトは「Unsupported」とラベル付けされ、「Apply to target」ボタンを
  表示しません。** 特定のプレースホルダー(例: MySQL の空間型)を持つテーブルは、以前は単に「N warning(s)」として表示され、編集可能な
  ままで、Apply ボタンが提供されていました(押しても no-op / SKIP)。プレビューはいまや、(1) オブジェクトのヘッダーに変換の重大度
  (「Unsupported」/「Review needed」)を表面化し、(2) 汎用の未変換ノートだけでなく、**すべての** 非 `CREATE` プレースホルダーを未自動変換
  として扱います: 読み取り専用で、再設計の理由と AI 提案のオプションとともに表示され、適用の対象として提供されることは決してありません。
  これは、適用の経路でそのようなオブジェクトを既に SKIP する v0.1.9 を補完します。

## v0.1.9

### Fixed

- **Schema Conversion: 自動変換できないテーブルは、いまや FAILED ではなく SKIPPED になります。** 変換器が自動変換できなかった
  テーブル(例: MySQL の空間/geometry 列を持つもの。Aurora DSQL には対応する型がありません)を適用すると、変換器がそれに対して
  (`CREATE` ではなく)コメントのプレースホルダーを出力するため、紛らわしい `SchemaApplyError: target DDL must be a CREATE
  TABLE/VIEW/MATERIALIZED VIEW/INDEX statement` が発生していました。そのようなテーブルは、いまや再設計の理由(その評価に一致)とともに
  **SKIPPED** として報告され、適用器に送られることは決してありません。他の選択されたテーブルは通常どおり適用されます。

## v0.1.8

### Fixed

- **CDC の offset-seeder(ギャップの無い Full Load → CDC の引き継ぎ)が、いまや配備できます。** CDC が Full Load のウォーターマーク
  (`SeedOffset`)とともに配備されると、cdc-stack は VPC 内の offset-seeder Lambda とその独自の IAM ロールを作成し、それはカスタム
  リソースによって呼び出されます。assume された `CdcDeployRole` にはこれを行う権限が欠けていたため、配備は `AccessDenied` で失敗して
  ロールバックしていました。`CdcDeployRole` に追加:
  - `function:mysql-dsql-cdc-*` に対する `lambda:*` のライフサイクル(`CreateFunction`/`DeleteFunction`/`InvokeFunction`/…);
  - IAM ロール管理のスコープを `*-ConnectorExecutionRole-*` から `role/mysql-dsql-cdc-*` に拡大し、自動命名される offset-seeder
    ロールもカバーするようにしました;
  - `lambda.amazonaws.com` への `iam:PassRole`(MSK Connect に加えて)。
- **CDC インフラ: MSK Serverless クラスターの作成。** MSK Serverless クラスターの作成は呼び出し元の資格情報で VPC を検証するため、
  assume された `CdcDeployRole` は `ec2:DescribeVpcAttribute`(および `ec2:DescribeAvailabilityZones`)も必要とします。これらが無いと
  配備は `You are not authorized to perform DescribeVpcAttribute` で失敗し、ロールバックしていました。
- **CDC インフラ: コネクタロールの作成 + ロールバックのクリーンアップ。** `logs:DescribeLogGroups`(CloudFormation が `!GetAtt` の
  ために LogGroup の `Arn` を解決する際に呼び出します)にはリソースレベルのサポートが無いため、いまやコネクタのロググループに
  固定されるのではなく、アカウント/リージョンのロググループにスコープされた独自のステートメントになりました。そして MSK Serverless
  クラスターの削除には `kafka:DeleteCluster` が必要です(`DeleteClusterV2` は存在しません) — これが無いとロールバック/解体が
  クラスターを孤立させたままにしていました。
- **不要になった Glue Schema Registry の権限を削除** しました: パイプラインは(v0.1.5 以降)組み込みの JSON コンバータを使用し、Glue
  レジストリを作成しないため、`glue:*` の付与は未使用でした。

### Notes

- デプロイテンプレートのみ(app-stack の IAM)。**コンテナイメージの変更はありません** — 公開されている `:0.1.7` イメージは変更されず、
  デフォルトのままです。

## v0.1.7

### Fixed

- **CDC インフラが、いまや配備されます(cdc-stack)。** assume された `CdcDeployRole` 経由での cdc-stack の配備が、IAM 権限の不足と
  テンプレートのバグにより失敗してロールバックしていました。修正:
  - `CdcDeployRole` の IAM: 特大のテンプレートをプラグインバケットにステージング(`s3:PutObject`/`GetObject`);MSK Connect の
    プラグイン + ワーカー設定のタグ権限(`kafkaconnect:TagResource`/`ListTagsForResource`/`UntagResource`)を、作成アクション
    (リソースレベルのサポートが無い)に対して `Resource: "*"` で付与;そして VPC エンドポイントの権限(`ec2:CreateVpcEndpoint`、…)。
  - `cdc-stack.yaml`: 無効な `!GetAtt ConnectorS3Endpoint.PrefixListId`(`AWS::EC2::VPCEndpoint` にはそのような属性がありません)を
    削除し、EC2 の 256 文字未満 / 制限された文字セットのルールを満たすためにセキュリティグループのルールの説明を短縮しました。

### Changed

- デフォルトの `ContainerImageUri` → 公開されている `:0.1.7` イメージ。

> 注: CDC の **インフラ** 経路はエンドツーエンドで検証済みです。コネクタの開始(「Start CDC」)と offset-seeder
> (ウォーターマーク/ギャップの無い引き継ぎ)の経路は別途強化中です。

## v0.1.6

### Fixed

- **CDC インフラの配備が、公開イメージで動作します。** 「Deploy CDC infrastructure」が実行時に読み取る cdc-stack の
  CloudFormation テンプレート(`deploy/cdc-stack/cdc-stack.yaml`)がコンテナイメージにバンドルされていなかったため
  (Dockerfile がそれをコピーせず、`.dockerignore` が `deploy/` を除外していた)、クリーンなイメージは「Could not read the
  cdc-stack template」で失敗していました。いまやテンプレートはイメージにバンドルされています。

### Changed

- デフォルトの `ContainerImageUri` を公開されている `:0.1.6` イメージに引き上げました(新規の配備が CDC テンプレートの修正を含むように)。

## v0.1.5

### Changed

- **CDC の配備コスト見積もりが、月あたりではなく時間あたりで表示されます**。これは、ツールが CDC パイプラインを(カットオーバー期間の間)
  一時的に使用することに合わせたものです。そして、パイプラインが使用しない **Glue が** 列挙されたコストドライバーから **削除** されました。

## v0.1.4

### Fixed

- **Schema Conversion が、サポートされない空間型で空白にならなくなりました。** MySQL の空間型(`POINT`、`LINESTRING`、`POLYGON`、…)を
  使用するテーブルは、以前は Schema Conversion 手順全体を中断させる `sqlglot` の `ParseError` を発生させていました。この失敗はいまや
  テーブルごとに分離されます: 影響を受けたテーブルは明確な理由(空間列を名指し)とともに `UNSUPPORTED` に分類され、残りのテーブルは
  引き続き変換されます。
- **Migration plan 手順の「Deploy CDC infrastructure」ボタンが、いまや機能します。** クリックは静かな no-op でした。非同期の
  confirm-dialog/deploy ハンドラが `await` なしで呼び出されていた(コルーチンが await されることがなかった)ためです。ハンドラは
  いまや await されるため、確認ダイアログが開き、配備が開始します。

### Changed

- **app-stack のネットワークガードレール。** `AllowedIngressCidr` のガイダンスが明確化され(インターネット向けの ALB → 自分の
  パブリック IP を `x.x.x.x/32` として設定)、`SourceDbSecurityGroupId` / `SourceDbCidr` のうち少なくとも一方を必須とする新しい
  `SourceReachabilityRequired` ルールにより、タスクが常にソース DB への egress を持つようにしました(配備後の静かな「ソースに接続
  できない」を防止)。
- **AI 支援モデルの選択。** `BedrockModelId` は、いまや Anthropic のキュレーションされたドロップダウンになり、タスクロールの
  `bedrock:InvokeModel` のスコープは選択されたモデルから自動的に導出されます。`BedrockModelArns` は任意のオーバーライドになります。
- **`CertificateArn` のテスト経路を文書化しました。** デプロイガイド(EN/KO)が整理されました: より明確な任意のセクション、そして
  パブリック IP / テスト証明書の前提条件が前面に表面化されました。

## v0.1.3

- 以前に公開されたベースライン(ECR Public イメージ `:0.1.3`)。
